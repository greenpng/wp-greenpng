# 16. iss/ ISS-10 闭环核验报告评审结论 (Review Verdicts)

> **评审对象**：`iss/ISS-10-POST-V1-0-1-HOTFIX-CODE-AUDIT-AND-CLOSURE-VERIFICATION.md`（同事对 v1.0.1 热修（Phase 7 H1~H7、ADR-0009、`15` 裁定）的代码级闭环核验与架构质量审计）。
> **评审日期**：2026-09-13（本仓库本地时钟口径，见 §1 末行日期口径注）。**评审方法**：逐项核对 git 度量、源码行号、DDL、既有实测记录（`13` §10、ADR-0008、ADR-0009、CI 运行记录）。
> **本文是 ISS-10 在本仓库的唯一引用口径**：引用其结论时以本文核验结果为准，不直接引用其原文表述（与 `14`/`15` 同构）。ISS-10 未提出新的缺陷论断——其价值在闭环确认与 v1.1/v1.2 排期建议，采编见 §3。

---

## 1. 事实核验记录

| 核验项 | 结果 | 核验证据 |
| :--- | :--- | :--- |
| 审计基线数字：33 个修改文件、+1,797/-82 行 | ✅ 逐字一致 | `git diff v1.0.0..v1.0.1 --shortstat` 复算：`33 files changed, 1797 insertions(+), 82 deletions(-)` |
| 单测 679 例全绿 / phpcs 零警告 / PHPStan L6 清洁 / E2E 12/12 | ✅ 一致 | 本仓库实测同数（`13` §10 H7 行 + v1.0.1 推送 CI 10/10 绿，main 与 tag 双跑） |
| 探针信号权重 40/30/20/10（webdriver/软渲染/无头窗/语言异常） | ✅ 逐字一致 | `plugin/assets/js/gr-probe.js` 评分块：`webdriver ? 40` + `softwareRenderer ? 30` + `headlessWindow ? 20` + `languageAnomaly ? 10`，`Math.min(score, 100)` 封顶 |
| 阈值 70 = 双信号佐证（40+30 或 40+20+10），单一 webdriver 40 不定罪 | ✅ 一致 | ADR-0009 D1 原推导；权重实测如上行 |
| C1/C2 闭环（`is_bot` 双向可置 1，CAPI/GA4 网关恢复拦截力） | ✅ 与实测一致 | `13` §10 H2/H3 行：:8091 实弹 signal(87) → 会话行 `is_bot=1, bot_score=87`；消费方 `visitor_bot_verdict()` 读真实结论 |
| C3 闭环（前门执法 + URL 豁免正交） | ✅ 与实测一致 | `13` §10 H5 行：封禁 127.0.0.1 → 前台/REST 双 403 + `ip_ban` 折叠行，删规则 200 复原；报告未提的补充事实：折叠行 `action_taken='blocked'`（拒绝与观察的动作字区分） |
| C4 前半闭环（生而 paid 订单经状态钩子绑定） | ✅ 与实测一致 | `13` §10 H6 行：订单 pending→processing 即绑、completed 重放仍恰 1 行；机理表述（Store API COD 生即 processing、`payment_complete` 结构性不可达）为 ADR-0008 round 14 实证复用，无误 |
| G1 闭环（5 控件 + 架构不变式测试） | ✅ 与实测一致 | `13` §10 H1 行 + E2E `the engine dials save from the security tab` ✓ 6.1s（真 UI 往返） |
| H4 shutdown 时序论述（init 标记会晚于会话行落地、`register_shutdown_function` 保证 wp_die 后执行、无会话行路径零行属诚实边界） | ✅ 一致 | ADR-0009 D2 同源；`15` §2 补丁 3 改造记录 |
| "`gr_conversions` 唯一键 `uk_source (source_type, source_id)`" | ❌ **索引名误引** | DDL 实名 `source_unique (source_type, source_id)`（`05` §3.3）。约束本身真实存在、语义引用无误，仅名称写错——`14` 同类"不实引用"先例：记录更正、不改原文 |
| 文内代码行号锚点 | ⚠️ 大体准确、三处偏差 | 实测：`apply_probe_score` 158 行（文称 157）、`arm_marker` 120 行（文称 108）、`front_door` 94 行（文称 65，疑指 run() 区域）；`is_static_banned` 91 行（文称 90）、Woo 状态钩子 115 行、不变式测试 300 行均准确。按内容定位无误，行号不作引用依据 |
| "24 小时内完成架构推演→发布全流程" | ✅ 属实且保守 | 实测约 **75 分钟**：v1.0.0 tag 推送 2026-09-13T11:16Z → v1.0.1 tag 推送 2026-09-13T12:31Z（GitHub Actions 运行时间戳） |
| 评级表述（"卓越（Exceptional, A+）"、"工业级坚固底座"） | 参考 | 评级类表述不作事实引用（`14` 先例）；技术论断的核验见上各行 |
| 日期口径 | 注 | ISS-09/ISS-10 自署 2026-09-14（同事所在地时区领先）；本仓库全部实测按本地时钟为 **2026-09-13**（GitHub 推送戳 2026-09-13T12:31:19Z = 本地 20:31）。v1.0.1 文档中误沿用的 09-14 已于本轮勘正（§4 第 4 条） |

## 2. 对 ISS-10 复审 docs/15 既有裁定的认定

ISS-10 §2 对 `15` 两项关键裁定的复审（C4 后半退款冲销延期 v1.1、C5 后半"表单提交=默示同意"驳回）均表赞同且**未提出新证据**。本评审确认：两项裁定维持原状，无需新决策记录——理由链已完整在案（`15` §1/§2、ADR-0005、ADR-0009；ISS-10 §2.1 对 UNIQUE 键约束与 7 天重算窗的复述与 `15` §4 一致，唯索引名误引见 §1）。

## 3. 对 v1.1 / v1.2 排期建议的采编

| ISS-10 建议 | 裁定 | 理由 |
| :--- | :--- | :--- |
| G2：v1.1 随包预置离线机房 CIDR 与 Tor 出口列表（零外呼） | **维持讨论项，不自动排期** | 两问未解：① Tor 出口列表小时级轮换，静态随包必然快速陈旧——"站长显式按钮更新"模式（铁律 1）对 DB-IP 月度库成立，对高频轮换列表的更新语义完全不同类；② 机房段库数据源许可与归属（逐源核验未做）。v1.1 启动时随 IP Intelligence 页扩面 ADR 一并定夺。`15` §4 原裁定（维持讨论）不变 |
| G7：一次性临时邮箱域名拦截 | 采纳为 **v1.2 评估项** | `15` §4 原列维持讨论；ISS-10 建议明确为 v1.2 评估——记录此定位，评估结论不预承诺 |
| 战役级无效流量报表（v1.1） | 记 **v1.1 开放项**，不自动排期 | 新想法：`visitor_bot_verdict()` 结论 × 战役维度聚合。需求形状待真实数据积累验证（v1.0.1 后各站才有第一批评定数据）；与 Analytics & CAPI 页扩面同议 |
| G3（A/B 创建表单）/G4+G5（访客会话列表、在线数与设备上屏）/G6（表格日期筛选/搜索/CSV） | **已在 `12` v1.1 显式补齐**（本轮落地，§4 第 1 条） | `15` §4 所称"并入 docs/12 既有 v1.1 范围"至此名实相符 |
| 其余 §三 矩阵行（01/02/04/05/10 核心达标；03 维持现状；08/09 维持现状） | ✅ 与 `12`/`15` 既有映射一致 | 无新事实、无新裁定 |

## 4. 本轮落地的闭环收尾（文档完善）

1. **`12` v1.1 显式补齐** G3/G4/G5/G6 四项（漏斗行增 A/B 实验创建表单；新增"运营面板"与"表格增强"两行）。
2. **`README`（docs 中心索引）补漏**：文档索引补 `15`/`16` 两条（`15` 上轮创建后漏登记）；ADR 索引补 0008/0009 两条（同漏）；`13` 行任务数 85→92 更新。
3. **`iss/README` 索引补 ISS-10 行**。
4. **日期勘误**：v1.0.1 文档中 11 处"2026-09-14"按本地实测时钟勘正为"2026-09-13"（ADR-0009 头、`05` §3.2、`10` §4、`13` 修订记录与 H1~H7 行、`15` 头）。
5. **`13` 修订记录**补本轮一行。

## 5. 开放项

- G2 两问：Tor 出口列表的陈旧性与更新语义；机房段库数据源许可与归属（v1.1 IP Intelligence 扩面 ADR 时定夺）。
- 战役级无效流量报表的需求形状（v1.1 与 Analytics 扩面同议）。
- 退款冲销三项开放问题（v1.1 启动时立 ADR，`15` §5 既有）。
- 检测路标记覆盖边界重评（v1.1 行为模块扩面时，`15` §5 既有）。
- `bot_verdict_threshold` 默认 70 的站点实测校准（首发后按真实分布回调，`15` §5 既有）。
