# 13. v1.0 实施清单 (v1.0 Implementation Plan)

> 本文把 `12-roadmap-free-v1.md`（2026-09-09 四支柱修订版）的 v1.0 范围拆解为**可独立验收的任务序列**，是 v1.0 开发期的工作真源：任务只做清单内的事，验收只认清单内的标准。
> **修订记录**：2026-09-09 依据 ADR-0007 重构——新增客户端探针（C13）、A/B 引擎（C14–C15）、出网支柱（I1–I5）、独立 Settings 页（U13）、UA 引擎升级（W2）等；任务 61 → 85 项；§8 开放问题全部收口。2026-09-10 §2–§7 任务表补齐状态列（落实 §9 状态列纪律）。2026-09-10 开发顺序调整（站长授权代理排序）：S9 提前使 §3.3 #2–#4 即时生效；S3 顺延至 S4–S7 之后统一接线服务。2026-09-13 新增 §10 Phase 7（v1.0.1 热修复 H1–H7：ISS-09 深度审计经 `15` 逐条核验后落地，决策记录 `adr/0009`；85 → 92 项）。2026-09-13 ISS-10 闭环核验评审（`16`）落地：`12` v1.1 显式补 G3/G4/G5/G6、docs 中心索引补 `15`/`16` 与 ADR-0008/0009、v1.0.1 文档 11 处日期勘正（09-14→09-13，本地时钟口径）。
> 状态图例：⬜ 未开始 · 🔧 进行中 · ✅ 已验收（AGENTS.md §3.3 四项检查通过 **且** 验收标准实测达标；S1–S8 期间工具链未建（S9 交付），以检查 #1 与任务自身验收标准为准，#2–#4 自 S9 起强制补跑）。
> 更新纪律：每完成一个任务，本表状态列随该任务的代码提交一并更新；禁止提前打勾；验收数字必须实测可复现（`AGENTS.md` §3.4）。

---

## 0. 任务总览

| 阶段 | 内容 | 任务 | 依赖 |
| :--- | :--- | :--- | :--- |
| Phase 0 | 开发与验证环境（ADR-0006/0007） | E1–E9 | — |
| Phase 1 | 插件骨架与存储底座 | S1–S10 | Phase 0 |
| Phase 2 | 核心运行时 + 采集 / 归因 / A-B | C1–C15 | Phase 1 |
| Phase 3 | 流量安全与 RPA 识别 | W1–W14 | Phase 1（W4/W6/W14 依赖 C2/C5） |
| Phase 4 | 汇总与后台 14 页 | U1–U17 | Phase 2、3、5 |
| Phase 5 | 出网支柱 + 隐私合规 | I1–I5、V1–V5 | Phase 1（I2–I4 依赖 C10 转化链路） |
| Phase 6 | 测试收口与发布准备 | T1–T10 | 全部 |
| Phase 7 | v1.0.1 热修复（ISS-09 评审落地） | H1–H7 | Phase 6（依据 `15` + `adr/0009`） |

共 92 项。Phase 1 先行；Phase 2 / 3 / 5 三线可并行（互不依赖）；Phase 4 依赖前三者的读接口。

## 1. Phase 0 — 环境准备

| ID | 任务 | 状态 | 验收标准（实测） |
| :--- | :--- | :--- | :--- |
| E1 | MariaDB 安装（Homebrew） | ✅ | `mysql --version` 返回 MariaDB；安装日志无错误 |
| E2 | MariaDB 服务 + 库/账号 | ✅ | 实测 2026-09-09：TCP 127.0.0.1:3306 `SELECT VERSION()` = 12.3.3-MariaDB；`greenpng_site` 库与 `gr_site`/`gr_admin` 账号存在；凭据只存 `greenpng-dev/CREDENTIALS.txt`（仓库外，600 权限，不入 git）。注：MariaDB 12 默认 skip-networking，已用 `/usr/local/etc/my.cnf.d/gr-dev.cnf` 显式 bind 127.0.0.1 + port 3306 开启 TCP |
| E3 | MySQL 主验证站 | ✅ | 实测 2026-09-09：`http://localhost:8091` HTTP 200；`wp core version` = 7.1；站点位于 `/Users/macos/greenpng-dev/wordpress`（与仓库分离，frankenphp php-server :8091） |
| E4 | 生态插件链接与激活 | ✅ | 实测 2026-09-09：9 个生态插件 symlink 就位；激活 woocommerce 11.1.0 / fluentform 6.2.13 / limit-login-attempts-reloaded 3.3.7；automatorwp / cartflows / fluent-crm / wp-slimstat / stream / user-submitted-posts 装入未激活 |
| E5 | WP-CLI 工作方式固化 | ✅ | `greenpng-dev/NOTES.md` 已记录 PHPRC 内存方案、服务启停、环境事实 |
| E6 | agy-suite 停用预案 | ✅ | 实测 2026-09-10：greenpng 装入 :8091 前已在 :8090 执行 `wp plugin deactivate agy-suite` 成功；`wp plugin list` 实测 `agy-suite inactive`；:8090 站点 HTTP 200 |
| E7 | greenpng 插件目录链接 | ✅ | 实测 2026-09-10：symlink `greenpng -> /Users/macos/greenpng/plugin` 就位并经 :8091 `wp plugin list` 可见 |
| E8 | 参考环境保留 | ✅ | `:8090` SQLite 站原样保留，仅作参考环境（ADR-0007：不承诺 SQLite 兼容，不设兼容门禁任务） |
| E9 | 常用插件补装（OQ-4） | ✅ | 实测 2026-09-13：五插件自 wordpress.org 官方 downloads 端点取 latest-stable zip——contact-form-7 6.1.7 / wpforms-lite 2.0.1.1 / elementor 4.2.4 / wp-super-cache 3.1.3 / wordpress-seo 28.4；沙盒外域经核心 HTTP 结构性拒发（DNS 映 198.18/15 + SSRF 硬化，I1 既有结论）→ 直接 curl 走映射代理下载、本地 zip 离线安装（五连 `Success: Installed 1 of 1`）；`wp plugin list` 五行就位且**全 inactive**（装而不启——激活时机随对应适配器/共存测试任务；dev 基线零扰动：greenpng 仍 active、WooCommerce 11.1.0/FluentForms 6.2.13/limit-login 状态原样）；代码零改动，四检查沿用 V4 全绿（649/3878）；T2/T7 矩阵任务按需激活 |

## 2. Phase 1 — 插件骨架与存储底座

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| S1 | 主入口 | ✅ | `plugin/greenpng.php`：插件头（`Requires at least: 6.0`、`Requires PHP: 7.4`、GPLv2+）、`GR_VERSION`、ABSPATH 守卫、PHP/WP 门槛不满足时仅 admin notice 不致命 | `php -l` 通过；主验证站激活成功。实测 2026-09-10：双路语法检查通过（本机 FrankenPHP php 不支持 `-l` 旗标，改用直接执行至 ABSPATH 守卫的全量编译检查 + `token_get_all(TOKEN_PARSE)`，方法记入 NOTES.md）；:8091 `wp plugin activate greenpng` 成功；`wp eval` 实测 `GR_VERSION=1.0.0`、`GR_PLUGIN_DIR`/`GR_PLUGIN_URL` 已定义；前台 HTTP 200；debug.log 无本插件条目（grep 唯一命中为 09-09 旧 WooCommerce JIT 提示中的站点目录名 greenpng-dev，与本插件无关）。phpcs/phpunit 已随 S9 补跑通过（phpcs exit 0；EntryHeaderTest 5 tests 17 assertions OK） | `04` |
| S2 | Autoloader | ✅ | `includes/core/class-gr-autoloader.php`：`GreenPNG\` → `includes/<模块>/class-gr-<slug>.php` 映射，无 Composer | 实测 2026-09-10：AutoloaderTest 7 tests（夹具树真实加载 core / Gr_ 前缀剥离 / 下划线转连字符 / 深层 Ecosystem 路径 / GR_PLUGIN_DIR 回退 / 外部命名空间·无模块·缺文件三种静默路径——phpunit failOnWarning 下零告警）；:8091 `wp eval` 实测 autoloader 在 SPL 链（registered=yes）、`spl_autoload_call` 未知类零输出（silent_call=ok）、前台 HTTP 200、debug.log 零 GreenPNG 类名条目。类名定稿 `Gr_` 前缀（docs/02 §2.1 与 docs/03 §1/§2 返回类型同步修正） | `04`、ADR-0003 |
| S3 | 主控类 | ✅ | `includes/core/class-gr-plugin.php`：`plugins_loaded@10` 构造主控；Phase 1 协作方为静态门面（`Gr_Queue::boot`、`admin_init → Gr_Schema::maybe_upgrade`、`init@10 → load_translations`、`Gr_Cli::register`）于 `register_hooks()` 显式接线，实例服务自 C 阶段起构造注入（落地形态记入 `02` §2.1）；入口文件收薄为 `add_action('plugins_loaded', …, 10, 0)` | 实测 2026-09-10：PluginTest 3 tests（类解析 + 双次 run 幂等/三钩注册/每日排程仅一条、admin_init 回调恰为 `Gr_Schema::maybe_upgrade`、入口文件 plugins_loaded 接线契约），全套 50 tests 702 assertions OK，§3.3 四项全绿。:8091 实测：`has_action` 验证 plugins_loaded=10 / admin_init=10 / init 回调在位；删除 `gr_queue_daily_event` → 下一请求经控制器自愈恢复；`wp greenpng maintenance` 经控制器注册 Success；前台 HTTP 200、debug.log 零 GreenPNG 条目。服务清单静态可查（构造签名 + `register_hooks()`）；PHPStan 全量覆盖按 T4 收口 | `02` §2.1 |
| S4 | 设置服务 | ✅ | `includes/core/class-gr-settings.php`：唯一 autoload=yes 的 `gr_settings`（默认值集中定义，≤8KB），含安全/探针/归因/隐私默认 | 实测 2026-09-10：SettingsTest 6 tests（默认逐项对齐 ADR-0007、serialize 560 字节实测 <8KB、autoload=yes 写入、幂等不重置、默认合并、set 持久化）；:8091 `wp eval` 实测 install 后 `gr_settings` 数组就位（probe_default=1）、`gr_*` autoload=yes 恰 1 行（option list 实测）。"激活后 option 存在"字面闭环已随 S6 收口（:8091 重装重激活实测 gr_settings 在位） | `05` §6 |
| S5 | Schema | ✅ | `includes/storage/class-gr-schema.php`：`DB_VERSION=1`；15 个物理对象 DDL 经 dbDelta；**补索引**：`gr_sessions(last_active)`、`gr_contact_tags(tag_id, contact_id)`、`gr_daily_stats` UNIQUE `(stat_date, metric_type, metric_key)`；`PRIMARY KEY` 双空格保险写法；`gr_db_version` autoload=no；激活 + `admin_init` 双挂载 | 实测 2026-09-10：SchemaTest 10 tests（15 对象及次序对齐 docs/05 §2、双空格 PK、VARCHAR≤191、零外键、行模板+逗号、三补索引、§3.1–§3.3 锚点、`table_names` 派生、版本常量、`store_version` autoload=no 创建/更新双路径），全套 33 tests 640 assertions OK，§3.3 四项检查全绿。:8091 实测：全新安装 0→15 张 `wp_gr_*`；`SHOW INDEX` 实证三补索引落地（last_active / tag_contacts / stat_unique）；二次 dbDelta **零变更**；`gr_db_version=1` autoload=off，且仅在 15 表齐备后写入（`all_objects_present` 闸门；drop 1 表 + 版本滞后 → `maybe_upgrade` 自愈复建 14→15 + 版本回写）；前台 HTTP 200、debug.log 零新增行零插件条目。两项 dbDelta 纪律经首轮实测失败定位并回写 `04` §3.11.8 与 `05` §3：字段行必须逗号分隔（dbDelta 对新表按原文执行 CREATE，无逗号 15 表全败）；整数必须带核心同款显示宽度（`BIGINT(20)`/`INT(10)`/`TINYINT(3)`，MariaDB 宽度失配每轮全量 CHANGE COLUMN；MySQL ≥8.0.17 由 dbDelta 显式忽略纯宽度差，upgrade.php 源码实核）。激活/admin_init 钩子挂载随 S6 落地后按连续激活两次字面复测 | `05`、ADR-0007 |
| S6 | 生命周期 | ✅ | `includes/core/class-gr-activator.php`（激活 = Settings + Schema 幂等安装）/ `includes/core/class-gr-deactivator.php`（清 `DAILY_HOOK`，不删数据）/ `includes/storage/class-gr-uninstall.php` + 根级 `uninstall.php`（无 autoloader 场景直 require 两类文件；`ABSPATH` + `WP_UNINSTALL_PLUGIN` 双守卫） | 实测 2026-09-10：LifecycleTest 5 tests（三类经真实 autoloader 解析、`DAILY_HOOK` 名对齐 docs/05 §5、入口激活/停用钩子注册契约、uninstall 守卫先于 require 的顺序契约、删除旗标默认保留且 '1' 显式开启），全套 38 tests 661 assertions OK，§3.3 四项检查全绿。:8091 全链路实测：伪排 `gr_cron_daily_maintenance` → 停用后 cron 0 条而 15 表 + `gr_db_version`/`gr_settings` 全保留；默认卸载（`wp plugin uninstall` 实跑 uninstall.php）数据全保留；旗标置 1 卸载 → 0 表 + `gr_*` option 清零（含旗标自身），证明 uninstall.php 经 WP-CLI 真执行；重装重激活幂等（S4"激活后 option 存在"字面闭环收口，S4 行同步标记）；前台 HTTP 200。停用清理项将随 Gr_Queue 落地扩展（互斥锁 transient 与 AS 动作） | `05` §4–5 |
| S7 | 自适应队列 | ✅ | `includes/core/class-gr-queue.php`：`enqueue($hook, $args, $delay=0)` 运行时嗅探 AS（组 `greenpng`）/ 否则 `wp_schedule_single_event`；`with_mutex` transient 互斥锁 TTL 300s；内部 `EVENT_HOOK` 每日事件 → 互斥内 `do_action(DAILY_HOOK)`（防重入且不可递归）；`includes/core/class-gr-cli.php` 的 `wp greenpng maintenance`；boot 自愈排程 | 实测 2026-09-10：QueueTest 9 tests（无 AS 后端判定、派发路由+args+delay、负 delay 钳制、每日排程仅一条且缺失才补、boot 注册 runner+自愈、run_daily 单次触发公钩+锁释放、互斥防重入+释放恢复、with_mutex 异常后锁仍释放、teardown 清场），全套 47 tests 693 assertions OK，§3.3 四项全绿。:8091 实测：boot 自愈 `gr_queue_daily_event`（daily）；`wp greenpng maintenance` Success（零参方法经 WP-CLI 实跑可行）；持锁 `gr_queue_lock_daily` → Warning Skipped、释放 → Success；**双后端各一次实测**——AS：动作落 `wp_actionscheduler_actions` 组 `greenpng`（id=11 实查）；WP-Cron：停 woocommerce+fluentform（:8091 双 AS 提供方）后 `backend=wp-cron`，`enqueue(...,120)` 于 cron list 可见并删除；停用清场实测：pending AS 动作 → canceled（`cancel_actions_by_group`，AS 源码 functions.php:312 实读）、`gr_*` cron 0 条、重激活后每日事件自愈；前台 HTTP 200。发现并修复：命名空间内 `WP_CLI::` 解析为 `GreenPNG\Core\WP_CLI` 致 CLI Fatal（`use WP_CLI;` 修复；守卫式 lint 与单测均盲区）；`wp cron event list` 只列未来事件（delay=0 落 t0 下一 tick 执行），两条均记入 NOTES.md | `02` §2.4、ADR-0007 |
| S8 | readme + pot | ✅ | `plugin/readme.txt`（`== External services ==` 五项逐项披露：GA4 / Meta / TikTok / 自定 Webhook / DB-IP 更新按钮——目的·发送数据·时机·政策链接，全部默认关；`== Privacy ==` 双轨 IP·探针两模块·隐私 API·邮箱哈希+加密；`== Attribution ==` DB-IP CC BY 4.0；FAQ 含数据存放位置与卸载保留策略）+ `plugin/languages/greenpng.pot` | 实测 2026-09-10：readme 结构自查通过（Contributors、Tags 4≤5、Requires at least 6.0、Tested up to 7.1、Requires PHP 7.4、Stable tag 1.0.0 与文件头·`GR_VERSION` 三处一致、FAQ 数据存放、External services 逐项、DB-IP 归属可见）；POT 经 WP-CLI 2.12 `wp i18n make-pot` 生成，6 msgid（3 代码串 + 插件名/描述元数据），行号引用实测（greenpng.php:52、class-gr-cli.php:57/61），`X-Domain: greenpng`；`Report-Msgid-Bugs-To` 经 realpath 符号链接陷阱修正为真实 slug（记入 NOTES.md）；text domain 一致性由 phpcs `WordPress.WP.I18n`（ruleset `text_domain=greenpng`）持续把关；§3.3 四项全绿。注：readme 描述 1.0.0 全量功能（docs/12 口径），截图与包内 NOTICE 随 U/I 阶段补齐，POT 发布前随新增字符串再生成 | `08` |
| S9 | 规范工具链 | ✅ | `composer.json`（仅 dev 依赖）+ `phpcs.xml.dist` + `phpcs-compat.xml.dist` + `phpunit.xml.dist` + `tests/` + vendor 安装 | 实测 2026-09-10：§3.3 四项全绿（#2 phpcs exit 0、#3 PHPCompatibilityWP exit 0、#4 phpunit 10 tests 29 assertions OK；#1 见 S1 行）；`str_*` 夹具零误报（tests/fixtures/str_polyfill_fixture.php 实测 exit 0）。本机适配三项均实测定位并记入 NOTES.md：dealerdirect 写 conf 失败→installed_paths 双写 ruleset `<config>`；Generic.PHP.Syntax 因本机 `php -l` 坏死锁→块内 exclude（语法由 #1 覆盖）；PrefixAllGlobals MIN_PREFIX_LENGTH=4 硬拒短前缀→exclude + PrefixDisciplineTest 承接 | `11` §2 |
| S10 | 打包脚本 | ✅ | `tools/build-zip.sh`（plugin/ 直拷 + 防御性排除清单；NOTICE/数据目录检查：默认 warn、`--strict` 失败——数据文件随 geoip/UA 引擎阶段落位，T8 以 `--strict` 全量收口；产物 `dist/greenpng-<ver>.zip`，dist/ 已 gitignore）、`tools/bump-version.sh`（三处同步：文件头 `Version:` / `GR_VERSION` / readme `Stable tag`；perl -pi 跨 BSD/GNU；改后自校验，半改树永不放行） | 实测 2026-09-10：`bash -n` 双脚本通过；bump 负路径实测（无参 usage、同版本 already-at 均 exit 1）；1.0.0→1.0.1 实跑三处同步（对齐空格保留；phpunit 50 tests 702 assertions 于 1.0.1 仍绿——EntryHeaderTest 同步不变式设计生效）、git checkout 还原 1.0.0；build 正路径产出 `dist/greenpng-1.0.0.zip`（13 文件、`unzip -t` 零错误、结构含 greenpng/greenpng.php·readme·uninstall·pot、排除项 vendor/tests/docs/.git/composer/phpcs/phpunit grep 计 0）、NOTICE/数据缺失 warn 实测、`--strict` exit 1 实测。两项脚本缺陷经实测定位修复：header 替换漏 `$1` 致整行塌缩（脚本改后自校验当场拦截）、`GR_VERSION` 匹配漏行首缩进。注：DB-IP/CrawlerDetect 数据文件与包内 NOTICE 随 I/W 阶段落位，T8 `--strict` 收口 | `02` §5 |

## 3. Phase 2 — 核心运行时 + 采集 / 归因 / A-B

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| C1 | 事件 DTO 与门面 | ✅ | `Gr_Event`（不可变风格 DTO：私有属性+getter；payload 中 7 个上下文键提升为列并从 payload_json 剔除、按列宽截断防整行插入失败；空名抛 `InvalidArgumentException`；`mark_persisted()` 为包内约定唯一可变点）+ `Gr_Event_Dispatcher`（构造 → `gr_persist_event` 过滤器决定落库 → `do_action('gr_event', $event)`；次序保证订阅者可读到行 id）+ `Gr_Event_Repository`（storage 层，经 `Gr_Database::table()` 解析表名；LIMIT 钳制 1..500；payload_json 读写双向编解码）+ `gr_dispatch_event()` / `gr_get_recent_events()` 门面（各 1 行转发）；`Gr_Plugin` 首个实例服务接线（`events()`） | 实测 2026-09-10：:8091 `wp eval-file`——派发真实落库（persisted_id>0）、上下文键提升与 payload 保留并存、钩子收到**同一** DTO 实例且已带行 id、`gr_get_recent_events` 读回 payload 解码 + visitor/group 列正确、探针行清理 deleted=1/剩余 0、前台 200、debug.log 无新增（1965 行基线）；phpunit 68 tests 773 assertions（EventTest 11 例）；坑位实录：phpcs 插值豁免须用 `WordPress.DB.PreparedSQL.InterpolatedNotPrepared`（`NotPrepared` 不覆盖插值场景）；wpdb 测试桩 `prepare` 需为 `%s` 补引号方与核心行为一致 | `03` §2 |
| C2 | 表名解析 + 容器门面 | ✅ | `Gr_Database::table()`（Core，不触 `$wpdb`，DDL 派生名单校验 + 请求内缓存）+ `Gr_Schema::resolve_table()`（Storage，注入式 prefix，未知名抛 `InvalidArgumentException`，可容忍 `gr_` 前缀两种写法）+ `gr()`（`Gr_Plugin::instance()` 惰性单例、私有构造；钩子注册仍只在 `plugins_loaded@10`）+ `includes/gr-functions.php` 门面文件（入口 require_once 加载，函数无法 autoload） | 实测 2026-09-10：:8091 `wp eval-file`——`table('events')`=`wp_gr_events`、`table('security_logs')`=`wp_gr_security_logs`、`table('gr_events')` 与短键等价、未知名 `evnts` 实抛异常、`gr()` 三重一致返回同一 `Gr_Plugin` 实例、解析名经 `SHOW TABLES LIKE` 实证存在；验收 grep：schema DDL 之外表名字面量与 `prefix . 'gr_` 拼接均 0 处；phpunit 57 tests 717 assertions（DatabaseTest 7 例）；PrefixDisciplineTest 同步升级（正则补 `function_exists` 守卫内的缩进定义 + `gr` 为 docs/03 §1 注册特例）；debug.log 探针残留清理（备份 .bak-c2）。执行序注：C2 先于 C1 提交——C1 仓储依赖本解析器 | `02` §2.3 |
| C3 | IP 解析 | ✅ | `Gr_Ip_Resolver`（security 层；`filter_var` 验证 + 缺失/无效回退 `0.0.0.0`；经 `gr()->settings()` 读 `trust_proxy_headers`/`trusted_proxies`）+ `Gr_Ip_Matcher`（CIDR 原语：inet_pton 字节比较、部分字节掩码、裸 IP 视为 /32|/128、v4/v6 不互配——W 阶段 `gr_match_cidr()` 门面届时一行复用）+ `gr_get_client_ip()` 门面；`Gr_Plugin` 接线 `settings()` 实例服务 | 实测 2026-09-10：phpunit 81 tests 805 assertions（IpResolverTest 13 例——4 场景 + 多跳右扫/全可信回退/断链回退/匹配器 IPv4·IPv6·族不互配·/0·部分字节·垃圾输入零警告）；:8091 `wp eval-file` 四场景全对（伪造 XFF 无效 / 右扫越两可信跳取回客户端 / 未列 REMOTE_ADDR 不激活头 / 无效值回 0.0.0.0），设置改动经 `set()` 备忘失效路径生效、探后还原 0/[]，前台 200，debug.log 1965 行无新增；坑位实录：`Gr_Settings` 备忘 + 容器静态实例跨测试残留 → 增 `Gr_Plugin::reset_instance()` 测试缝（reset 桩内调用）；phpcs 超全局读需 `wp_unslash`，XFF 整头豁免的真实 sniff 码是 `ValidatedSanitizedInput.InputNotSanitized`（默认输出不显码，须 `phpcs -s` 实查） | `10`、`03` §1 |
| C4 | 密钥与签名 | ✅ | `Gr_Secrets`（AES-256-GCM，信封 = base64(iv 12B + tag 16B + 密文)；密钥 `hash('sha256', wp_salt('auth') . '\|greenpng-secrets')` 每次派生永不落盘；`store()`/`reveal()`/`forget()` 走 autoload=no option，openssl 不可用即拒存不降级明文；三原语：`hash_pii`（小写规范化 + 类型域前缀 + 空进空出）、`sign_hmac`（sha256 64hex 确定性）、`generate_event_id`（32hex；有熵 = 确定性哈希幂等收敛，无熵 = 16 随机字节））+ `Gr_Request::user_agent()`（超全局读点唯一：sanitize_text_field + wp_unslash + 512 截断）+ 四门面 | 实测 2026-09-10：phpunit 92 tests 847 assertions（SecretsTest 11 例——含空串信封、篡改 GCM 拒绝、密文不含明文、autoload=no 标记、熵收敛、UA 消毒截断）；:8091 `wp eval-file` 真盐实跑——往返 ok、信封逐次唯一、store 后 DB 原值为信封且不含明文（"密钥明文不落库"验收项实证）、reveal/forget/空回退 ''、hash 归一/类型隔离、HMAC 确定性 64hex（"输出确定性可测"验收项实证）、event_id 形状与熵收敛、UA：CLI 值 13 字符 + 600 字符/CRLF 合成值截 512 无换行；前台 200，debug.log 探针残留清理（备份 .bak-c4）；坑位实录：解密长度闸门 `<=` 误拒空明文信封（恰 28 字节为合法信封）改 `<`；wp eval-file 整文件先解析——链式 `===`（PHP 非结合）即全文件零输出；base64 豁免码实为 `DiscouragedPHPFunctions.obfuscation_base64_*` | `10` |
| C5 | 身份与会话 | ✅ | `Gr_Identity`（attribution 层：`gr_attr` 签名 cookie = `32hex . hmac-sha256(密钥=wp_salt 派生)`，`hash_equals` 验签、形状不符/验签失败即回退；`issue()` 仅在 `Gr_Consent::allows('marketing')` 通过时发送双 cookie——`gr_attr`（30 天，`attribution_cookie_days` 可调 1..365）+ `gr_session`（UUID，30 分钟滑窗）；标志位 HttpOnly/SameSite=Lax/Secure(is_ssl)/path=/；回退轨 = `sha256(wp_salt . 日期 . 域分隔符 . 匿名化IP . UA)`，跨天必然变化）+ `Gr_Consent`（privacy 层单检查点：DNT/Sec-GPC='1' 一票否决 → 有 WP Consent API 用之 → 无则回落 `marketing_consent_fallback` 设置开关，默认 0）+ `Gr_Privacy::anonymize_ip()`（IPv4 /24、IPv6 /48）+ `Gr_Session_Repository`（`INSERT ... ON DUPLICATE KEY UPDATE` 原子 upsert——落地属性仅首触写入、重复 touch 只滑 `last_active`/`pageviews+1`；`count_online()` 走 `last_active` 范围 COUNT，窗口钳制 30..3600s，cutoff 用显式 UTC DateTime 计算对运行时时区免疫）+ `Gr_Plugin` 接线 `identity()`/`sessions()` | 实测 2026-09-10：phpunit 110 tests 911 assertions（IdentityTest 14 例 + SessionRepositoryTest 4 例——含跨天旋转、/24 归一聚合、匿名化开关两态、DNT 否决、篡改 cookie 回退、落地白名单丢弃未知键）；:8091 实测——回退轨形状/稳定性/域分隔 ✓、签名 cookie 验证 ✓、篡改回退 ✓、**EXPLAIN type=range key=last_active（1200 陈旧行诱导，验收项实证）**、upsert insert=1/update=2/pageviews=2/落地 channel 保持、cleanup 1201 行删净后 online=0；**本站 WP 7.1 无 `wp_has_consent`** → 无 CMP 回落开关路径真站实测（denied→on=allowed→还原 denied）；前台 200，debug.log 1965 行零新增；坑位实录：`setcookie` 为内建函数全局不可重定义 → 命名空间级覆盖桩（`GreenPNG\Attribution\setcookie`）；stub 时钟覆盖未进 reset 跨用例泄漏（可覆盖旋钮统一 unset）；多行 prepare 字符串中 `{$table}` 行需把 ignore 注释放进参数列紧贴字符串行；`date()` 被嗅探禁止 → 显式 UTC `DateTime` 运算 | ADR-0007、`05` §3.2 |
| C6 | REST 采集端点 | ✅ | `Gr_Collect_Controller`（Rest 层，`rest_api_init` 接线）+ `Gr_Rate_Limiter`（Core 层）：permission 阶段三重防护（日盐令牌 `hash_equals`——`sign_hmac('collect\|日期', wp_salt 派生)` 非秘密跨日必变 → 每 IP 限流 60/60s `gr_collect_rate` 可调、`wp_using_ext_object_cache()` 真走 `wp_cache_add/incr` group `greenpng` 否则 per-key transient `gr_rl_collect_{md5(ip)}`、**被拒只读不写** → 8KB body）；严格 schema = 事件词表白名单（`pageview`/`signal`，`gr_collect_events` 过滤器可扩展）+ 字段白名单 9 键硬编码（**身份键不在白名单，服务端 `gr()->identity()` 双轨解析注入，客户端断言身份键按未知字段拒**）+ `signal` 要求 `bot_score` 整数 0..100 与四布尔标志、`probe_enabled=0` 时 400；`nocache_headers()`；token 刻意不标 `required`（核心必填 args 检查先于 permission 回调，401 归口 gate）；token 入口 `script_data()` 供 C13 enqueue | 实测 2026-09-10：phpunit 132 tests 1003 assertions（CollectControllerTest 17 例 + RateLimiterTest 4 例——transient/对象缓存双路径计数、键分散、被拒不写、401/429/413/未知事件名/未知字段/缺 bot_score/非布尔标志/probe 关闭/超长 path 全分支）；:8091 实测——无令牌 401 + 错令牌 401、超频 429（预置 ::1 键 60 后单发即拒、拒后计数停 60）、合法 JSON 8.3KB 413、未知事件名 400（args enum）、客户端 `visitor_id` 400 `field=visitor_id`、pageview/signal 200 `stored:true` + `Cache-Control: no-store` + `Content-Type: application/json`（sendBeacon 形态闭环）；SQL 预算 SAVEQUERIES 实测 6 条/请求（裸主机）/ 有对象缓存 3 条（见 `09` §1.1 落地记录与 `13` §8 张力待议）；清理 events 10→0 sessions 2→0 transients 0、debug.log 剔除探针残留 1988→1967（备份留档）；坑位实录：核心必填 args 先于 permission 回调 → token 不标 required 否则无令牌得 400 而非 401；`rest_do_request` 探针须显式 `set_header('Content-Type','application/json')` 否则核心不解析 JSON body；curl 本机回环走 IPv6 `::1`（限流键 ≠ `127.0.0.1`）；SQL 直删 transient 残留过期 `_transient_timeout_` 行会毒化后续 `set_transient`（add_option 失败 → get_transient 判过期删新值） | `02` §2.5、`09` §1.1、`11` §3.1 |
| C7 | 归因监听 | ✅ | `Gr_Attribution_Listener`（attribution 层，`template_redirect@10` 经 `Gr_Plugin` 接线，`listener()` getter）+ `Gr_Attribution_Params`（纯解析器：utm 五键 + gclid/gbraid/wbraid/msclkid→`cpc`、fbclid/ttclid→`social` 点击 ID 择一、utm_medium 词表映射 channel 闭合词表、`apply_referrer()` 纯函数把 direct 升级 referral——自家域排除归监听器）+ `Gr_Touchpoint_Repository`（storage 层：`record()` 全列宽截断 insert、`get_for_visitor()` 窗口钳制 1..365 天 + 显式 UTC cutoff + LIMIT 500 + `visitor_time` 索引序读）+ 门面 `gr_parse_attribution_params`/`gr_record_touchpoint`/`gr_get_touchpoints`。门控：`attribution_enabled` 管所有营销写（cookie/触点/会话落地属性）；`Gr_Consent::allows('marketing')` 一票否决（DNT/Sec-GPC）；**技术性会话滑动（last_active/pageviews）不受门控**——在线数是聚合计数；`Gr_Identity` 增请求内备忘（`issue()` 后同请求读 `visitor_id()/session_id()` 见 cookie 轨身份，修复入口触点落回退轨的结构性断裂） | 实测 2026-09-10：phpunit 149 tests 1093 assertions（AttributionParamsTest 7 + TouchpointRepositoryTest 3 + AttributionListenerTest 9——含无同意零 cookie 零触点、DNT 否决回落开关、直接访客发 cookie 不落触点、disabled 抑制营销写、纯引荐入触点、自家域不算、**跨天同 visitor_id（时钟旋钮）**、列宽截断、窗口序读 SQL 形状）；:8091 实测——DNT 请求零 gr_ cookie + 会话行 channel direct 零营销字段（**无同意不写 cookie 不落触点实证**）、同意入口双 Set-Cookie（gr_attr+gr_session）、触点行 channel=cpc/gclid/landing_url、会话行带落地属性且二访 pageviews=2 同 visitor_id（含 404 页与 cookie 回放两链）、**归因落地 SQL=3 条**（会话 upsert 1 + 触点 insert 2，≤4 预算达标，规则/cookie 0 SQL）；清理 touchpoints 3→0 sessions 4→0、回落开关还原 0、debug.log 1974→1967（备份留档）；坑位实录：测试 tearDown 不得 `unset($_GET/$_COOKIE)`（删全局变量本身 → 后续用例 Undefined variable，应赋空数组）；单测环境有 `wp_has_consent` 桩 → `Gr_Consent` 走桩而非回落开关，测试要用 `gr_stub_consent`；多行 SQL 字符串 `{$table}` 必须在首字符串行（ignore 只覆盖紧邻行，够不到字符串内部）；wp-cli 进程内驱动 `issue()` 走真 `setcookie` 产生 headers 警告（HTTP 语境不存在）；eval-file 探针别 unset `REQUEST_URI`（wp-cli cron shim 崩）也别 `(string)` 强转 SAVEQUERIES 行（是数组） | `02` §4、ADR-0005/0007 |
| C8 | 归因参数与模型 | ✅ | `Gr_Attribution_Models`（attribution 层纯静态）：`calculate(touchpoints, amount)` 一次算齐 5 模型——first/last（单点全取）、linear（均分）、position-based 40/20/40（n=1 全取、n=2 五五、n≥3 首尾各 40% 中间均分 20%）、time-decay（权重 `0.5^(age_days/7)` 相对最新触点，7 日半衰）；**分币对账**：逐触点 floor 到分、缺分按权重降序（平手给更晚触点）补齐 → 每个模型合计恒等于转化金额（含 `round(…,6)` 护浮点尘埃再 floor——0.4+0.1+0.1+0.4 二进制和为 1.0000000000000002 曾把 20.00 floor 成 19.99）；输入内排序（created_at, id）使调用方顺序不影响结果；空序列/单触点退化路径全覆盖；门面 `gr_calculate_attribution()`。**`gr_parse_attribution_params()` 已随 C7 落地**（`Gr_Attribution_Params` 纯解析器 + 渠道闭合词表，见 C7 行） | 实测 2026-09-10：phpunit 159 tests 1138 assertions（AttributionModelsTest 10 例——5 模型 × 手工序列全对：三触点 14d/7d/0d 的 time-decay 权重恰 1/7、2/7、4/7、金额 14.28/28.57/57.15；linear 33.33/33.33/33.34；position 40/20/40 与四触点 80/20/20/80；退化 n=1 全模型 99、n=2 五五；乱序输入与正序 assertSame；同时间戳按 id 决 first；空序列全空；门面转发）；:8091 实测——`gr_calculate_attribution()` 真站可调，canonical 序列五模型输出与单测逐位一致、各模型 sum=100；debug.log 1967 零新增（纯计算无落库）；坑位实录：浮点和尘埃必须 round(,6) 后再 floor（19.99 vs 20.00） | `03` §4 |
| C9 | 转化幂等绑定 | ✅ | `Gr_Conversion_Repository`（storage 层：`bind()` = `INSERT IGNORE` 打 `source_unique` UNIQUE 键 + insert_id>0 直返 / 否则按 source 回查既存 id——重放恒返同 id；amount 两位小数字符串化、currency 截 3 位大写、source_type 截 16；`id_for_source()` 点查）+ `Gr_Attribution_Service`（attribution 层组合：`get_for_visitor(30 天回看)` → `Gr_Attribution_Models::calculate()` 五模型随行 `model_weights` JSON + first/last_touch_id（直连 0/0 + 空模型）+ 当前 `identity()->session_id()` 侧行接）+ 门面 `gr_bind_conversion(order_id, visitor_id, amount, currency, source_type='woocommerce')`（可选第 5 参供 C11 表单源；**调用方持有同意门**——v1.0 适配器仅传 cookie 轨 id） | 实测 2026-09-10：phpunit 163 tests 1167 assertions（ConversionBindingTest 4 例——新绑定 INSERT IGNORE 形状/五模型随行/两位小数/币种归一、重放双查同 id（计数×2 因桩 prepare+执行双记录）、直连 0/0+空模型、门面 4 参文档签名）；:8091 实测——探针触点 + 同一 order 双绑定 **first_bind=1 second_bind=1 same_id=true conversion_rows=1（真 MariaDB UNIQUE 幂等实证，双回调仅一条记录）**、行内 amount '100.00'/currency 'USD'（小写入参归一）/first=last=touch_id/model_weights 五模型 JSON（单触点全模型 weight 1）；直连访客绑定 touch 0/0 + 五空模型 + fluentform 源类型；清理 2+1 行删净、debug.log 1967 零新增；坑位实录：INSERT IGNORE 被吞的插入仍消耗自增值（1→跳 2→3，非缺陷）；wpdb 桩 prepare 会 addslashes JSON 引号（断言要带 `\"`）；门面新仓储必须同步 use 导入（类名解析无全局回退） | `05` §3.3、`11` §3.2 |
| C10 | WooCommerce 适配器 | ✅ | `Adapter_Interface`（integrations 层契约：get_id/is_available/register_hooks）+ `Gr_Woocommerce_Adapter`（ecosystem 子层，实现接口）：**三挂载** = `woocommerce_checkout_update_order_meta`（经典，$order_id+$posted）+ `woocommerce_store_api_checkout_update_order_from_request`（Blocks/Store API，$order+$request——WC 11.1 实核存在）+ `woocommerce_payment_complete`；**HPOS 安全写法** = `wc_get_order` + `update_meta_data`/`get_meta` + `save()`（零 post meta 分支，两栖 posts/HPOS store）；`capture_visitor` 仅存 **cookie 轨** visitor_id（回退轨无跨天价值，ADR-0005）且 write-once（双路径先到先得）；`bind_order` = meta 锁 `_gr_attributed` + gr_conversions UNIQUE 双防线、无捕获时回落当次请求 cookie 身份、`Gr_Consent` 门控；每个回调 `\Throwable` 隔离 → `do_action('gr_adapter_error', id, $e)` 静默放行（§2.7 fail-open）；`is_available` = `class_exists('WooCommerce', false)`（公开面探针，无版本锁定/样本匹配，铁律 6）；Gr_Plugin 接线：`class_exists('WooCommerce', false)` **先于** 适配器类引用 → 无 WC 站点连类文件都不加载 | 实测 2026-09-10：phpunit 172 tests 1193 assertions（WooCommerceAdapterTest 9 例——**absent 目标零挂载 + Gr_Plugin 级跳过**（标记类未定义态、方法序 absent-first）、present 三挂载、经典/Store API 双形状捕获、双路径 write-once、回退轨不落单、支付绑定一次+锁、重放锁先于任何写、无同意跳过且不锁、爆炸 order 下 Throwable 隔离 + gr_adapter_error 上报）；:8091 实测（WC 11.1.0，**HPOS 功能在位但站点走 posts store**——写法两栖如实记录）——真钩子经典路径 `_gr_visitor_id` 落成、Store API 路径（真 `WP_REST_REQUEST`）write-once 不改写、`woocommerce_payment_complete` ×2 → `_gr_attributed`='1' + **conversion_rows=1（锁+UNIQUE 双防线真站实证）**、conversion 行 source=woocommerce/订单号/amount '100.00'/USD；清理订单 delete(true)+conversion 删净+回落开关还原 0；debug.log 剔除探针 fatal 块 1990→1967（备份留档）；坑位实录：**Woo 自带的 Store API 监听器（OrderAttributionBlocksController）对 null request 直接 fatal**——探针必须传真 `WP_REST_Request`（我们适配器对 request 参数零依赖故无恙）；直发 `do_action('woocommerce_payment_complete')` 只走钩子链不走 Woo 方法级状态流转（order status 保持 pending，对监听器验证无碍）；present 态测试用 eval 定义标记类 + absent 用例必须排前（进程内类定义不可撤销） | `02` §2.6、ADR-0007、`14` §1 |
| C11 | 表单适配器 | ✅ | `Gr_Form_Adapter_Base`（integrations/ecosystem 抽象基座，实现 `Adapter_Interface`）：**主钩直处理 + 回退钩停车哨兵**——回退钩命中只把提交停入 `pending` 并挂一次 `shutdown` 漂移哨兵，主钩命中立即绑定并清对应 pending（**主钩无论先发后发都赢**，三种钩序全兼容）；shutdown 时仍 pending = 主钩漂移 → 照常绑定（绝不静默丢）+ `do_action('gr_bridge_drift', bridge_id, source_id)` 上报供状态页；`process()` = cookie 轨身份 + `Gr_Consent` 门 + `Gr_Semantic_Extractor` 提取（无金额存 0.00、无币种存 ''——不入造数据）+ `attribution()->bind(source, visitor, amount, currency, get_id())`，**gr_conversions UNIQUE 天然幂等**（主+回退双发/钩重放恒一行）；`synthetic_source_id()` 每请求按 cache_key 只铸一次（无提交 id 的插件主/回退两钩同 id）；`guarded()` Throwable 隔离 → `gr_adapter_error`。三桥：`Gr_Fluentforms_Adapter`（主 `fluentform/submission_inserted` (insertId, formData, form)、回退 `fluentform_submission_inserted` 官方保留的兼容旧形（真源码实读：紧邻主钩前发）、在位探针 `defined('FLUENTFORM')`、对 $form 对象零依赖）；`Gr_Cf7_Adapter`（主 `wpcf7_mail_sent` (contact_form)、回退 `wpcf7_submit` (contact_form, result) 仅 status=mail_sent 时站哨、探针 `function_exists('wpcf7')`（生态插件同款）、posted data 经公开 API `WPCF7_Submission::get_instance()->get_posted_data()` 防御式读取、CF7 无持久提交 id → 每请求铸）；`Gr_Wpforms_Adapter`（主 `wpforms_process_complete` (fields, entry, form_data, entry_id)、回退 `wpforms_entry_saved` (entry_id, form_data)（条目存储开启才发——覆盖"主钩消失且存条目"场景）、探针 `function_exists('wpforms')`（Lite/正式同函数）、label 键归一（空格→下划线小写）使语义提取命中数字键字段值、entry_id 0 → 每请求铸）。Gr_Plugin 接线：三闸门先于类引用，无目标站点零加载 | 实测 2026-09-10：phpunit 195 tests 1337 assertions（FormAdaptersTest 14 例——三桥 absent 零挂载 + Gr_Plugin 级跳过（标记进程不可逆故 absent-first）、present 双钩挂载、FF 主钩绑定 1250.00/EUR、主+回退双发幂等零漂移、回退单发绑定 + shutdown 哨兵挂载 + `gr_bridge_drift` (fluentform, 9003)、CF7 mail_sent 经公开提交 API 绑定 12500.00/EUR、submit 回退仅 mail_sent 站哨（validation_failed 零停车）、合成 id 跨钩稳定（主清 pending 零重绑零漂移）、WPForms label 语义 99.50、entry_id 0 铸每请求 id、entry_saved 单发绑定 0.00/''（诚实空载荷）+ 漂移、无同意/无 cookie 双向零写、Throwable 隔离双上报、插件级三桥全挂）；:8091 实测（FF 6.2.13 真站）——主+回退钩已由插件挂进真注册表（has_filter true×2）、真 `do_action` 主钩 → conversion 行 **source=fluentform/91001/visitor=cookie 轨/amount '1250.00'/currency 'EUR'（语义提取真站生效）**、重放恒 1 行（真 UNIQUE）、漂移机制 → 第二行 '60.00'/'' + `gr_bridge_drift` 动作真站触发（closure 监听捕获 fluentform:91002）、**新进程 consent-off denied_zero_write=true**；清理 91000+ 行删净、回落开关 DB 还原 0、debug.log 2145→2126（备份留档）；CF7/WPForms 实测按既有口径顺延 E9/T2（wordpress.org 无网络）；坑位实录：**Gr_Settings 备忘 $values 首查冻结——同进程内 update_option 绕过备忘**，探针中途改设置造成"关不掉"假象（denied_rows=1 是探针自伤非适配器缺陷；正确姿势：改设置走 `Gr_Settings::set()` 或分进程实证）；wp eval 内联 SQL 必须先 prepare（裸 %s 直发即语法错误进 debug.log）；直发 FF 钩子传 null form 使 FF 自家监听器告警（我们桥对 form 零依赖，属探针噪音） | `02` §2.6、`12` v1.0、ADR-0007 |
| C12 | 语义提取与生态检测 | ✅ | `Gr_Semantic_Extractor`（integrations 层纯静态，`extract($payload)`：深度 8 封顶递归走查数组/对象——对象经 `get_data()`（WC CRUD 惯例）或公有属性并入；**email** 键提示词表 + RFC 值采纳双通道（url/link/href/ref/site/domain/sku/id 结尾键拒绝捐赠值）小写归一、**phone** 仅键命中且纯数字 ≥7 位、**first/last/full name** 键词表含 CF7 `your-name`、**amount** total/price/fee/calc 词表 + `$`/千分位剥离转 float、**currency** 3 字母大写；名字单侧检出自动推导另一侧（full→first+last / first+last→full）；`detected_keys` 语义→点路径；custom_fields `sanitize_text_field` + 64 键封顶）+ `Gr_Ecosystem_Detector`（`detect()`：单次 `get_option('active_plugins')`，四桥目录 woocommerce/fluentform/cf7/wpforms 固定序返回、wpforms-lite 归并、非数组值降级空目录）+ 门面 `gr_uif_extract_fields`/`gr_uif_detect_ecosystem`（use 导入 `GreenPNG\Integrations`）。**实施次序刻意先于 C11**（提取原语先行、C11 三桥消费，同 C2→C1 先例） | 实测 2026-09-10：phpunit 181 tests 1251 assertions（SemanticExtractorTest 9 例——CF7 形 your-name/your-email 提取与名字推导、FF 嵌套 names[first_name] 形 + 检出侧 vs 推导侧区分、`$12,500.00`→12500.0 + `eur`→EUR + 电话符号归一、`client_mail` 无标签 RFC 值采纳 / `invoice_url` 裸邮箱拒绝 / 混文值拒绝三态、`get_data()` 对象 + 9 层封顶 vs 7 层活读边界、空/标量/无 PII 载荷 + custom 64 封顶挤出后写的叶子 + 标签剥离换行折叠、生态报告全目录固定序 + wpforms-lite 归并 + 垃圾值降级、双邮箱候选首个胜出）；:8091 实测——FF 真实 `$formData` 形（嵌套 names + 邮箱小写归一 + `+86 138 0013 8000`→`+8613800138000` + 标语进 custom）、CF7 形（`$12,500.00`→12500.0/EUR/full_name）、**生态报告与站点真实状态一致**（fluentform 6.2.13 + woocommerce 11.1.0 active=1、cf7/wpforms active=0）、`auto:email` 语义（client_mail）真站采纳；纯函数零写入，debug.log 2126 零残留；坑位实录：rg 工具输出存在令牌损坏（连自建测试文件内容都显示错），内容核验一律用 Read；超长嵌套数组字面量括号易失衡，用循环构造；custom_fields 封顶以遍历序挤出后写键（保留序=遍历序） | `03` §7 |
| C13 | 客户端安全探针 | ✅ | `plugin/assets/js/gr-probe.js`（安全模块 v1.0 部分）：IIFE 失败静默、零依赖、零存储访问（无 localStorage/cookie/indexedDB——**无持久标识符**）、**指纹原始串不出客户端**（WebGL 渲染器串仅本地归类为"软件光栅器"布尔，SwiftShader/llvmpipe/Software/Basic Render/Mesa Offscreen/ANGLE Software 词表，浏览器拒供 debug_renderer_info 时诚实 false）；四结论 = `navigator.webdriver`（40 分）+ 软件渲染器（30）+ 无头窗口 outerWidth/outerHeight===0（20）+ `navigator.languages` 空缺（10），封顶 100；**score>0 才上报**（正常访客零行——数据最小化，合法利益范围 Recital 49）；传输仅 `navigator.sendBeacon` + Blob type application/json（字符串 sendBeacon 会以 text/plain 被 REST 拒——C6 教训）；配置经 `window.GreenPNGProbe`（url+token）由 `Gr_Probe_Script`（rest 层）`wp_enqueue_scripts` 注入：`probe_enabled` 门（关=**零脚本输出**非静默脚本）、is_admin 跳过、footer + `script_loader_tag` filter 补 `defer`（WP 6.0 无 enqueue strategy——兼容面用 filter，文档口径 defer 达成）、inline before 携 `Gr_Collect_Controller::script_data()`；服务端已在 C6 拒越界 bot_score；readme 探针披露段既有（结论值-only + 一键关 + 合法利益）。**行为模块（停留/滚动/怒点）为 v1.1——非本任务** | 实测 2026-09-10：phpunit 201 tests 1369 assertions（ProbeScriptTest 6 例——默认开 enqueue+footer+inline 配置含 token+插件级 wp_enqueue_scripts 挂载、**关开关零 enqueue 零 inline**、is_admin 零输出、defer filter 仅本句柄且 src 缺省不加、体积预算 raw 4247B < 8192 + gzencode 1875B ≤ 8192、静态契约 sendBeacon 在/fetch/XHR 禁/localStorage/sessionStorage/document.cookie/indexedDB 全禁/'signal'+bot_score 载荷键在、script_data 形状恰 url+token）；:8091 实测——前台 curl 见 `<script id="gr-probe-js" defer src=...gr-probe.js?ver=1.0.0>` + inline `window.GreenPNGProbe={"url":...,"token":...}`（单发无重复，sourceURL 注释非第二实例）；`settings()->set('probe_enabled',0)` 后 curl **零 gr-probe 字样**、还原 1；**真浏览器端到端（agent-browser 自动化 Chromium）**：页面加载 → `navigator.webdriver=true` + 探针配置在位 → sendBeacon → gr_events 落 **signal|probe|{"bot_score":70,"webdriver":true,"software_renderer":true,"headless_window":false,"language_anomaly":false,"path":"/"}**（SwiftShader 软渲染真实检出 30 分 + webdriver 40 分；新 headless 报实尺寸故 headless_window=false 如实）；禁 JS 站点正常（服务端渲染，curl 全页无 JS 依赖）；清理 signal 行删净 events=0、probe_enabled 还原 1、debug.log 2127→2126（备份留档）；坑位实录：单测 bootstrap 不加载主入口 → 命名空间类引用 GR_PLUGIN_URL 报未定义（bootstrap 补 GR_VERSION/GR_PLUGIN_URL/GR_PLUGIN_FILE 桩常量）；探针上报 score=0 静默是设计（普通访客不增长事件表）；gr_events 事件名列是 `event_name` 不是 `name`（DESCRIBE 先行，别再裸猜列名） | ADR-0007、`08`、`09` §1.1 |
| C14 | A/B 分流引擎 | ✅ | `Gr_Ab_Experiments`（funnel 层，实验定义仓储：非 autoload option `gr_ab_experiments`——首写 `add_option(...,'no')` 定死标志、**批清空时整行删除**不留 a:0:{} 空数组行；请求内静态备忘、写路径自失效；有界校验：≤20 实验 × ≤8 变体、键/变体 slug 化 ≤64/32、去重、<2 变体拒绝、脏存储批量 sanitize 防御）+ `Gr_Ab_Engine::assign(experiment, visitor_id)`（**一致性哈希**：`sprintf('%u', crc32(experiment|visitor)) % n`——%u 归一化使 32/64 位 PHP 同值；**URL 强制** `?gr_variant=` 仅当值在实验声明的变体表内才短路哈希，垃圾值回落自然桶（sanitize_key+白名单，无注入面）；未定义/停用实验返 ''）+ `Gr_Ab_Shortcode`（`[gr_ab experiment="..." control="..." treatment="..."]` 属性名=变体名：**不走 shortcode_atts**——真核该助手只保留 defaults 声明的键、动态属性会被剥掉（实测抓到），直接解析原始 $atts；fail-open：未知/停用实验回落 control 文本、'a' 键视为 control、缺 experiment 返 ''——页面永不因实验定义断掉）+ 门面 `gr_ab_assign_variant`（签名照 docs/03）。**曝光/转化事件落 gr_events 属 C15** | 实测 2026-09-10：phpunit 209 tests 1405 assertions（AbEngineTest 8 例——同 visitor 三次调用同桶+变体表内、1000 合成访客双桶 350+ 均衡带、未知/停用实验返 ''、强制参数命中声明变体/垃圾值与 script 标签值回落自然桶、option autoload='no' 实证+重复大小写变体归一+单变体拒绝+脏批 sanitize+删除往返+**空批整行删除断言**、短码渲染身份所分桶的内容（gr_attr 签名 cookie）、未知实验/缺 experiment/'a' 键三态 fail-open、插件级 add_shortcode 挂载）；:8091 实测——播种经仓储后 **autoload 列 = off（真 DB 实证非 autoload 纪律）**、同 visitor 进程内两次同桶、200 合成访客 split 109/91、`?gr_variant=treatment` 强制命中、垃圾值回落、**跨进程（新 wp-cli 进程同 visitor）同桶 treatment——真 DB 稳定分桶实证**、真 `do_shortcode` 渲染 = 身份所分桶内容（期望值吻合）、未知实验回落 SAFE；**真站抓到实缺陷**：真核 `shortcode_atts` 剥掉非 defaults 键 → 短码空返回（单测桩曾错误保留附加键故未拦）→ 重写为直接解析 $atts + 修桩对齐真核语义；空批残留 a:0:{} 行清理 + 修复路径真站实证 option_rows_after=0；events=0 零污染、debug.log 2126 零新增；坑位实录：**动态属性短码禁用 shortcode_atts**（真核只回 defaults 键，桩必须同语义否则假绿）；crc32 跨位宽须 %u 归一；静态备忘仓储要接进 stub reset（class_exists 守卫同 Gr_Plugin 模式） | `03` §5、`05` §2 |
| C15 | A/B 记录与显著性 | ✅ | `Gr_Ab_Recorder`（funnel 层：`record(experiment, variant, type)`——type 词表 {impression, conversion}、实验须已定义且变体在声明表内（**停用实验照记**：暂停前已分桶的访客转化不作废，否则低估每臂）、经 `gr_dispatch_event('ab', …)` 落 gr_events 专列 `ab_experiment/ab_variant/ab_type`（event_group=funnel，visitor/session 随行）——继承事件流的保留期与聚合体系，不建私有计数表）+ `Gr_Event_Repository::ab_counts()`（`GROUP BY ab_variant, ab_type` 聚合读，走 `ab_events` 索引，报表时才查、零前台路径）+ `Gr_Ab_Significance`（**双比例 Z 检验**：pooled p=(cA+cB)/(iA+iB)、SE=sqrt(p(1−p)(1/iA+1/iB))、z=(pB−pA)/SE round4；control（首变体）vs 其余逐对比对；**每臂 <30 曝光 → insufficient**；SE=0（双臂全零/全满）→ 诚实 inconclusive tie 不造 z；|z|≥1.96 → significant + winner=高转化变体 + confidence 95；实验总态 = 任一对显著→significant、任一对样本不足→insufficient、否则 inconclusive）+ 门面 `gr_ab_record`/`gr_ab_significance` | 实测 2026-09-10：phpunit 218 tests 1458 assertions（AbSignificanceTest 9 例——双类型落列 INSERT 断言（ab/event_name/group 逐列）、非法 type/未声明变体/未定义实验拒绝且零写、停用实验仍记录（in-flight 不丢）、**手工期望值**：10/100 vs 20/100 → z=1.9803 significant winner=treatment 95%；12/100 vs 14/100 → z=0.4205 inconclusive tie；5/20 vs 8/20 → insufficient 零 z；双臂 0 转化 → SE=0 诚实 tie；三变体两对（3.5921 显著 / 0.4118 不显著）单对显著抬总态、未知实验静默 insufficient）；:8091 实测——真站批量记录 283 行（精确期望 230+53）、行级真值 `event_name=ab/group=funnel/ab_* 三列满`、`gr_ab_significance` 真站输出 **z=1.9803/winner=treatment/conf=95/cvr 0.1 vs 0.2——与单测手工值逐位一致（真 MariaDB GROUP BY 聚合实证）**、20 臂实验 status=insufficient 三态闭环；清理 283 行删净 events=0、实验 option 双删 option_rows=0、debug.log 2126 零新增；坑位实录：**手算 sqrt 别心算**——0.041770 实为 0.0417582（z 3.5909→3.5921）、0.4117 实为 0.4118，PHP 对、注释错，以实现为准回填注释；多行 prepare 的 `{$table}` 必须在首字符串行（本任务又踩一次，ignore 只覆盖紧邻行） | `03` §5、`11` §3 |

## 4. Phase 3 — 流量安全与 RPA 识别（保守档：默认仅记录不拦截）

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| W1 | 请求检查器 | ✅ | `Gr_Request_Inspector`（security 层，`init@10` 经 Gr_Plugin 服务化接线 + `inspector()` getter）：**双层 Throwable 隔离**——外层 run() 整帧兜底（帧级异常静默跳过 + `do_action('gr_inspector_error','inspector',$e)`）、内层每检查独立 try（一检测器炸不断其余，错误上报带检查 id）；请求上下文一次构建（`Gr_Ip_Resolver::resolve()` 按 ADR-0007 信任策略、`Gr_Request::user_agent()/path()`——path 为新增截 191 派）；**检查器注册面 = 公开 filter `gr_inspection_checks`**（返回检查清单，W4/W9/W12 检测器后续挂入，真 WP 语义：过滤器回调改清单、帧自调 `check($context)`）；发现规范化 `rule_id`（sanitize_key 截 64）+ `reason`（截 191）两字段——检测器无法走私任意形状下游；发现汇总经 `do_action('gr_security_findings', $findings)` 出帧（W6 浪涌折叠日志将订阅）；门控：`is_admin` 跳过（管理面板保护归后续 W 任务）、`security_enabled` 关=零工作（filter 都不 apply）；默认仅记录（docs/10 §4 档 1，v1.0 无拦截路径） | 实测 2026-09-10：phpunit 225 tests 1488 assertions（RequestInspectorTest 7 例——插件级 init@10 挂载回调与优先级、上下文构建（ip/ua/method/path 全到检查器）+发现收集+findings 钩触发、**爆炸检查隔离**（幸存检查仍产出+`gr_inspector_error` 带 id 上报）、四类畸形返回规范化（slug 化/截宽/非数组/null 全拒）、**enabled/admin 双门**（关=filter 零 apply、admin 零工作）、帧级 filter 爆炸静默跳过不逃逸+上报 'inspector'）；:8091 实测——真注册表 `has_filter('init')=true`、裸站 inspect() 零发现零写、探针检测器经**真 WP filter 链**入帧产出规范发现（真 REQUEST_URI 上下文路径吻合）+`gr_security_findings` 真站触发、**security_log_rows=0 双测**（默认仅记录无订阅者=零持久化）、关开关 filter 调用数 0 vs 重开 1、还原默认 1、前台 200 无扰；debug.log 剔自伤 1 行回 2126（备份留档）；坑位实录：**探针/单测里检查器不能直接塞 `gr_stub_filters` 当检查清单**——apply_filters（真核与桩同语义）把回调当过滤器调（首参=初始值），正确形状=过滤器回调返回检查清单、帧自调 `check($context)`；自己笔记里的"eval 必须 prepare"再犯一次（SHOW TABLES 裸 %s 进日志） | `02` §2.7 |
| W2 | UA 引擎升级 | ✅ | `Gr_Scanner_Ua`（security/，静态引擎）+ 数据文件 `plugin/assets/data/gr-ua-crawlers.txt`（1468 条）+ `gr-ua-exclusions.txt`（52 条），两文件各带 5 行 MIT 版权头（来源/许可/数据日期）；包根 `plugin/NOTICE`（完整 MIT 许可文本 + 计数 + 提取说明）+ readme `== Attribution ==` 双处归属；门面 `gr_is_scanner_ua()`；**首个检测器** `Gr_Scanner_Ua::register_detector()` 挂 `gr_inspection_checks`（`Gr_Plugin::register_hooks()` 接线）→ rule_id `scanner_ua`、reason `ua:<命中片段>`；算法=Exclusions 剥离（剥空短路，纯浏览器 token 永不进爬虫正则）→ 1468 备选单正则交替（i）单次匹配；单匹配路径 `matched_token()`（`is_scanner()` 为其判定，检测器每请求只付一次匹配）；装载器跳 `#` 头/空行、保留模式行首空格（如 ` YLT`）；请求内静态备忘一次编译；数据不可读（is_readable 守卫）或 PCRE 失败（preg_replace null / preg_match false）一律**失败开放为普通访客**；零外呼 | 实测 2026-09-10：提取脚本做 PHP 单引号反转义（`\\`→`\`，捕获 `Zend\Http\Client` 一行的字节语义）后**与真 JayBizzle 库行为对齐校验 18/18 样本判定全同**（12 爬虫含 sqlmap/Nuclei/Googlebot/bingbot/curl/Wget/python-requests/Go-http-client/WordPress/SemrushBot/facebookexternalhit + 6 真浏览器含 iPhone/Android/Edge 全不误伤）；phpunit 235 tests 1539 assertions（ScannerUaTest 10 例——种子 12 判定、浏览器 6 不误伤、门面、空/纯 token 短路、matched_token 片段、缺目录失败开放、数据目录改写驱动判定、随包文件完整性 1468/52+版权头+NOTICE MIT 全文、检测器入帧（人类 UA 零发现）、插件级接线）；**耗时实测入报告**：独立进程冷路径 3.6–3.9ms（分解：装载 0.3–0.5ms + implode 0.03ms + PCRE 首编译 ~3.3ms——每进程一次，进程内缓存后 0.0023ms/次；:8091 真进程 init@10 走查完成首编译后稳态 0.011ms/判定，FPM 复用下 P95 ≈ 装载 0.5ms，记 `09` §1.1）；:8091 live——真路径（符号链接）1468/52 计数、8/8 判定、`filter_check_ids=scanner_ua`、`inspect()` 产出 `[{"rule_id":"scanner_ua","reason":"ua:sqlmap"}]` + `gr_security_findings` 真站触发、4 组 curl（sqlmap/Googlebot/Chrome/空 UA）全 200 仅记录无扰、debug.log 维持 2126 零残留；**数据文件入包**（build-zip 的 NOTICE/数据目录检查自此可命中）；坑位实录：本机 PHP 构建缺 `FILE_IGNORE_NEW_LINE` 核心常量（与无 `-l` 同源怪癖）→ `file_get_contents`+`explode` 全版本等价写法 + `is_readable` 先行守卫（缺失文件警告会被 PHPUnit 转异常）；命名空间内未限定全局常量不回退全局层（函数/类会回退，常量不会）——单测当场抓真缺陷 | `07` §3、`14` §1 |
| W3 | CIDR 匹配 | ✅ | 门面 `gr_match_cidr(string $ip, string $cidr): bool`（docs/03 §3 签名落地，转发既有 `Gr_Ip_Matcher::match_cidr()`——S 阶段随 IP 解析器实装的 inet_pton 字节前缀比较：IPv4 位掩码与 IPv6 前缀同一循环；裸 IP = /32（IPv6 = /128）等值；**v4/v6 永不互配**（含 `::ffff:a00:7` v4-映射写法，按 16 字节地址处理）；畸形输入（非法 IP/非数字前缀/负前缀/> 家族位宽）恒 false；列表入口 `match()` 跳过非字符串项） | 实测 2026-09-10：phpunit 244 tests 1594 assertions（CidrMatcherTest 9 例——**/32 精确**（±1 邻址拒）+裸 IP 等值、**/0 全配**（含 0.0.0.0 与 255.255.255.255 端点）、**/24 跨界**（.255 入/.114.0 出/.112.255 出 + 网段半主机位宽容）、/25 与 /31 部分字节掩码、IPv6 /128（压缩/全写双形等值）/0//48//64//52（半字节）//33（1 位）边界、两族互不配（4 组）、畸形 8 组恒 false、列表 API 首中即停+噪声跳过、门面与类逐值一致）；:8091 实测——真站 15/15 边界用例全对（v4 精确/跨界/部分字节 + v6 前缀/半字节 + 映射写法拒配）、`gr_match_cidr()` 真站可用、resolver 127.0.0.1、debug.log 维持 2126 零残留 | `11` §3.1 |
| W4 | 访问规则 | ✅ | `Gr_Access_Rules_Repository`（storage 层，唯一持 $wpdb 处：`active_rules()` 单条 SELECT 活跃行三列，表名经 `Gr_Database`，读失败/缺表降级空清单**失败开放**；写路径随 U 阶段 Access Rules 页）+ `Gr_Access_Rules`（security 层静态 L1：**懒加载一次备忘**——首次谓词调用才读，security 关/无消费者请求 0 SQL，符合 `09` §1.1 "规则 L1 命中 0~1 条"；谓词 `is_trusted_ip()`（allow×ip，经 `Gr_Ip_Matcher` CIDR/裸 IP）/ `is_ip_blocked()`（**allow 先评且恒胜**——文档"站长永不锁死自己"原则对静态封禁同样成立）/ `is_url_allowed()`（allow×url：**段安全前缀** `/checkout` 覆盖 `/checkout/thanks` 永不覆盖 `/checkoutzone`，含 `*` 值为全 URI 通配且其余字符 preg_quote 字面化，匹配前剥 `?query`）；未知 rule_type/match_kind/空值行忽略；门面三枚 `gr_is_trusted_ip`/`gr_is_ip_blocked`/`gr_is_url_allowed`；备忘重置接入 stub reset | 实测 2026-09-10：phpunit 252 tests 1636 assertions（AccessRulesTest 8 例——allow/ban IP 判定（含裸 IP /32 等值、**v6 窄 allow /48 ⊂ ban /32 对冲**真测 allow 恒胜）、段安全 URL（`/checkoutzone` 拒、query 免疫、`/docs/*` 通配 `/docs` 拒 `/docsx` 拒、ban url 不应答 URL 谓词）、通配值正则元字符字面化（`/a.b/*` 拒 `/axb/c`）、**一次读备忘**（四谓词调用 gr_access_rules SELECT 恰 1 条、reset 后恰再 1 条）、空规则集降级开放、未知形状/空值忽略、门面一致）；:8091 实测——真表 wp_gr_access_rules 种子 5 行（allow/ban ip 窄 allow 对冲 + url 前缀/通配）**11/11 判定全对**（含 allow 恒胜 `198.51.100.7` 拒封、`/checkoutzone` 拒、`/docs` 拒）；**num_queries 增量证一次读**（首谓词 +1、再三谓词 +0）；清理种子行归 0、debug.log 剔自伤 2 行回 2126（备份留档——**裸 `%s` 教训第三次重犯**：get_var 第二参是列偏移非参数，SHOW TABLES 必须 prepare）；坑位实录：测试中途换种子必须 `reset_for_tests()`（备忘按请求持有，否则旧种子串场）；allow/ban 夹具方向必须窄 allow ⊂ 宽 ban（宽 allow 会吞掉 ban 全域，"ban 但非 allow"地址不存在） | `05` §2、`09` |
| W5 | 临时封禁 | ✅ | `Gr_Temp_Bans`（security/，静态瞬态锁仓）：`block($ip,$reason,$ttl)`——IP 校验拒非法、TTL 钳 1..30 天（超限该走 W4 规则表持久封禁）、瞬态值携带 reason（191 截）+blocked_at+实际 ttl 供审计显示、`do_action('gr_temp_ban',ip,reason,ttl)`；`unblock()` 幂等（无锁=false 不算失败）+ `gr_temp_unban`；`is_locked()` 任意非 false 值即锁（键形状自持）；`lock_reason()` 供 CLI/状态页；键 `gr_block_`+md5(ip)（IPv6 冒号不入 option 名）。**`Gr_Access_Rules::is_ip_blocked` 三层序：allow 恒胜 → 临时锁 → 静态 ban**（docs/10"站长永不锁死自己"对瞬态锁同样成立，允许列表=无需 CLI 的自救阀）。门面 `gr_block_ip()`/`gr_unblock_ip()`（registry `: void` 签名）。**WP-CLI `wp greenpng unblock <ip>`**（Gr_Cli 增子命令：无参 error、无锁 warning 非失败（幂等恢复不吓站长）、释放 success，i18n 三串 + translators 注释）；transient 桩升级为**真 TTL 语义**（记 expires_at + `gr_stub_epoch` 数值时钟旋钮 + 过期清除，对齐真核行为） | 实测 2026-09-10：phpunit 262 tests 1676 assertions（TempBansTest 10 例——瞬态落位含 ttl/到期时点审计字段、**TTL 到期自释放**（299s 仍锁/300s 过期+条目清除+reason 空）、unblock 幂等报告、非法 IP 全拒且零存储、**TTL 钳位**（0→1、999999999→2592000）、is_ip_blocked 消费瞬态锁（无规则时纯瞬态定夺）、**allow 恒胜于瞬态锁**、ban/unban 动作携带负载、CLI 注册+释放+幂等 warning 零 error、CLI 无参抛 Gr_Cli_Error_Halt（真 error 语义））；既有测试随 TTL 桩升级对齐新存储形状（RateLimiter/CollectController 直读断言改 `['value']`、429 预置改新形状——**桩变真语义时旧直读断言必须跟形状**）；:8091 实测——真库瞬态行 102B + `_transient_timeout_` 行在位（真 TTL 机制）、**3s 短锁 sleep(4) 后真过期自释放**、reason 审计留存、**新进程 allow 规则压过活锁**（`blocked=false`；同进程评估=备忘按请求持有的正确语义，规则插入须下请求生效——单测教训的现场版）、真 CLI `wp greenpng unblock` Success + 幂等重复 Warning 引导语、清理 access_rules 0 行/block 瞬态 0/timeout 瞬态 0、debug.log 剔 phar 自身 106 行回 2126（备份留档） | `03` §3、`10` §4 |
| W6 | 浪涌折叠日志 | ✅ | `Gr_Security_Log_Repository`（storage 层）：`log($ip,$rule,$path,$ua,$reason,$window=0)`——**fold_key=md5(ip.'\|'.rule.'\|'.小时窗)**（窗 id=intdiv(time(),3600)，0=自动取当前窗；显式窗参为测试消除小时边界 flake）；**MySQL 原生 `INSERT ... ON DUPLICATE KEY UPDATE hit_count=hit_count+1, last_seen=VALUES(last_seen)` 原子折叠**（首见上下文 path/ua/reason 保留不更新——一行一样本非最新）；**IP 列由 SQL `INET6_ATON(%s)` 填充**（原始二进制不过转义层，MySQL 5.6.3+/MariaDB 10.0+ 原生；非法 IP 回落 '0.0.0.0' 不丢行）；**`security_log_anonymize` 开→入库前截断**（IPv4 /24、IPv6 /48，PHP 侧字符串截断后再交 INET6_ATON）；rule 64/path/ua/reason 191 列宽截断；`current_window()` 供后台同桶分组。`Gr_Security_Logger`（security 层，Gr_Plugin 显式装配接线）**订阅 `gr_security_findings`**：逐发现写折叠行（IP=Gr_Ip_Resolver 解析、path/ua=活请求上下文），畸形行跳过、单发现 Throwable 隔离上报 `gr_inspector_error('logger')`。门面 `gr_log_security_event()`（registry 五参签名）。**do_action 桩升真语义**（记录 + 按优先级稳定序调用回调——对齐真核，此前仅记录不调用） | 实测 2026-09-10：phpunit 271 tests 1718 assertions（SecurityLogTest 9 例——**同折两次命中=字节全等 upsert**（prepare+query 双记录计 10 条、distinct 形状 4）、异窗/异 IP/异规则各自成折、**upsert 形状原子折叠**（INET6_ATON 文本入列+ON DUPLICATE 恰两句+action_taken logged）、**匿名化开关**（默认全 IP；开后 v4→/24、v6→/48 入库）、非法 IP 回落 0.0.0.0、列宽截断（64/191/191/191）、门面、findings 订阅（两发现两形状+畸形跳过+活请求上下文随行）、**W2 检测器→W1 帧→W6 记录器真链 e2e**（单测内闭环）、插件级接线断言）；坑位实录：**do_action 桩此前只记录不调用**——订阅类测试全零行后升真语义（优先级稳定序：PHP7.4 usort 不稳，装饰序号排序）；prepare+query 双记录→计数/形状断言需分全量与去重两径；`{$table}` 多行坑第四次（对齐会话仓两段式布局：ignore 各盖各的行） | :8091 实测——**串行 50 发=1 行 hit_count=50**（INET6_NTOA 复原全 IP、action_taken logged）；**真并发**（双 wp-cli 进程并行各 25 发同折）**仍 1 行 hit_count=100（50+50 精确）、debug.log 零 1062/Duplicate entry**；**匿名化真库切换**（开→203.0.113.0 /24 入库、还原 0）；异规则各自成折（3 规则 3 行）；**HTTP 端到端真链**：curl sqlmap+Nuclei 两发→同一 (ip,rule,小时) 折行 **hit_count=2 且首见 reason 'ua:sqlmap' 保留**（fold 键不含 reason——Nuclei 命中折入首见行，设计语义现场验证）+ **WP-Cron 自身 `WordPress/7.1` UA 被 W2 种子如实标记**（`ua:WordPress/` 行、wp-cron.php 前台请求走全链——真请求不请自来的链路证明）；清理 w6_*+scanner_ua 行归 0、debug.log 维持 2126 零残留 | `05` §3.1、`11` §3.1 |
| W7 | 登录保护 | ✅ | `Gr_Login_Protection`（security/，静态引擎）：`record_failure($username,$ip)`——**键含 IP+用户名哈希**（`gr_login_fails_`+md5(ip'\|'user)，900s 滑窗计数）；达阈值（`login_fail_threshold`=5，钳 ≥2）→ **梯度锁定**（轮次 `gr_login_locks_`+md5(ip) 24h 记忆窗，时长=base×2^(轮-1) 钳 86400，`login_lockout_base`=300 经 W5 `Gr_Temp_Bans::block` 落锁）+ 清计数（下轮从零）+ 折叠日志双行（`login_fail` 每败 / `login_lockout` 触发与锁中尝试同折）；**信任地址全跳过**（不锁、不爬轮——允许列表测试不预陡梯度）；`check_lockout()` 返回 {locked/remaining/failures/threshold/round}（`Gr_Temp_Bans` 补 `lock_remaining()`——锁值带 epoch 到期，transient API 不暴露剩余 TTL 故随锁自记）；**接线**：`wp_login_failed` 记败（活请求地址）+ `authenticate` 门（**accepted_args=1**——锁判地址不判凭据）；**默认仅记录模式明确**：`security_action_mode='log'`（默认）= 透传仅记锁中尝试，`'block'` 升档才拒（`gr_login_locked` WP_Error 403）；门面 `gr_check_login_lockout()`/`gr_record_login_failure()`；设置键 `login_fail_threshold`/`login_lockout_base` 注册 | 实测 2026-09-10：phpunit 282 tests 1759 assertions（LoginProtectionTest 11 例——**4 败不锁/5 败即锁**（failures=4→locked round1 remaining=base 计数清零）、**梯度 60→120→240→…→轮 12 封顶 86400**（封顶点 60×2^11>86400 算了三轮才对——先想 2^n≥1440 再写轮数）、**对独立计数**（editor 视角 failures=0 但地址锁+轮记忆共享）、**允许列表恒不锁且不爬轮**（10 败零锁零轮）、CLI 解锁后轮记忆留存（梯度不因解锁重置）、**默认 log 透传/block 拒 `gr_login_locked`**、security_enabled 关=零瞬态零行、`wp_login_failed` 真钩计数活请求、失败与触发双行达折叠日志、插件级双钩接线）；坑位实录：门面名照注册表 `gr_check_login_lockout`（别自造缩写）；`Gr_Ip_Resolver` 在 **security** 命名空间（误 import Core 即 fatal——同层引用免 import）；`?:` 短三元全规则禁（transient_int 助手展开）；`authenticate` 过滤器回调 unused 参告警→accepted_args=1 收参 | :8091 实测——**梯度真 TTL 实走**（base=3s：轮 1 锁 3s 过期自释放、轮 2 锁 6s——倍增在真库瞬态上验证）、**round 记忆跨锁存活**（过期后 round=1 保留）、**block 模式锁中拒绝 `gr_login_locked`**（链路里错误码归属可辨）、**log 模式锁中透传**（链路终值=共存插件 LLAR 的 `invalid_username`，greenpng 无拒——仅记录语义现场可证）、`wp_login_failed` 真钩活请求计数、**折叠行精确算术**（login_fail=10=5+5 两轮、login_lockout=4=2 触发+2 锁中尝试）、CLI 幂等 Warning 路径、清理瞬态零残留+行归 0+设置还原（base 300/log）；坑位实录：**开发站装有 LLAR（wp-plug 参考 04）**，其 `authenticate` 回调强制 3 参——探针 `apply_filters('authenticate',$user)` 单参即 ArgumentCountError fatal（真实登录恒 3 参故生产无恙；同 C10"传真对象"教训类：**共享核心钩上探针必须复刻真实调用形状**，且共存登录插件生态从此成为每登录探针的既定变量）；debug.log 剔 75 行（67 phar 噪声+1 探针 fatal 迹）回 2126（备份留档） | `03` §3、PEER-01 |
| W8 | 蜜罐（opt-in） | ✅ | `Gr_Honeypot`（security/，静态引擎）：**无状态动态字段名**——陷阱与载体都是普通观感输入（词根 url/company/phone/fax/zip + 6 hex，`decoy_name()` 带 except 守卫保证陷阱≠载体），链接关系封进 **AES-256-GCM 信封**（`Gr_Secrets::encrypt`，前缀 `gr1.`）由载体值携带 {d: 陷阱名, t: 渲染纪元, c: 上下文≤32}——标记里没有任何字段告诉机器人哪个是饵；`render()` 在模块关或加密不可封时返回 ''（封不出的陷阱只会误判）；标记 = text 输入 `aria-hidden="true"` + `tabindex="-1"` + `autocomplete="off"` + 移屏内联样式（**禁 display:none**）+ hidden 载体；`evaluate($post)` **纯函数**（不碰设置不碰存储）——首个可开封载体定谳：陷阱非空（纯空白=未碰）→ 'trap'，否则耗时 <2s → 'fast'，篡改/缺失载体**永不判**（规避≠证据，GCM 认证自证）；接线：`login_form`/`register_form` 渲染、`authenticate` 门优先级 **20**（先于 W7 的 30，accepted_args=1）、`registration_errors` 门；默认 `log` 仅记录，`block` 升档拒（登录 `gr_honeypot` WP_Error 403；注册追加错误）；`report()` 折入 W6 日志；`render_carrier()` 公开 = 时间差测试夹具铸造器；`enabled()` = security_enabled **且** `honeypot_enabled`（新设置，默认 0——opt-in）。门面 `gr_render_honeypot`/`gr_check_honeypot`；`Gr_Honeypot::register_hooks()` 接入 `Gr_Plugin` | 实测 2026-09-12：phpunit **298 tests 1841 assertions**（HoneypotTest 16 例——默认关全链零输出（含 do_action 路径）、双表单注入、a11y 契约（aria-hidden/tabindex/autocomplete/无 display:none）、跨渲染动态名、陷阱≠载体、`gr1.` 前缀、陷阱非空判 'trap'、纯空白=未碰、慢+空=人类、**1s=自动化/2s=人类边界**、渲染即提= 'fast'、篡改载体不判、缺失载体不判、判定零 SQL、登录门 log 折行/block 拒、注册门 record-only/block 追加、门禁态惰性）；桩升级：wp-functions +`esc_attr`；**WP_Error 桩升真核心多错误语义**（errors[code][] + error_data + `add()`/`get_error_codes()`，旧消费者兼容——注册 block 模式追加错误所需） | :8091 实测——默认关：登录/注册页 `gr1.` 0 匹配（wp-login 自带 dashicons aria-hidden 标记是核心家的——**探针认 `gr1.` 形状别认通用属性**）；开启后双表单注入完整 a11y 契约、跨 fetch 名字全换（phone_30d835→url_110ff8→company_cb38b9…）；机器人 POST 折行 hit 1→2 原子折叠；**正确凭据+填陷阱双模式终证**：log→302 跳 wp-admin+登录 cookie，block→200 无跳转无 cookie（语义证据而非页面文案）；链级 apply_filters（真 3 参）：log→WP_User、block→`WP_Error[gr_honeypot]`（同进程切模式，唯一变量）；注册 e2e：block→用户未创建、log→用户创建放行；四判定折一行 hits=4（W6 折叠键含 IP 不含 reason，首见 reason 保留——U 阶段 UI 需明示）；坑位实录：**LLAR 会把探针 IP 自己锁掉**（连败累计→20 分钟锁定遮蔽一切后续登录验证——清 `limit_login_lockouts`/`retries` 残留再验）；**LLAR LoginErrorPresenter 把页面错误文案统一遮蔽成 "Incorrect username or password."**——页面文案不可作证据，链级错误码归属+302/cookie 语义才是证；wp-cli eval-file **无 $argv**（传参静默失效探针白跑——写死值）；core 在 authenticate@20 有三个凭据过滤器+spam_check@99+LLAR -9999/99990 双哨——我们挂 20 经 plugins_loaded 注册排最后→block 能覆盖 WP_User，LLAR 尾哨对 WP_User "do not override"、对他人错误码透传→`gr_honeypot` 活到链尾；测试助手隐藏副作用（夹具铸造器内 enable）泄入 disabled 测试——铸造后显式翻设置；shell `eval "$CLI" eval '...'` 引号嵌套炸（block 没设上 B 探针白跑）——一律 eval-file；清理：设置三项还原+双探针用户删除+行归 0（含 4 行 scanner_ua 探针残留）+瞬态清零+debug.log 剔 83 行回 2126（备份 .w8） | `03` §3、`06` §4、PEER-01 |
| W9 | 载荷检查 | ✅ | `Gr_Payload_Inspector`（security/，静态引擎）：**恰两族高置信规则**——`sqli_union`（`UNION` [空白/`/**/`/`+` 分隔] [ALL] `SELECT`，`\b` 边界）与 `lfi_traversal`（`../`/`%2e%2e` 跳 × `/``\``%2f`%5c` 分隔 × **≥2 跳**，字面与百分号编码两形）；docs/10 §4 禁用清单（`<?php`/`eval(`/`0x`/`document.cookie`/`wp-config.php` 独立条件）**全部不做规则**——漏一次扫描丢一行日志，误判一次丢一位访客；`inspect(array)` **纯函数**（首族命中定谳该参、一参一行、值扫描钳 4096B、PCRE 失败=未中失败开放、reason=`参数名:片段` 双半皆攻击者可控故 sanitize 后截 120）；**前门只扫 GET**（POST 是评论者交内容的地方=docs/10 §4 防的误报类；跳过 `s`/`utm_*`/`gclid`/`fbclid`/`msclkid`/`mc_cid`/`mc_eid`——搜索是教程文本的家、归因参数不是攻击面；wp_unslash 在读取点承重——wp_magic_quotes 加斜杠后 `..\..\` 断成不匹配形）；**命中恒仅记录**（tier 1，结构性不可升档：发现只有 rule_id+reason 两字段，无任何消费者可读作拦截的动作指令；docs/10 §4 tier 3 只留 scanner UA/蜜罐/显式封禁）；接线：`gr_inspection_checks` 滤下 `payload_rules` 探测器 + **W1 框架扩展**：单检查可返回**列表形发现**（`normalize_many`——单形带 rule_id 键与数字键列表可辨，旧检查零改动） | 实测 2026-09-12：phpunit **316 tests 1931 assertions**（PayloadInspectorTest 17 + 框架列表形 1——UNION 五变体全中、穿越五形全中、**误报弹药库 13 样本零命中**（wp-config.php 教程/document.cookie/eval(/`<?php`/0x/or 1=1/trade unions selected/单跳 `../`/REUNION/union of tables）、单跳非穿越、非标量/空值跳过、数字参名入 reason、reason sanitize+钳长、首族定谳、多参多行、**4096B 外匹配按预算丢弃**、门面=引擎、探测器扫 $_GET、跳过搜索+营销名+大小写不敏感、**unslash 承重证明**（addslashes 形不匹配/去斜杠后命中）、非标量 GET 忽略、Gr_Plugin 注册探测器；框架：列表形收集+垃圾项丢弃） | :8091 实测——攻击 GET → `sqli_union cat:UNION SELECT`/`lfi_traversal tpl:../../../` 行落账（param:token 形 reason）；同攻击重发 → 同折行 hits 1→2；不同 token 同键 → 同一行首见 reason 保留（W6 语义）；双规则单请求 → 两行各自落账；**误报弹药库线上零行**（`?s=UNION SELECT 教程`/utm_/gclid/wp-config.php+document.cookie 均 不入账）；**security_enabled=0 → 攻击 GET 零行惰性**（还原=1）；scanner_ua 与 payload 两探测器独立并行共触发；坑位实录：127.0.0.1 与 localhost 是两个回环地址（::1）——同攻击不同 host 各折各键（折叠键含 IP 的正确行为，探针期要意识到 301 规范跳转前后 IP 不同）；curl 查询串里 `+` 被 PHP 解码为空格（要发字面 `+` 得用 %2B——单元测试直接覆盖 `+` 分隔类所以无洞）；wp-cli 传参无 $argv 旧教训复用（eval-file 写死值）；phpcs:ignore 必须贴在**真正读取 `$_GET` 的那行**（isset 守卫行不消 sniff）；清理：行归 0+debug.log 剔 20 行回 2126（备份 .w9） | `03` §3、`10` §4 |
| W10 | FCrDNS | ✅ | `Gr_Crawler_Verify`（security/，静态引擎）：`resolve($ip,$ua)` **PTR→正查双向确认**——gethostbyaddr 失败=返回原址=无 PTR；正查恒跑 **DNS_A + DNS_AAAA 双族**（参考项目 IPv6 盲区：只查 A 致 v6 爬虫永不通过——修复）；确认比对走 `Gr_Ip_Matcher::match_cidr` 裸址字节等值（IPv6 文本异形同址互认）；**状态词表恰两值**：`verified`/`unverified`——**永不返回"伪造"**（任何失败=无法验证，不惩罚，docs/10 §4 失败开放）；`verify()` 经 24h transient 缓存（`gr_fcrdns_`+md5(ip|ua)，**正负结论同缓存**——不可验证地址不能被重复查询成负载）；**前台零同步 DNS（铁律 3）**：`handle_findings` 订阅 `gr_security_findings`——仅 `scanner_ua` 发现触发 `queue_if_first_seen`（缓存/pending 双闸，`gr_fcrdns_pending_` 900s 去重风暴）→ `Gr_Queue::enqueue`（AS 优先回退 wp-cron）；worker `handle_job` 跑真 DNS 并清 pending；**日清扫 `sweep()`**：`recent_scanner_ips(24h, LIMIT 25)`（GROUP BY ip,ua ORDER BY MAX(last_seen)，二进制 ip 长度先验后 inet_ntop）兜底首见入队丢失的申述；门面 `gr_verify_crawler`；`Gr_Crawler_Verify::register_hooks()` 三钩接入（job/日维护/发现） | 实测 2026-09-12：phpunit **331 tests 1980 assertions**（CrawlerVerifyTest 15——v4 正查确认、**v6 经 AAAA 且文本异形同址互认**（agy 盲区修复证）、无 PTR 不验证、**正查不匹配=unverified 永无 forged**、resolver 缺失失败开放、**非法地址零 DNS 调用**、结果五字段契约形状、**双结论 24h 缓存**（第二次调用 DNS 计数不增+expires_at=86400 实证）、负结论同样入缓存（重查防负载）、门面=引擎、**发现订阅首见入队**（参数=活请求 ip+ua）、pending 闸拦第二次、payload 发现不入队、已缓存申述跳过、**worker 钩子真跑**（缓存落账+pending 清除）、日清扫只入队未缓存申述（SQL 形状断言 rule_id/GROUP BY/LIMIT 25）、Gr_Plugin 三钩接线；**桩升级：dns-overrides.php**——gethostbyaddr/dns_get_record 是 PHP 内建，function_exists 守卫的桩永不生效（真函数被调用真 DNS 在跑），同命名空间遮蔽（attribution-overrides setcookie 同型先例）+ DNS_A/AAAA/ANY 常量桩 | :8091 实测——**首见入队走真 AS 队列**（curl UA 请求→`actionscheduler_actions` 出现 `gr_crawler_verify|pending|greenpng|["::1","curl/8.7.1"]` 参数精确；**wp cron event list 看不见**——WC 在场 AS 优先，Gr_Queue::backend() 决定落点，探针要看对表）；**AS 自主跑完一例**（wp-cli boot 触发 wp-cron HTTP ping 的 WordPress/7.1 UA 申述→complete+24h 缓存落账——整链无人值守自走）；**pending 闸**（同申述二次请求不再入队）；**worker 真跑**：直火 `do_action('gr_crawler_verify','::1','curl/8.7.1')` → **真实 DNS 全链 verified**（gethostbyaddr '::1'→'localhost'，正查回含 ::1，host 入账）+ pending 清除——失败开放路径单测已证、真机路径走到 verified；**日维护链真跑**：`wp cron event run gr_queue_daily_event` → 互斥锁内 DAILY_HOOK → sweep → 清缓存后两条近期申述全部重新入队 pending；坑位实录：as_unschedule_all_actions 空 args 只匹配无参动作（按钩清残要 SQL——清理语义与函数名直觉不符）；W9 清理漏了 AS 动作与 fcrdns 瞬态（跨任务残留类：新任务引入新存储面时，前一任务的清理不会覆盖——每个任务收尾要清"本任务代码激活期间产生的一切新面残留"）；wp-cli 每次完整 boot 自身可触发 wp-cron HTTP（探针计数永远有背景噪声）；清理：AS 动作归 0+瞬态清+行归 0+debug.log 回 2126（备份 .w10） | `03` §3、铁律 3 |
| W11 | 安全总熔断 | ✅ | `Gr_Security_Gate`（security/，静态扼点）：`active()` = **`GR_SECURITY_OFF` 常量优先**（wp-config 级紧急逃生——不依赖数据库即可修复站点）∧ `security_enabled` 设置；`emergency_off()` 独立谓词（状态页要说清"为什么停"：偏好 vs 修理中）；**消费面全部收拢到扼点**——W1 框架 `run()`（同时移除框架不再需要的 settings 注入，构造零参）、W7 登录门/失败钩 `enabled()`、W8 蜜罐 `enabled()`（∧ honeypot_enabled）、W10 `handle_job`/`sweep`（**cron 路径自门控**——请求路径由上游 frame 门控、cron 无上游故自查）；直接 API（record_failure 类）不门控=库惯例，熔断断言打在钩子路径；`reset_for_tests(?bool)` 接缝旋钮测常量语义（进程隔离在本机构建挂死——见实测列） | 实测 2026-09-12：phpunit **338 tests 1998 assertions**（SecurityGateTest 7——默认 active、设置停摆≠emergency（原因可辨）、**emergency 压过设置**（设置=1 仍 false）、**emergency 停框架**（checks 滤都不应用零动作）、**emergency 停表单+登录门**（honeypot 开仍渲染空串；锁中地址过门不拒；失败钩零瞬态）、**emergency 停队列工作**（worker 零 DNS 调用零缓存；sweep 零入队）、接缝旋钮清回常量路径；框架构造点 12 处随零参更新） | :8091 实测——**常量熔断双向终证**：wp-config 临时插 `GR_SECURITY_OFF`（备份还原）→ HTTP 侧**登录表单 0 陷阱字段**（honeypot_enabled=1 开着）、**SQLi 攻击 GET 零行落账**、CLI 侧 emergency=true/active=false；移除常量对照 → 表单陷阱字段回归 + 攻击行落账；**此前假阴定性为 opcache 复检窗**：插常量后 <2s 探针 HTTP 当常量不存在（CLI 新进程恒见新状态）——CLI/HTTP 字节码与配置可见性不同，改配置后的探针必须先证 HTTP 已见新态；坑位实录：define() 二次定义不同值只发 notice 不改值（两行探针 define 后真值仍首轮——配置探针要幂等可验）；折算日志上行数 COUNT 是弱信号（同 IP 同小时折一行+wp-cron 噪声）——强判别面用确定性表面（蜜罐表单）与"只有攻击者会触发的规则族"（sqli_union）；PHPUnit @runInSeparateProcess 在本机 FrankenPHP 构建挂死（同无 -l/缺常量家族怪癖）→ 常量测试用 reset_for_tests 旋钮；清理：行归 0+AS verify 动作清+瞬态清+honeypot 复 0+wp-config 验净（无 GR_SECURITY_OFF 残留）+debug.log 回 2126（备份 .w11） | `10` §4 |
| W12 | Blackhole 陷阱（opt-in） | ✅ | `Gr_Blackhole`（security/，静态引擎）：**虚拟路径** `/gr-blackhole/`（常量，从不路由）——`robots_txt` 滤追加 `User-agent: *\nDisallow:` 声明组（**声明即机制**：守规蜘蛛永不抵达=永不误伤）；`init@5` 拦截（先于 W1 框架@10）：`wp_parse_url` 剥查询取路径，双侧斜杠归一（丢/多尾斜杠的扫描器照样落网，大小写敏感防误伤）；命中 → `handle_hit()`：折叠日志行（rule_id `blackhole`，reason 'disallowed trap path crawled'）恒记 + **block 模式**交 `Gr_Temp_Bans::block(ip, 'blackhole: disallowed path', 86400)`（W5 锁——登录门 W7 已消费，未来阻断面同理）；应答 `wp_die('Access denied.', '', ['response'=>403])`（生产带 no-cache 头+终止；桩记录不终止）；`enabled()` = W11 总熔断 ∧ `blackhole_enabled`（新设置，默认 0——opt-in）；设置默认注册 + `Gr_Plugin` 接线（init@5 + robots_txt 滤） | 实测 2026-09-12：phpunit **349 tests 2031 assertions**（BlackholeTest 11——开启后 robots 含 Disallow 行、**模块关=原文返回**、**总熔断=原文返回**、路径形判定（精确/无尾斜杠/带查询=中；`/gr-blackhole-zone/`、`/other/gr-blackhole/`、大写=不中）、禁用惰性、**log 模式记行零封禁**（SQL 含 blackhole+reason、无 gr_block_ 瞬态）、**block 模式落封禁**（reason 逐字）、**拦截应答 403+文案**（wp_die 桩 args.response=403、message='Access denied.'）、**普通页面零触发**（零 wp_die 零 SQL——守规蜘蛛语义）、禁用路径零动作、Gr_Plugin 注册 init@5+robots_txt；**既有测试 1 处松动修复**：W1 接线断言从"任何 init"改为按回调匹配（多模块共乘 init 后最后注册者劫持断言）；桩升级：wp_die（记录不终止）+ esc_html__ + wp_parse_url | :8091 实测——**robots.txt 声明落位**（Sitemap 行后追加声明组）；**陷阱应答**：精确路径/无尾斜杠/带查询三形皆 HTTP=403+Access denied（log 模式折叠行 hits=2）；**普通页面不受影响**（HTTP=301 常态）；**block 模式封禁落账**（gr_block_ 瞬态 reason/ttl=86400/remaining 实走）；**跨模块闭环**：黑洞封禁 IP 下登录链 `apply_filters('authenticate')` 终值 `gr_login_locked`（W12→W5→W7 模块图贯通）；**关闭态复证**：robots.txt 零声明行 + 陷阱路径归还 HTTP=404（WP 常态 404 而非我们的 403）；坑位实录：**CLI 链探针的 REMOTE_ADDR 不设即非封禁地址**——跨模块地址相关断言必须显式设 `$_SERVER['REMOTE_ADDR']`（真实调用形状的一部分，不是装饰）；`$public` 是保留字+未用即双警告（核心滤的旁参不声明即可——accepted_args=1）；parse_url 嗅探禁用改 wp_parse_url（桩补）；`robots_rules` 收 1 参后仍被核心传 2 参调用——PHP 方法允许旁参静默（与 LLAR 3 参教训同一知识面）；清理：行归 0（3 行=2 log+1 block 精确）+封禁清+fcrdns/AS 残清+设置还原+debug.log 回 2126（备份 .w12） | PEER-01、`10` §4 |
| W13 | 安全→质量结论通道 | ✅ | `Gr_Security_Conclusions`（security/，静态）：**纯映射器** `conclude(findings)` → `{suspected_bot: bool, bot_tier: 'high'/'medium'}` 或 null（人类请求=无事件——通道传结论不传安抚）；HIGH_RULES = {scanner_ua, sqli_union, lfi_traversal, honeypot, blackhole}（一条高置信规则定档 high；未分类规则=medium；**登录失败故意不馈送**——人类也会输错密码，锁定说明地址被攻不含访客是机器人）；**馈送面**：`gr_security_findings` 框架订阅（每请求主面）+ `record(...$rule_ids)` 直馈（蜜罐 report() 判定缝 + 黑洞 handle_hit()——两条永不到框架的路径）；事件名 `security_conclusion` 经 `gr_dispatch_event` 上总线（默认落库 gr_events）；**payload 契约锁死**：恰 `suspected_bot`+`bot_tier` 两字段——rule_id/reason/UA/IP/指纹明细**永不进事件**（docs/07 §4 第 3 款，修复参考项目 fingerprint_hash 双用缺陷 + iss-01"机器人被评优质客户"漏洞）；v1.1 CRM 消费打标 `sys:suspected_bot` | 实测 2026-09-12：phpunit **359 tests 2065 assertions**（SecurityConclusionsTest 10——高置信五规则逐条定档 high、一条 high 定全局档、未分类=medium、空/畸形发现=null、**框架订阅派恰一事件且 schema 精确**（payload 键恰 [suspected_bot,bot_tier]、类型断言、落库行 event_name/payload_json 断言 + **reason 字串不入库证**）、人类发现零派发、record 直馈（蜜罐档 high；空参零事件）、**蜜罐门判定→通道**（log 模式过访客+恰一事件）、**黑洞命中→通道**、Gr_Plugin 注册订阅；蜜罐/黑洞既有测试随馈送加入全绿） | :8091 实测——扫描 GET（curl UA）→ `gr_events` 落 `security_conclusion` 行，payload **恰** `{"suspected_bot":true,"bot_tier":"high"}`（无 UA/规则/地址泄漏，visitor 空串——机器人无 cookie）；黑洞陷阱命中 → 结论事件落账；**人类访问（浏览器 UA）→ 零新增结论事件**（通道静默即语义）；坑位实录：**PHP 引用捕获穿透 return 即死**（closure use(&$events) 引用的是助手局部，return 后引用悬空——捕获数组必须生在调用者作用域）；static 闭包内不可用 $this（断言移出闭包）；wpdb 桩 insert() 记 `inserts` 不记 `queries`（事件落库断言看对存储）；内联 eval 嵌套引号第三次咬人（SQL 里的引号转义）——一切 wp-cli 代码一律 eval-file；清理：结论事件 3 行清零+行归 0+AS/瞬态清+blackhole 复 0+debug.log 回 2126（备份 .w13） | ADR-0007、`07` §4 |
| W14 | 完整 IP 与脱敏 | ✅ | **存储双轨 W6 已落地**（`gr_security_logs.ip VARBINARY(16)` 完整 `INET6_ATON` 入库 + `normalize_ip` 匿名开关入库前截断 v4 /24、v6 /48，`security_log_anonymize` 默认 0）；W14 补**展示侧缺口**：`Gr_Ip_Mask`（security/，静态纯函数）`mask(string $ip): string`——`FILTER_VALIDATE_IP` 守卫后 `inet_pton/inet_ntop` 规范化，v4 末八位组遮蔽（`203.0.113.*`）、v6 末组遮蔽（`2001:db8::1`→`2001:db8::*`）；**规范形 `::` 尾部（/48 截断读回）追加遮蔽标记**（`2001:db8:abcd::`→`2001:db8:abcd::*`——读者仍见"有隐"）；非法输入返回空串——展示层永不回显无法解析的地址；纯展示，遮蔽形永不回存/参与匹配；门面 `gr_mask_ip` 随实现登记入 docs/03 §3（新增行）；UI 明示"封禁降级为网段级"文案落 U 阶段（W14 交机制） | 实测 2026-09-12：phpunit **366 tests 2083 assertions**（IpMaskTest 7——v4 末段、v6 末组与全 8 组形、压缩尾标记（含 `::`→`::*`）、v4-in-v6 映射形、非法四形空串、**遮蔽形永不回流**（对遮蔽输出再 mask=空串 + FILTER_VALIDATE_IP false——展示产物对存储/匹配世界即不可解析）、**存储二进制→inet_ntop→mask 流水**恰 U 阶段列表管线；SecurityLogTest 匿名化开关两模式入库 W6 已覆盖） | :8091 实测——**默认模式**：攻击 GET（curl UA）真实请求行 `INET6_NTOA(ip)` 读回完整 `::1`、展示 `::*`；CLI 直写 `203.0.113.99`/`2001:db8:abcd:1234::1` 入真实表读回**逐字完整**、展示 `203.0.113.*`/`…1234::*`；**匿名模式**（开关=1）：同两址入库读回 `203.0.113.0`（v4 /24）/`2001:db8:abcd::`（v6 /48）——入库前截断真跑；**封禁联动**：匿名行仅存网段址，以其派生 `/24` CIDR `gr_match_cidr('203.0.113.99','203.0.113.0/24')` **true**（网段级封禁仍罩得住攻击者），/32 精确址已不在库（构造性不存在）——降级诚实可验；坑位实录：**namespace 声明必须紧跟 `<?php`/declare**（ABSPATH 守卫置于其前=致命错；本库头序：docblock→declare→namespace→守卫→use）；phpcbf 一键消 36 处对齐类嗅探（新文件先跑 cbf 再 phpcs 省手对）；探针标签语义笔误自查（echo 标签描述的须是断言真值——`exact_gone` 打的是"存址===全址"的 var_export，false 恰证精确址缺席）；清理：6 行删净+结论事件 3+AS 2+瞬态 4 清+开关复 0+debug.log 回 2126（备份 .w14） | ADR-0007、`05` §3.1、`10` |

## 5. Phase 4 — 汇总与后台页面（v1.0 页面子集 = 14 页）

v1.0 页面：Dashboard、Traffic & Security（3 标签）、Access Rules、Login Protection、Bot & Device Signals（爬虫验证标签）、Campaigns（4 标签）、URL Builder、Funnels & Goals（A/B 标签先行）、Settings（独立页）、Analytics & CAPI、IP Intelligence（GeoIP 部分）、Audit Log、Data Retention、Status & Diagnostics。其余 6 页随 v1.1/v1.2 交付。

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| U1 | 日汇总 | ✅ | `Gr_Daily_Aggregator`（storage/，静态）：`register()` 挂 `Gr_Queue::DAILY_HOOK` **优先级 5**（先汇总钉死——先于一切默认优先级日骑手，不受注册次序影响，docs/05 §5"先汇总后瘦身"）；`run()` 重算窗口 = 今日回看 7 天（`LOOKBACK_DAYS=7` **必须 < 最小默认保留期 30 天**，注释钉死该耦合——窗口外日期永不重访，此即"瘦身后报表读数不变"的结构保证）；`aggregate_date(string $date)` 公开缝（单测/回填），Y-m-d 正则拒非法；指标词表 10 族（5 标量 sessions/visitors/pageviews/conversions/revenue + 5 维度 sessions_by_country/channel/device/bot + security_hits by rule_id——**来源 SQL 与词表已登记 docs/05 §3.4**）；日界 = 站点本地日（原始表存 `current_time()` 本地值，日算术走 mysql 串非 timestamp 型——timestamp 型非 UTC 纪元且触发嗅探告警）；**静默日标量也写 0 行**（趋势稠密），维度键零值不写；upsert = `INSERT ... ON DUPLICATE KEY UPDATE metric_value = VALUES(metric_value)` **替换语义**批 100 行/批；表名全走 `Gr_Database::table()`（C2 零拼接纪律）；日链整跑经 `wp greenpng maintenance`（真互斥锁→DAILY_HOOK→聚合@5→FCrDNS 清扫@10） | 实测 2026-09-12：phpunit **373 tests 2120 assertions**（DailyAggregatorTest 7——10 指标族逐族断言 upsert 元组（含 bot 键 human/bot 映射、国家空串键、revenue 129.90）、静默日恰 5 标量 0 行无维度、**二跑语句逐字节相同 + ODKU 替换臂无加法臂**、窗口恰 8 日最旧在前、**窗口外日期重跑零重访**（瘦身不变性的可测形）、非法日期零查询、Gr_Plugin 接线优先级 5 按 hook+回调匹配；wpdb 桩升级：`results` 支持闭包按查询应答 + 新增 `get_row` + `current_time('timestamp')` 分支 + `gr_stub_ts` 旋钮随 reset 清） | :8091 实测——真链 `wp greenpng maintenance` 双跑：今日（本地 2026-09-12）sessions=6/visitors=6/pageviews=3（恰播 3 事件）/conversions=1/revenue=129.90/security_hits u1_probe_rule=1+scanner_ua=1（真噪音）/维度 direct=5+organic=1、desktop=5+mobile=1、''=5+US=1、human=6——**57 行二跑逐值相同不翻倍**；静默日（09-05..09-09）稠密 0 行；历史日 09-10=7/09-11=1 真实旧探针流量如实入账；**瘦身不变性真站证**：窗口外日 2026-08-03 播 1 会话→聚合 sessions=1→删原始行→再跑整链→**stat 行仍 1.0000**（永不重访）；坑位实录：`current_time('timestamp')` 触发 WordPress.DateTime 嗅探（非 UTC 纪元）——本地日改从 mysql 串切片；InterpolatedNotPrepared 豁免必须落在**插值字符串行**上方（放 prepare 调用行上不生效——同一教训三现）；UnfinishedPrepare 嗅探对动态拼占位符列表需单独豁免；坑位：WP-CLI phar 自身 php-cli-tools Deprecated 告警（null 数组偏移）非本插件代码；清理：66+2 汇总行清零+u1 会话/事件/转化/安全行删净+AS/瞬态清+debug.log 回 2126（备份 .u1） | `05` §1/§3.4 |
| U2 | 图表库 | ✅ | uPlot **1.6.19** 本地打包 `assets/js/vendor/`（`uplot.iife.min.js` 44,330B 原始 / **19,063B gzip ≤ 20KB 预算**——1.6.32 实测 gzip 22,021B 超预算，逐 tag 回测 1.6.31/28/24/19/13/9/8 后取余量最稳的 1.6.19）+ 未压缩源码同目录（127,598B）+ MIT 许可证随行（`uplot-license.txt`）+ `assets/css/uplot.min.css`；`Gr_Chart_Assets`（admin/ 模块首类，autoloader 命名空间段动态派生免登记）：`register_hooks()` 挂 `admin_enqueue_scripts`，`register()` **只注册不出队**（注册免费、出队是页面的事——docs/06 §2.3 反例：原型 40 页无条件载 208KB Chart.js），`enqueue()` 供图表页渲染器显式调用；句柄 `gr-uplot`/`gr-uplot-css`，版本随 `GR_VERSION` 破缓存；**预算机器钉死**：测试 gzencode 实测 min 构建 <20,000B，超支即红 | 实测 2026-09-12：phpunit **379 tests 2144 assertions**（ChartAssetsTest 6——四文件在位（min/源码/许可/CSS）+ 版本横幅 + MIT 文本在包、**gzip 预算断言**、注册后零出队（scripts/styles 双侧）、URL 本地前缀 + 磁盘文件在位、显式 enqueue 后句柄入队且 URL 经注册表解析、Gr_Plugin 挂 admin_enqueue_scripts 按 hook+回调匹配；桩补 wp_register_script/wp_register_style/wp_enqueue_style + handle-only 入队经注册表解析 + 四数组随 reset 清） | :8091 实测——CLI 真 WP 注册表：registered=true / **enqueued=false**（注册≠出队）、style registered、显式 enqueue 后 enqueued=true；URL = 站点自身插件目录（零 CDN）；HTTP 实取 min.js 200/44,330B、css 200/1,883B 站点自伺服；`tools/build-zip.sh` 实跑 zip 内含 uPlot 全套四文件 + admin 类（打包端到端）；坑位实录：**1.6.32 gzip 22KB 超预算**——"≤20KB"按线上尺寸（gzip）计量且 20KB 取 20,000B 严格读法，版本回测选 1.6.19；raw.githubusercontent `dist/uPlot.css` 返回 404 体（14B "404: Not Found"）——下载后必须验内容非状态码；compat 规则集补 `*/assets/*` 排除 + extensions=php（vendored 压缩资产触发"File appears to be minified"噪声退出码 1——compat 门审自有 PHP 不审第三方资产，与主规则集同款卫生）；debug.log 恒 2126 无需备份 | `06` §2.3 |
| U3 | Dashboard | ✅ | 读侧 `Gr_Daily_Stats_Repository`（storage/）：`series(days)` 稠密透视（5 标量、缺日缺型聚 0.0、最旧在前、窗口钳 1..90）+ `dimension(type,days,limit)` 排名（GROUP BY key SUM 值 ORDER DESC LIMIT 钳 1..50）；**只读 `gr_daily_stats` 作机器断言**（每条 SQL 必含 daily_stats、必不含 sessions/events/security_logs/conversions）。菜单 `Gr_Admin_Menu`（admin/）：`admin_menu` 挂顶级页（slug=greenpng-dashboard 自指首子页、cap manage_options、dashicons-chart-area、位 30，docs/06 §1 树）+ **捕获 add_menu_page 返回的真实 hook 后缀**供资产页域判定（无手拼字符串，防未来重排漂移）；`admin_enqueue_scripts` 仅当后缀匹配才 `Gr_Chart_Assets::enqueue()` + `gr-dashboard.js`（依赖 gr-uplot）+ inline 注入 `window.GreenPNGDashboard`（端点 URL/rest nonce/窗口/**本地化图表标签**——JS 零自带文案，铁律 5）。页面 `Gr_Dashboard_Page::render()`：`wrap`+`wp-header-end`+KPI 五 postbox（当日行，日汇总口径=每日维护链刷新）+ 趋势挂载 div + **服务端同数据 screen-reader-text 表**（14 行×6 列，JS 关也完整）+ 国家分布 widefat striped 表（30 天 top10，空码='Unknown' 桶待 I5 GeoIP 填码；分布走可见表非 JS 图——WP 原生组件优先）。REST `Gr_Dashboard_Controller`：`GET /greenpng/v1/dashboard`，`permission_callback` current_user_can(manage_options) 否则 WP_Error 403（gate 零参声明——core 传参 PHP 惯性忽略），days 参数 schema 1..90 + handler 二次钳制，payload={dates[], series{4 线}, countries[]}；`script_data()` 供菜单注入 | 实测 2026-09-12：phpunit **394 tests 2252 assertions**（DailyStatsRepositoryTest 4——稠密透视（恰 14 日、存储值落位、缺聚 0.0、键序=标量词表）、排名断言、**零原始表 SQL 机器断言**、90 天窗日期精确；DashboardPageTest 6——顶级菜单 cap/slug/icon/位 30+子页回调、**资产页域**（他页零入队/本页 gr-uplot+gr-dashboard 入队、deps 链、inline 含 nonce 与本地化标签）、渲染全表面断言（wrap/postbox/KPI 真值/span 数字、挂载 id、SR 表 14 行恰、国家表 US/Unknown）、空数据态（No country data yet.+0 KPI 稠密）、Gr_Plugin 接线+触发 rest_api_init 后双路由在册；DashboardRestControllerTest 6——路由注册（命名空间/方法/权限回调/days schema max 90）、**门拒默认**（WP_Error 403 码+状态）、持 manage_options 放行、payload 形状（7 日窗稠密+末值）、默认窗与越界钳（14 日/5000→90 日 SQL 日期实证）、script_data 标签键序；桩补 add_menu_page/add_submenu_page（返回确定性 hook 后缀）/current_user_can（默认拒+gr_stub_caps 旋钮）/wp_create_nonce/esc_html + 入队记 deps + reset 全清 + Gr_Admin_Menu::reset_for_tests 缝） | :8091 实测——真链：u3 会话/事件播种→`wp greenpng maintenance`→真表落账；`Gr_Dashboard_Page::render()` 真渲染 7,929B HTML 七面全真（wrap/KPI/SR 表/挂载/US/Unknown/当日行）+ **KPI 首值=6 与聚合真数一致**；真 REST 分发：匿名 **403**/`wp_set_current_user(1)` 后 **200** 14 日稠密末值 6、国家 [{unknown:12},{DE:1},{US:1}] 30 天真和；HTTP：admin 页匿名 **302 → wp-login**（门在）、gr-dashboard.js **200/1,816B** 站点自伺服、REST HTTP 匿名 **403**；坑位实录：**同命名空间类引用缺 use 解析成 Admin\ 前缀**（菜单类引 Rest 控制器）；闭包帧 0 vs 0.0 严格同一性（稠密帧显式 0.0 与库值同型）；**90 天含今=回看 89 天**（测试算错 1 天两处）；rest 路由测试必须**触发 rest_api_init**（挂≠注册）；短三元禁用（`?:` → 显式 if）；未用参数 gate 撤声明（PHP 对多传参静默忽略）；phpcbf 修赋值对齐后 phpcs 归零；清理：57 汇总行+u3 会话/事件+结论事件+安全行+AS/瞬态清+left=0+debug.log 回 2126（备份 .u3） | `06` §1/§2.2/§2.3、`05` §1 |
| U4 | gr-datagrid.js | ✅ | 原型 `agy-datagrid.js`（781 行、内联假数据、toast/drawer/容器全 innerHTML、fetch 仅模拟演示）**修复移植**为 `assets/js/gr-datagrid.js`（131 行 vanilla）：`window.GrDataGrid` 组件 {containerId, endpoint, nonce, columns, pollMs}——**修复 1** 数据源真 fetch + `X-WP-Nonce` 头（响应形 {rows:[{k:v}]}）；**修复 2** 渲染全 DOM API（createElement + `textContent` 赋值，表/thead/tbody/行列组装，**零 HTML 串拼**——源码静态无 innerHTML/outerHTML/insertAdjacentHTML/document.write）；**修复 3** fetch 失败 catch 留存服务端渲染内容不清容器；轮询 start/stop（pollMs 缺省关，document.hidden 跳拍后台页不付更新费）；**组件零自带文案**（列头/消息经 options 由页面本地化注入，铁律 5）；测试双轨：`tests/js/gr-datagrid-test.js`（node v24 直跑，**Stub DOM 拒解析语义**——textContent setter 只存串）承载行为验收：**六形 XSS 夹具**（script/img onerror/svg onload/javascript:/iframe/事件串）逐列注入断言逐字落 text 节点 + created 元素恰表格家具**零 SCRIPT/IMG/SVG/IFRAME 构造** + fetch 头含 nonce + 500 失败容器不空；`tests/Unit/DataGridAssetTest.php`（phpunit 桥，标准四检内守卫：文件在位+四 HTML 串水槽零出现+三修复义务特征在源+node 挂具随仓） | 实测 2026-09-12：`node tests/js/gr-datagrid-test.js` exit 0（三修复义务+XSS 弹药全绿）；phpunit **398 tests 2262 assertions**（DataGridAssetTest 4）；lint/phpcs/compat 全 0；:8091 实测——`gr-datagrid.js` HTTP **200/3,774B** 站点自伺服；`tools/build-zip.sh` 实跑包内含 datagrid+dashboard 双 JS；坑位实录：**fetch 桩 ok 布尔首写即反**（`ok = 200 !== status`——状态码语义断言写反自己先翻车，测试桩的布尔命名要对着真值读一遍）；node 挂具无框架纯 assert+setTimeout 微任务序（promise 链先于宏任务定时器，免 fake-timers）；行为测试在 node、契约守卫在 phpunit 的**双轨分工**——JS 行为进不了 phpunit 进程，但文件级安全契约必须进标准电池 | `06` §2.2 |
| U5 | Traffic & Security | ✅ | `Gr_Traffic_Page`（admin/，slug greenpng-traffic）：原生 `nav-tab-wrapper`/`nav-tab-active` 三标签（tab 键经 `sanitize_key` 白名单化，未知回落 live）；**实时流**——服务端渲染初表（Gr_Event_Repository::recent 30 行）装于 `#gr-live-grid` 容器，U4 组件增强：菜单页域 enqueues `gr-datagrid` + inline `new window.GrDataGrid({…})`（端点/nonce/pollMs 15000/列头全部 PHP 本地化注入），fetch 失败留存初表（降级契约）；**威胁事件**——新增 `Gr_Security_Log_Repository::recent(30)`（INET6_NTOA 读回完整文本，**脱敏留在页面层**：`gr_mask_ip()` 渲染列，W14 落地真用；附"展示脱敏/封禁走完整存储形"说明行）；**欺诈审计**——`recent('security_conclusion',30)` 恰读结论通道载荷 {suspected_bot→Yes/No、bot_tier}（W13 契约：结论非原始信号）；REST `Gr_Live_Controller`：`GET /greenpng/v1/live` manage_options 门 403、payload 展示形（visitor 截 8 字符+省略号、payload_json/session_id 永不出端点） | 实测 2026-09-12：phpunit **410 tests 2307 assertions**（TrafficPageTest 7——子页注册、三标签原生类+网格挂载+初表真行+短访客、**威胁表脱敏断言（掩码形在场+完整形全 HTML 零出现）**、欺诈列恰 Yes/high+无原始信号、未知标签回落、**网格页域**（traffic 钩 gr-datagrid+inline 含 new GrDataGrid/容器/端点 json 形/pollMs；dashboard 钩零 datagrid）、Gr_Plugin 三路由；LiveControllerTest 5——路由+GET+权限回调、门拒 403/放行、**payload 形状逐行断言+反证 payload_json/session_id 不出**、script_data 标签键序） | :8091 实测——攻击 GET（scanner UA）+2 事件真播；三标签真渲染：live（pageview 真行+挂载+3 nav-tab）、threats（**`::*` 在场、完整 `::1` 全页零泄漏**、scanner_ua 行）、fraud（真结论行 Yes/high）；REST 真分发：匿名 **403**/管理员 **200 rows=4 首行 pageview**；HTTP：traffic 页匿名 **302 登录**；坑位实录：`class="nav-tab` 子串撞上 `nav-tab-wrapper`（计数断言数 `<a class="nav-tab` 前缀——类名子串断言要防前缀撞名）；wp_json_encode 斜杠转义（`https:\/\/…`）——期望值用同函数生成勿手写字面量；REST handler/gate 零参声明模式二用（core 传参 PHP 静默忽略+过 unused-param 嗅探）；清理：u5 事件+结论事件+安全行+AS/瞬态清+sec_left=0+debug.log 回 2126（备份 .u5） | `06` §1/§2.2 |
| U6 | Access Rules | ✅ | 仓储写侧 `Gr_Access_Rules_Repository`：`rules_of_type(type)`（全列含 inactive 行）、`add(type,kind,value,note)`（created_by=get_current_user_id、note 截 191）、`delete(id,type)`/`set_active(id,type,active)`——**type 乘每一次 WHERE**（陈旧标签页够不到另一列表）；`Gr_Access_Rules::invalidate()` 公开 memo 丢弃（写后页面即调——同请求不得再匹配陈旧规则集）；`Gr_Access_Rules_Table extends \WP_List_Table`（class_exists 守卫+require 核心类；列 cb/match_value/match_kind/note/is_active(Yes/No)/created_at；行经构造注入——读已发生的页面层负责）；`Gr_Access_Rules_Page`：admin_init `handle_actions()` 三写操作（add/delete/toggle）**双门 may_write()=current_user_can(manage_options) AND check_admin_referer(NONCE_ACTION,NONCE_FIELD)**；tab 派生 type；校验：kind 白名单、**ban+url 拒收**（URL 豁免只在 allow 侧）、IP 裸址或**族正确 CIDR**（v4 ≤32、v6 ≤128——`203.0.113.0/64` 家族错配拒）、URL 需根植 `/` 且无 `..`；写后 invalidate + `wp_safe_redirect(add_query_arg(admin_url))` PRG；渲染 = nav-tabs（ban 默认/allow）+ delete 表单（nonce+bulk 复选+`submit_button`）+ add 表单（form-table+nonce+URL 选项标注 allow list only）；菜单挂 Traffic 父级下（docs/06 树），`Gr_Plugin` 接 admin_init | 实测 2026-09-12：phpunit **421 tests 2354 assertions**（AccessRulesPageTest 11——add 行形（created_by=7 经 user_id 桩）、delete/toggle WHERE 带 type、**无能力写零插入零跳转**、**无 nonce 写零插入**、合法 add 写+PRG（page=greenpng-access&tab=ban）、非法值电池（垃圾 IP/v4 家族错 /64/ban+url/遍历拒、allow+/checkout 过）、**批量删只删本类型**（2 语句 id=4/7、零 id 跳过、桩 prepare+query 双录去重）、渲染原生组件（nav-tab-wrapper/wp-list-table/form-table/p class="submit"、行文本、2 nonce 字段、复选值）、allow tab 自列、插件接 admin_init+submenu 'greenpng-traffic:greenpng-access' 经 do_action('admin_menu')、**引擎 memo 失效**（allow-over-ban 仅在 invalidate() 后成立）；PluginTest admin_init 断言改双回调断含）；坑位实录：phppcs:ignore 桩码名不符真码（`NonceVerification.Recommended` 写在注释里而真码 `.Missing`——`-s --report=emacs` 先取真码再写 ignore，`trim()` 形状还挂 `InputNotSanitized` 需同注一并豁免）；真核 `check_admin_referer` 返 1/int 非 bool——`strict_types` 下 `: bool` 返回位 TypeError，`(bool)` 显式转；`submit_button` 恒 echo（wrap 只管 `<p class="submit">` 包裹），非 echo 调用会丢按钮 | :8091 实测（eval-file 四臂+HTTP）——**正常臂**：user1+真 nonce POST → 真库行 ban/ip/by1/act1 + PRG `admin.php?page=greenpng-access&tab=ban`；**无能力臂**：user0 同 POST → 零插入零跳转；**无 nonce 臂**：坏 nonce → 真核 wp_die「The link you followed has expired」先于写（GATE_FAILED_OPEN 未打印）、leak=0；**引擎闭环（W4 跨模块）**：/24 ban 拦 203.0.113.9 → allow 覆盖放行 → allow url /checkout `is_url_allowed` → toggle 停用后 ban 透出（is_active 落 0）→ ban 删单后不拦 + **allow 行幸存（rows=1）= 跨 tab 删除保护实证**；双 tab 真渲染（真 WP_List_Table 父类）：table/2 nonce/nav-tabs/URL 选项标注全在场；匿名 HTTP **302 登录**；坑位实录：eval-file 作用域 `$wpdb` 未定义（字符串插值空前缀→查错表名）——局部 `$wpdb = $GLOBALS['wpdb']` 再插值；`set_current_screen('greenpng-access')` 先于 CLI 渲染否则 List_Table 无 screen；清理：探针行全删（表 COUNT=0）+debug.log 回 2126（备份 .u6） | `06` §2.1 |
| U7 | Login Protection | ✅ | 仓储双读：`Gr_Security_Log_Repository::recent_by_rules(rule_ids,limit)`（**动态 IN 占位符列**——implode 拼占位符、数据全经 prepare；空规则集零查询直返）+ `distinct_recent_ips(hours,limit)`（`GROUP BY ip`+`MAX(last_seen)` 去重候选，**窗口 720h=锁仓 TTL 上限 30 天**——任何本插件落的活动锁其放置行必在窗内）；`Gr_Login_Protection_Page`（admin/，SLUG greenpng-login，Traffic 父级下）：admin_init `handle_actions()` 释放写——**双门 may_write()=current_user_can(manage_options) AND check_admin_referer**（`(bool)` 显式转——check_admin_referer 真核返 int，U6 教训二用）；`lock_ip` FILTER_VALIDATE_IP 白名单；`Gr_Temp_Bans::unblock` 释放→PRG `gr_released=1/0`（1=已释放绿条、0=表单过期黄条）；渲染：**恢复指引常驻两 tab**（CLI `wp greenpng unblock <ip>` code 块 + allow 列表覆盖指引带 Access Rules 页路径——**验收项"CLI 解锁指引在页面可见"**）；audit tab=折叠日志 login_fail/login_lockout 读侧（事件名本地化、`gr_mask_ip` 页面层脱敏、hits、reason）；sessions tab=候选地址逐个 `is_locked` 活锁过滤（reason 取锁值、remaining 取 `lock_remaining` 人体化 `Xh YYm`/`Xm YYs`）+ 释放表单——**展示掩码、表单携完整存储地址**（释放需精确键，注释明示）；菜单挂 Traffic 父级（docs/06 树 Access Rules 同级），`Gr_Plugin` 接 admin_init | 实测 2026-09-12：phpunit **433 tests 2400 assertions**（LoginProtectionPageTest 12——recent_by_rules SQL 形（IN 双引号串+ORDER+LIMIT）+空规则零查询、distinct GROUP BY 形、**无能力释放锁存留零跳转**、**无 nonce 同**、合法释放锁消+PRG 三参、陈旧表单 gr_released=0 黄条路径、**垃圾地址不动他锁**、audit 渲染（掩码在场+**完整地址全 HTML 零出现**（audit 无表单）+CLI 指引+allow 指引+hits+reason）、sessions 渲染（掩码显示+`name="lock_ip" value=完整`+reason+`2h ` 时距+nonce+Release 钮）、无活锁空态零表单、插件接 admin_init+submenu 'greenpng-traffic:greenpng-login'）；坑位实录：**多行 SQL 字符串里插值不在首行则单行 ignore 盖不住**（`FROM {$table}` 落第二行报 InterpolatedNotPrepared——插值必须聚在字面量首行、ignore 注释放其上一行；与"ignore 在字符串字面量行"同一知识面的多行版）；printf 位置参数带宽度 `%2$02d` 合法但别手写 `&amp;` 进 esc_html（双重转义翻倍） | :8091 实测（eval-file 四臂+CLI+HTTP）——**arm1**：record_failure×5 真锁 round1/remaining=300 → 页面释放 POST（真 nonce）→ 锁消+PRG `page=greenpng-login&tab=sessions&gr_released=1`；**arm2**（user0）：锁存留零跳转；**arm3**（坏 nonce）：真核 wp_die「The link you followed has expired」先于写、事后锁仍活；**arm4 双 tab 真渲染**：audit=掩码在场+**完整 203.0.113.77 全页零泄漏**+CLI 指引+allow 指引+login_fail/login_lockout 行+hits；sessions=掩码+hidden 完整值+reason 'login lockout round 2'+`9m 44s`（**round2 梯度 600s 现场可读**）+nonce+Release；匿名 HTTP **302 登录**；**arm5 CLI**：重锁 round3/remaining=1200（**轮记忆跨臂延续 1→2→3**）→ `wp greenpng unblock` Success 全文→复跑 Warning 幂等全文（带 Access Rules 指引）；坑位实录：**shell 变量展开的 CLI 串写漏命名空间**（`$CLI unblock` ≠ `wp greenpng unblock`——报错文本说 'unblock' 未注册即前缀丢失的指纹，勿与命令缺失混淆）；锁 600s 自然过期曾伪装成"已释放"——时间敏感断言先看 TTL 是否耗尽；清理：login 双行删（hits 15=5+5+5 三轮精确算术）+瞬态残留清（rounds 记忆 1 行）+**U6 漏网 scanner_ua ::1 2 行一并归零**+debug.log 回 2126（备份 .u7） | `06` §1、`10` §4 |
| U8 | Campaigns | ✅ | 仓储四读（`Gr_Touchpoint_Repository`）：`campaign_breakdown(days,limit)`（`utm_campaign <> ''` + GROUP BY campaign/channel/source/medium + entries/COUNT(DISTINCT visitor_id)/first/last——**direct 流量无战役不入此表**）、`utm_breakdown`（五参元组 GROUP BY，**至少一参非空**才入）、`click_id_breakdown`（`click_id <> ''` GROUP BY click_id+channel——渠道词表已闭 cpc/social）、`campaigns_for_ids(ids)`（IN 动态占位符、**ids 去重+空集零查询**——对比表把 split 里的触点 id 还原成战役名）；`Gr_Conversion_Repository::recent(days,limit)`（**带绑定时的 split 快照**读回——对比表永不重算）；读框共享 `read_frame()`（UTC cutoff + 双钳）；`Gr_Campaigns_Page`（admin/，SLUG greenpng-campaigns，**顶级 greenpng 菜单下 Marketing 首员**）四 tab 读侧零写操作：campaigns（战役量）、utm（五参元组、空参格 `—`）、clickids（点击 ID+渠道）、models（**验收核心：归因对比只显示实测计算值**——行=战役+币种、列=first/last/linear/position/time_decay 五模型固定序、格=记入金额+占该模型总额百分率；**快照语义**：读转化行随带的 model_weights 快照，转化后落地的触点永不重写历史；`(deleted touchpoint)` 标签=瘦身已移除但快照仍记（历史保全）、`(no campaign name)`=触点无战役名；**direct 转化不入行只计数**（"N conversion(s) carry no model credit" 注记）；空态永不编造数字（无转化=无表无百分率）；行序=跨模型合计降序）；菜单挂 `greenpng-dashboard:greenpng-campaigns` | 实测 2026-09-12：phpunit **446 tests 2454 assertions**（CampaignsPageTest 13——三 breakdown SQL 形（WHERE/GROUP BY/ORDER/COUNT DISTINCT 逐断）、campaigns_for_ids（IN (12,13) 去重恰 2 键、空集零查询）、conversions recent SQL 形、campaigns tab 渲染（4 tab href+行值 entries/visitors）、campaigns 空态无表、utm 元组+`—` 空占位、clickids 行+空态 gclid 家族提示、**models 表**（五列头固定序、spring/summer 行、100.00 (100.0%)/50.00 (50.0%)/40.00 (40.0%) 逐格、`(deleted touchpoint)` 行 10.00 (10.0%)、direct 注记）、**models 空态反证**（无 widefat、无 `%)`——验收"只显示实测计算值"的可测形）、菜单注册）；坑位实录：printf 的参数嗅探不认 `self::` 常量为已转义——`esc_html(sprintf(__(),const))` 整包形态过 OutputNotEscaped（逐参 esc 形态对常量参数不适用）；**全插值 IN 列表触发 UnfinishedPrepare**（串里无字面 %s 占位符可认——豁免码补在字符串行上方的 InterpolatedNotPrepared 同注里，prepare 调用行上方那条盖不住字符串行）；测试内 FQCN 前缀 `GreenPNG\Core\...` 在带 namespace 测试类里解析成 `GreenPNG\Tests\Unit\GreenPNG\Core\...`（U3 教训重犯——跨命名空间一律 use 导入） | :8091 实测（eval-file 真链种子+双臂+HTTP）——**种子走真服务链**：`Gr_Touchpoint_Repository::record` 三触点（spring/google/cpc+gclid、summer/meta/social+fbclid、spring 再入）+ `Gr_Attribution_Service::bind` 双转化（129.90 USD 走链 / 40.00 USD direct）；四 tab 真渲染：campaigns spring_sale entries=2/visitors=1、utm `<td>google</td>`+`—` 空参、clickids gclid 值+cpc+social 双渠道、**models 数学吻合**（first 129.90 (100.0%)=spring、linear 86.60=129.90×2/3、**position 103.92=40+40 spring+20 summer 精确**、direct 注记在场）；**arm2 瘦身保全**：删 summer 触点行后重渲染——`(deleted touchpoint)` 标签出现、summer_sale 行消失（窗口内无触点行）、**43.30 (33.3%) linear 份额改记 deleted 行**（转化行原 split 快照不重写——历史保全镇）、first/position 原值不动；匿名 HTTP **302 登录**；清理：触点 3+转化 2 行删净（双表 COUNT=0）+debug.log **零增量** 维持 2126（备份 .u8） | `06` §1、`03` §4 |
| U9 | URL Builder | ✅ | `Gr_Url_Builder_Page`（admin/，SLUG greenpng-url，顶级菜单 Marketing 次员）：`build(landing,source,medium,campaign,term,content)` **公开纯函数**（零超全局/零存储/零网络）→ {url,error}——**落地校验** `wp_parse_url` 方案 http/https+host 必在（javascript:/data:/ftp:/协议相对/裸串全拒 error 'landing'）；**三件套强制**（source+medium+campaign 非空——缺任何一件本插件归因读不回战役入口，error 'trio'）；term/content 可选空则不入；**值在构建器层 `rawurlencode`**（真核 add_query_arg 组 query **不编码值**——build_query 传 urlencode=false，编码责任在唯一知道值是任意管理员文本的层）经 `add_query_arg`（已有 query 保留）；渲染：**GET 表单**（纯计算零状态变更——填充态可分享免 nonce，manage_options 门已在）hidden page + form-table 六行（落地 URL+五 UTM）+ `submit_button`；结果面板 `<code>esc_url($url)</code>` + readonly 拷贝框（esc_attr）+ 描述；错误双 notice（trio/landing 各本地化文案，**错误时零 URL 输出**）；顶部"全程本地不外呼"描述 | 实测 2026-09-12：phpunit **456 tests 2499 assertions**（UrlBuilderPageTest 10——全参组合（五参在场+**值 %20 编码**）、**已有 query 保留**（p=42+utm 并存）、空可选参不入串、三缺一三向拒、非 http 落地五形拒（javascript:/data:/ftp://、协议相对、裸串）、空表单（form-table+method get+hidden page+Build link 钮、**零结果面板反证**）、合法输入渲染（组合 URL+readonly 值+表单回显）、三缺一渲染（notice-error+零 URL 反证）、**零外呼源级机器断言**（源码无 wp_remote_/curl_/fsockopen/dns_get_record——验收项"零外呼"的可测形）、菜单 'greenpng-dashboard:greenpng-url'）；桩补 esc_url/esc_url_raw（http/https+根植相对门+&amp; 实体化差异）+ **add_query_arg 桩改真核语义**（值不编码——与真核 build_query urlencode=false 对齐）；坑位实录：**真核 add_query_arg 不编码值**（桩 http_build_query 默认编码=语义分歧，实站探针 `utm_term=running shoes` 裸空格揭穿）——**跨桩/真核的编码责任类分歧要在值含空格的探针里才现形**，安全字符值两态同形；esc_attr__ 桩缺——`esc_attr( __() )` 组合等效零新增 | :8091 实测（eval-file 四态+HTTP）——空表单（form-table/method get/hidden page/零面板）；合法输入：组合 URL（welcome=1 **已有 query 保留**+utm_term `running%20shoes` **值编码**）+code 输出+readonly；三缺一：notice-error 且全页零 URL；javascript: 落地：notice 且零链接；**zero_outbound 渲染 HTML 断言**（src/href 无任何外部 http 引用）；匿名 HTTP **302 登录**；纯渲染页零 DB 写零瞬态——**零清理**，debug.log 维持 2126（备份 .u9） | `06` §1 |
| U10 | Audit Log | ✅ | `Gr_Audit_Diff`（core/ 纯函数：共享键双方皆数组则递归、**数组↔标量转变记单条 modified**（整值级 old/new）、严格 `!==`、点路径基于标识符键；参数 `before`/`after` 避 `$new` 保留字告警）；`Gr_Audit_Repository`（storage/）：`log()` **diff 在写时计算一次**存 `diff_json`（表无 old/new 原料列——存结果而非原料，展示层不重算——DB 往返字符串化后重算会报出没人做过的类型变化）；`query()` 白名单过滤（user_id/object_type/object_id/action，未知键零子句）、**无过滤 COUNT 直接裸跑**（真核对无占位符语句 prepare 是 doing-it_wrong；表名来自 DDL 注册表）、LIMIT 钳 1..200/OFFSET ≥0、COUNT 与页读共用 WHERE；门面 `gr_audit_diff`/`gr_audit_log`/`gr_audit_query`；`Gr_Audit_Log_Table`（column_object=type #id、column_changes **逐行 esc_html** 免 kses、user_id #id 引用）；`Gr_Audit_Log_Page`（SLUG greenpng-audit，顶级菜单 Tools 首员，**服务端分页 PER_PAGE=20** + `paginate_links` core 锚点 + GET 过滤表单（object_type/user_id/action）+ diff 展开三形态纯文本行（`+ path = value` / `~ path: old → new` / `- path`）60 字符截断）；**U6 Access Rules 三写接线**（add/delete/toggle 各记一行——`row_of()` 单行读做写前快照，delete 记全行 removed 快照、toggle 记 is_active 单字段 old/new；双门失败零审计行） | 实测 2026-09-12：phpunit **478 tests 2599 assertions**（AuditLogTest 22——**递归 diff 电池**：根级三态/嵌套共享键只报 `rule.flags.active`/嵌套增删落 `a.z`+`a.y`/数组↔标量单条 modified/严格类型 `'1'` vs `1`/同态空 diff/门面等价；log 行形（表名、字段、**diff_json 内容**、insert_id）；query 无过滤（count 无 prepare+`LIMIT 20 OFFSET 20`）、白名单（未知键 `DROP TABLE` 零子句）、钳制（9999→200、-5→0）、门面等价；页面空态（无编造+**零分页调用**）、分页（total 45→**3 页**、current=1、base `%#%`）、页码+过滤（LIMIT 20 OFFSET 20+GET 透传）、diff 三形态行、菜单 `greenpng-dashboard:greenpng-audit`；**U6 三写审计行**（add 的 diff added 含全语义字段、toggle `is_active` old 1→new 0、delete 全行快照、**门失败零行**））；坑位实录：**phpcs prepare 系违规定位在字符串字面量行或参数行而非语句首行**——UnfinishedPrepare 落字符串行（豁免必须并进字符串行上方那条 ignore，U8 教训重验）、**ReplacementsWrongNumber 落 prepare 首行且 spread 数组计 1 参**（豁免码并进 prepare 行上方 ignore）；**prepare 无占位符语句 = 真核 doing-it_wrong**（无过滤 COUNT 裸跑）；phpcbf 对齐重排会移 doc 行——先 phpcbf 后放 ignore；桩的 prepare+读各记一次 queries（**断言索引计双记**——count×2、page×2）；WP_List_Table 桩升级 column_{$key} 方法分派（core 顺序 cb→column_{key}→default） | :8091 实测（eval-file 两臂+HTTP）——**arm1 真页面写链**（wp_set_current_user(1)+wp_create_nonce+**$_REQUEST 同步**——CLI 不并 $_POST 进 $_REQUEST 而 check_admin_referer 读 $_REQUEST，第一轮探针 wp_die "link expired" 揭穿+wp_redirect filter 短路）：add/toggle/delete 三真审计行（diff JSON 现场核：added 全语义字段、`is_active` 1→0、delete 全行快照含 toggle 后状态）；**anon 0 泄漏**；**arm2 渲染**：45 行 seed→P1/P2 各 20 数据行、`3 pages` 标签、`current">1<`/`current">2<`、`paged=2` 链接、`~ seq: ` 行、过滤 login_lockout 收窄（零 access_rule 行）、空类型=空态**无分页**、门面 query total=45/p2=20；**anon HTTP 302 登录**；清理：审计 45 行删净 COUNT=0+规则表 0 残留+debug.log 裁回 **2126**（新增仅 WP 自动更新消息+真核 List_Table CLI 无 HTTP_HOST 环境噪声×8——**零插件错误**；备份 .u10；gr_db_version option 系既有合法状态非本任务污染——白名单口径自校） | `03` §8、`05` #13、`06` §1 |
| U11 | Data Retention | ✅ | `Gr_Retention`（storage/）：**双轨瘦身骑手**挂 `DAILY_HOOK` 优先级 10（aggregator 5 之后——汇总先行，瘦身不吃未吸收数据）；age 轨 `prune(table_key,date_col,days,batch,deadline)`（DELETE WHERE date_col < cutoff ORDER BY id ASC LIMIT 2000 批循环+usleep(100ms)——**date_col 标识符形状校验**（`^[a-z_]+$`）拒注入面、days ≤0 保全部）；行数轨 `prune_rows(table_key,max_rows)`（边界=第 max_rows+1 新行 id：`SELECT id ORDER BY id DESC LIMIT max_rows,1`→**DELETE id <= boundary**——边界行自身即首个被逐行，`<` 会漏删使 ceiling+1 表瘦 0 行）；**时间预算墙**（默认 10s，`run_daily(?$budget)` 到点留明日——瘦身是收敛非事务）；8 张可瘦表+日期列注册表（security_logs/last_seen、sessions/started_at、events/touchpoints/audit_logs/created_at、funnel_sessions/entered_at、cart_abandonments/captured_at、daily_stats/stat_date——静态表与 conversions 聚合历史永不入册）；`optimize(table_key)` 手动 only（OPTIMIZE TABLE，InnoDB 重建锁表——**cron 永不自动**）；`counts()` 页面行数；settings `retention_days`+**新 `retention_rows` 轨**（0=仅天数；宽上限默认=失控保险非主瘦）；`Gr_Data_Retention_Page`（SLUG greenpng-retention，Tools 第三员）：save 臂（双轨表单 8 行——键白名单+absint 钳 days≤3650/rows≤1e8、PRG gr_saved=1、**审计行 diff 递归跨 retention_days.events 嵌套键**）、optimize 臂（复选+按钮、逐表审计）、文案明示 cron 绝不自动 OPTIMIZE；门面 `gr_prune_table`（docs/03 §10 签名） | 实测 2026-09-12：phpunit **495 tests 2720 assertions**（DataRetentionTest 17——prune SQL 形（WHERE/ORDER BY id ASC/LIMIT 2000）+days0/非标识符列零语句、短批停循环、门面等价；prune_rows 边界查询形+**DELETE `id <=` 边界**+无边界跳过；run_daily 双轨齐发（events/sessions 三语句在场、未设轨表零出现）+**预算墙 budget=0 零语句**+**`run_daily`/`prune`/`prune_rows` 三方法源级反证无 OPTIMIZE**（ReflectionMethod 源段——"cron 绝不自动 OPTIMIZE"的可测形）；optimize 手动路径+失败返 false；双轨默认全表在册+daily_stats 仅天数；渲染（8 行输入+12,345 行数格式+8 复选+文案+双 nonce）、save 白名单钳制（bogus 键零落库、他键保值）+**审计 diff `retention_days.events {old:30,new:14}` 递归嵌套键断言**、钳 ceiling、optimize 只重建勾选表+逐表审计、门失败零写、菜单+骑手优先级 10）；坑位实录：**行数轨边界 off-by-one 是 live 揭穿的真缺陷**（arm3 真库 2 行/上限 1 → 修复前删 0 修复后删 1——单测断言语句形状 `id < 5000` 与保留语义脱节=测试替假实现盖章，形状断言必须锚住数学）；探针 INSERT 用错列名（name/group_name vs 真列 event_name/event_group）→ 全败插入让 OLD-DELETED=yes 成"从未存在"的假阳性——**探针输出缺行（SEEDED 未打印）本身就是断言**；双记陷阱再现（prepare+执行各录 queries——DELETE 计数 array_unique、边界 SELECT 后 DELETE 在 queries[2]） | :8091 实测（eval-file 三臂+HTTP）——arm1 真页面写：save（events 14/5000 落库+security_logs 30 不动+审计行 diff 嵌套键精确）+optimize（真跑 OPTIMIZE TABLE dev sessions+审计行 object_id=sessions）+骑手真跑+渲染（8 输入+PRG notice+文案）；**arm2 真删证明**：旧行（40 天前）删/新行留（age 轨 14 天）；**arm3 行数轨数学**：2 行/上限 1 → **恰删 1 留最新**（修复验证）；**anon HTTP 302 登录**；清理：audit/events 探针行 COUNT=0+**settings 恢复默认**（days events=30/rows=1000000——写探针动过的设置必须写回）+debug.log 裁回 2126（新增=探针错列名 2 条 DB error+HTTP_HOST 噪声，零插件缺陷；备份 .u11） | `05` §5、`06` §1、`03` §10 |
| U12 | Status & Diagnostics | ✅ | `Gr_Table_Stats`（storage/，docs/03 §10 `gr_get_table_stats`）：information_schema 目录读（`table_schema = DATABASE() AND table_name LIKE %s`——**短键经 Gr_Database 注册表反解**过滤非本插件表；rows=引擎估算 by design——容量读非账务读，精确 COUNT 在 Retention 页）+ **transient 缓存 300s** + `flush()`；`Gr_Diagnostics`（core/，`gr_export_diagnostics`）：**白名单字段集**（plugin.version/environment{php,wp,db,multisite,locale}/queue{backend,next_daily}/adapters/tables/generated_at）——**"不能被抄进去的凭据就漏不出去"**（字段逐名挑选，永不读 option/行/secret 内容）；**DB 家族判定以 `db_server_info()` 身份串为权威**（CLI 上下文 `is_mariadb` 可为 NULL——live 揭穿：dev MariaDB 12.3 显示 "MySQL"；含 MariaDB 字样→MariaDB，fallback 空防御标志）；适配器状态 = detector 词汇（bridge=>{mounted,file}——只报桥名与文件名）；`Gr_Status_Page`（SLUG greenpng-status，Tools 第四员）：环境表/队列卡（backend 人话+下次运行 UTC+**wp-cron 时低流量引导 notice**：系统 cron + `wp greenpng maintenance` 命令——已有 CLI 命令，docs/09 §4）/适配器 roster/表容量（自写 bytes 格式化 B/KB/MB 免新桩）；**导出下载流**：POST 双门 → 审计行 → header×2 + JSON 体 + **exit（流即响应）**；门面 `gr_get_table_stats`/`gr_export_diagnostics` | 实测 2026-09-12：phpunit **506 tests 2775 assertions**（DiagnosticsTest 11——catalog SQL 形+外来表滤除（wp_posts 不入）+四字段整形+**缓存零二查**+flush 后二读（4 记录=2 读×双记）；export 白名单集（PHP_VERSION/6.7/`MariaDB 12.3`/backend wp-cron/adapter mounted=false/表容量随行）+**脱敏机器断言**（毒化 option gr_secret_meta/gr_settings license 键→JSON 零 TOPSECRET/secret/password/license/token 字样+IP 正则不中——验收"无任何凭据/敏感值"的可测形）+门面等价；**DB 家族双场景**（identity 串 MariaDB→MariaDB、MySQL 串+false 标志→MySQL）；渲染（真值行/WP-Cron+CLI 命令文案/Not installed/1.5 MB+512.0 KB/nonce+按钮）；导出门失败零审计零查询、非 export 动作不路由、菜单+admin_init）；坑位实录：**detector 返回结构是 {plugin,active} 而非裸文件名**——消费方先读源再定形（错配=Array to string 炸在渲染深处）；**is_mariadb 在 CLI 是 NULL 且读它出 warning**——身份判定换 db_server_info() 串（版本串 12.3.3 本就是 MariaDB 独有方案号）；assertRegExp PHPUnit 10 移除→assertDoesNotMatchRegularExpression | :8091 实测（eval-file 两臂+HTTP）——arm1 真栈快照：PHP 8.5.10/WP 7.1/**MariaDB 12.3.3（修复后）**、backend **action-scheduler**（dev 有 WC+FluentForm→AS 在场——wp-cron 引导 notice 按条件不显示）、适配器四桥真状态（woocommerce/fluentform mounted、cf7/wpforms not）、**15 表目录真读**（sessions rows=12）、NO-SECRET/NO-IP 双清；**arm2 导出流**：STREAM-BEGIN 后**纯 JSON 体**（结构完整至 generated_at）、**退出标记永不打印=exit 在流内 fired**、审计行先落；清理：审计 2→0、transient 删、settings 仍默认（30/1000000）；**anon HTTP 302 登录**；debug.log 裁回 2126（新增=修复前 null 属性 warning×3+CLI headers-already-sent×2——环境态零插件缺陷；备份 .u12） | `03` §10、`09` §4、`06` §1 |
| U13 | **Settings 独立页**（OQ-1 决议） | ✅ | `Gr_Settings_Page`（SLUG greenpng-settings，**顶级菜单第一子页**——先于 Dashboard 镜像；三 tab：General（marketing_ip_anonymize/marketing_consent_fallback 隐私默认+**数据保留入口链接**+**卸载行为选项**——gr_delete_data_on_uninstall 独立 option，off=删行即缺省语义）、Security（security_enabled 总开关/security_action_mode 档位 select 词表 log|block/security_log_anonymize/trust_proxy_headers+trusted_proxies 文本→数组清洗 trim/空滤/截 20/probe_enabled 探针开关）、Attribution（attribution_enabled/attribution_cookie_days 数字钳 1..365/attribution_default_model select 五模型词表））；**每 tab 自带字段白名单**（外来 POST 键零落库；checkbox 缺席=0 语义）；双门+nonce；PRG gr_saved=1+tab；**审计行=扁平快照 diff**（前后 snapshot 平键数组→递归 diff 报移动的键；未动即零行——空保存不写审计）；**Gr_Settings memo 改进程级静态**（live 揭穿：实例级 memo 在同进程写后读回旧值——CLI/长请求下 gr()->settings() 陈旧；静态+set 全实例失效；reset_for_tests 桩挂 reset 惯例段） | 实测 2026-09-12：phpunit **517 tests 2822 assertions**（SettingsPageTest 11——三 tab 渲染（字段在场+checked 态+五模型 value+retention 链接+nonce）、general save（consent 0→1+**缺席 checkbox=0**+卸载 flag 落 '1'+audit diff 双移动+PRG tab/gr_saved）、flag off 删行、security save（**proxy 列表清洗** ' a /24 , b , ,'→[a,b]+bogus 档位→log）、**探针开关双臂**（缺席=0 落库→再 save 恢复）、attribution save（days 400→**365 钳**+bogus 模型→last）、**未动保存零审计行**（before===after 反证）、门失败零写零重定向、**菜单第一子页断言 slugs[0]==greenpng-settings**）；坑位实录：**prefix 纪律测试扫裸 `const`**（无修饰符的 const 被扫 GR_ 前缀——仓内惯例显式 `public const` 即不触发；新页常量补 public）、**菜单结构变化破既有索引断言**（Dashboard/Traffic 测试的 submenu[i] 顺序更新——结构合法变化时断言语义不变只换索引）；**静态 memo 破测试隔离**（gr_stub_reset_options 重建 options 而 memo 跨测试存活→reset_for_tests 挂进桩 reset 的服务段——Gr_Plugin/Ab_Experiments/Scanner_Ua 同列） | :8091 实测（eval-file + HTTP）——三 tab 真渲染全绿；**探针开关真联动闭环**（save security 缺席 checkbox→probe_enabled 落 0→**Gr_Probe_Script::enqueue 后 wp_scripts 队列不含 gr-probe**（修复前 yes-bad：gr() 读陈旧 memo→gate 误过；memo 静态化后 no-good）→save 恢复 1→**队列含 gr-probe**——验收"探针开关实测生效"的队列级实证）；审计 2 行（probe 1→0→1 两移动）+ 两次 PRG；**anon HTTP 302 登录**；清理：settings 写回默认+audit 0+卸载 flag absent；debug.log 裁回 2126（新增全为 phar 弃用噪声——**零插件错误**；备份 .u13） | ADR-0007、`06` |
| U14 | Bot & Device Signals | ✅ | `Gr_Security_Log_Repository::log()` 扩可选 `$action` 参数（默认 'logged' 截 32——向后兼容，VALUES() 携 %s）；`Gr_Crawler_Verify`：`LOG_RULE='fcrdns'` + **verify() 每次新鲜 DNS 走查落一行安全轨**（verdict→action_taken、PTR 主机名→reason——页面读 DNS 真查到的东西而非刮瞬态缓存；24h 缓存命中不重复落账；record-only docs/12）；仓储三读：`fcrdns_recent(hours,limit)`（INET6_NTOA+rule 过滤+最新前+LIMIT 钳 100）、`fcrdns_summary(hours)`（GROUP BY action_taken、SUM(hit_count)=walks、COUNT(*) **AS fold_rows**——`rows` 是 MariaDB 保留字 live 揭穿，SQL 别名 fold_rows/PHP 键 'rows'）、`ua_engine_stats(hours,limit)`（scanner_ua、GROUP BY user_agent、SUM(hit_count) hits 降序）；`Gr_Session_Repository::bot_score_distribution(days)`（**单聚合六带 CASE** 0/1-25/26-50/51-75/76-99/100+SUM(is_bot)+total、天窗钳 1..365、零态 empty_bands 全 0）；`Gr_Bot_Signals_Page`（SLUG greenpng-bot，Traffic 父级下 Access/Login 同级）：三段只读汇总——FCrDNS（汇总行 walks 计数+表：掩码 `gr_mask_ip`/PTR `—` 空占位/verdict/walks/last seen）、UA 引擎表（agent/hits/fold rows/last seen）、bot_score 分布（human/bot 行+带表+% share number_format(,1)）；**无 POST/无 nonce/无写臂**；空态各行自述零编造（两段说明行仅窗口有数据时渲染）；设备信号标签 v1.3 注记不做预览 | 实测 2026-09-12：phpunit **528 tests 2890 assertions**（BotSignalsPageTest 11——verify 落账电池（fresh 一次行含 'fcrdns'+'verified'+真 PTR 主机名进 reason、缓存二次零新行、unverified 同落）、log() $action 向后兼容（默认 'logged'+40 字截 32）、四读 SQL 形（rule 过滤/GROUP BY/ORDER/LIMIT 钳 100/INET6_NTOA+**fold_rows 别名断言**）、分布带 CASE SQL+365 天窗钳（before/after 双沿容秒级跳变）+零态全 0、渲染（三段+掩码+**完整 IP 全 HTML 反证**+75.0% share+walks 汇总行）、空态反证（无 '%)'/无 0 汇总行/**无表单无 nonce**）、菜单 'greenpng-traffic:greenpng-bot'）；坑位实录：**`rows` 是 MariaDB 12 保留字**（COUNT(*) AS rows 语法错——live SQL error 揭穿；别名换 fold_rows+PHP 键不变）；**渲染期望值要与表真实内容对齐**（存量 12 会话使 13 human/86.7% 而非种子独占的 1/33.3%——针文按"只有我种子"写即 MISS 假信号，先 COUNT 再写针文）；**HEAD 基线 phpcs 欠账揭穿**（U11–U13 提交时管道掩盖退出码——`phpcs | tail` 的 EXIT 是 tail 的；stdin 探针证 HEAD 已红，欠账独立提交修清后 U14 四检才真绿） | :8091 实测（eval-file 三探针+HTTP）——**种子走真链**：verify 真跑 DNS（PTR 真名 crawl-66-249-66-1.googlebot.com、正向未确认→unverified **如实落账**）+ 缓存二次零新行（fcrdns COUNT=1）+ scanner 折叠双写 hits=2 + 3 会话 bot_score 0/90/100；**渲染全绿**：三段+真 PTR+掩码 66.249.66.\*+完整 IP 零泄漏+13 human/2 bot（total=15 含存量 12）+86.7% 带 share+SUMMARY unverified walks=1 rows=1（修复前保留字报 SQL 语法错——缺陷现场即修）；**匿名 HTTP 302 登录**；清理：今日 5 行删净（fcrdns 1+scanner_ua 4——含 WP-CLI UA 被引擎真实检出 3 行=引擎真工作证明）+会话种子 3 删存量 12 留+fcrdns 瞬态删+**AS gr_crawler_verify 3 任务删**（findings 链真入队、AS_GR_LEFT=0）+审计零行（只读页）+debug.log 裁回 2126（新增=保留字 DB error 2 条+探针自身 3 错——零最终态插件错误；备份 .u14） | `06`、`12`、`05` §3.1 |
| U15 | Analytics & CAPI 页 | ✅ | `Gr_Analytics_Page`（SLUG greenpng-analytics，顶级菜单 **Integrations 区首员**（URL Builder 之后）、双门 admin_init 写臂共享单 nonce——按钮即页面的一部分）：Meta CAPI + GA4 MP 双服务块；**凭据只走 Gr_Secrets**（四 option：gr_secret_meta_pixel_id/gr_secret_meta_access_token/gr_secret_ga4_measurement_id/gr_secret_ga4_api_secret——信封加密 autoload=no）；**空字段=不变**（重存表单不清空凭据，remove 复选框是唯一出口且**联动关开关**）；**半对整拒**（孤 id 无 secret 拒存——看起来配置了实则不可用）；形状门（Meta pixel `^[0-9]{5,20}$`、GA4 id `^G-[A-Z0-9]{4,12}$`、secret ≥20 字符 token 字符集）——拒存零落库 PRG gr_error=字段名；开关入 gr_settings（capi_meta_enabled/capi_ga4_enabled 默认 0——全部默认关）；**审计行=状态词表 diff**（enabled 0→1、id/secret absent→stored→removed——值永不入审计）；**v1.0 自检=本地**（解密回读+形状四态词 absent/broken/shape/ok——出网连通性探测（GA4 debug 端点）按 docs/07 §5.2 归 I3 经 Http_Client 交付，页面文案如实声明）；渲染：**未配置=显式状态非空白**（"Not configured."+开关无效说明）、已配置=掩码预览（Gr_Secrets::mask 首 2+****+尾 2，短值全掩）**明文永不回显**（输入框恒空）；双提交按钮共享 name=gr_analytics_action 值即动作（core 原生形态）；同意门控披露文案（营销同意后才派发/PII 仅哈希出网/全程走队列）；readme External services 段已有双服务披露 | 实测 2026-09-12：phpunit **540 tests 2964 assertions**（AnalyticsPageTest 12——未配置渲染双显式态+四字段+双钮+同意文案、save 落库（autoload=no+**信封≠明文**+reveal 回读+PRG+审计词表+**全语句零 token 机器断言**）、空字段保留凭据（audit 仅 enabled 移动）、半对整拒（gr_error=meta_pair 零落库零审计）、形状门三向（meta_id/meta_secret/ga4_id）、remove 忘却+开关归零、**本地自检四态电池**（absent/ok/broken（伪信封直写 option）/shape（越过 save 门直存坏形状——手改 option 的现场形态））、已配置渲染（双掩码在场+**双明文全 HTML 反证**+输入恒空）、门失败双臂零写零跳、PRG 标记渲染、mask 边缘电池、菜单+admin_init）；坑位实录：**case 合并臂里 `'meta' === $action` 恒假**（$action 是 'check_meta' 不是 'meta'——三臂全派给 ga4，测试抓住；比较对象必须是与分支值同形的常量）；**U14 提交后改 SQL 别名未复跑 phpunit**（fold_rows 改了仓储读键、测试闭包种子键没跟——528 绿后的一切改动都要重跑四检再提交）；Gr_Audit_Repository 在 Storage 非 Core（use 导入写错=运行时才炸）；phpcs ignore 每行一条（相邻两行 $_POST 各需自己的注释） | :8091 实测（eval-file 三臂+HTTP）——**arm1 真写链**（wp_set_current_user(1)+真 nonce+$_POST/$_REQUEST 双设+wp_redirect filter 短路）：双凭据落库为信封（**PIXEL-IS-PLAINTEXT no/TOKEN-CARRIES-PLAINTEXT no**）+真库 autoload='off'+reveal 回读 ok+开关 1+**审计 diff 纯词表**（`{"enabled":{"old":0,"new":1},"id":{"old":"absent","new":"stored"},...}`）+PRG gr_saved=meta；**arm2 渲染**：Configured（掩码 EA\*\*\*\*ef/12\*\*\*\*56）+GA4 "Not configured." 显式+同意文案+**双明文全 HTML 零泄漏**；**arm3 自检真 POST 路径**：check_meta→gr_result=ok（真 openssl 回读+形状）/check_ga4→absent；**arm4 门失败**：无能力 user0→零写零跳；坏 nonce→**真核 wp_die "The link you followed has expired" 先于写**（末行永不打印=absence is the assertion）；**匿名 HTTP 302 登录**；坑位实录：**同进程静态 memo 陈旧读再现**（清理臂 update_option 直写后 gr()->settings() 仍回 1——CLI 启动时 inspector 已建 memo；新进程读 DB 真值 0 证清理实成——设置恢复必须跨进程验证）；eval 探针裸类名不解析（FQCN 强制）+ Gr_Secrets 命名空间笔误（Admin≠Core）；清理：四 secret option forget 全 absent+settings 恢复默认（跨进程证 0/0）+审计 1 行删 COUNT=0+**引擎侧今日 2 行**（WP-CLI UA 检出）删+AS gr_ 0+debug.log 裁回 2126（新增全为 phar 弃用噪声+探针笔误 2 条——**greenpng/plugin 零行**；备份 .u15） | `07` §5、`06`、`10` §2 |
| U16 | IP Intelligence 页 | ✅ | **`Gr_Geoip_Refresh`**（core/，站长点击才存在的出网任务）：`register()` 挂队列钩子（点击可能在早前请求发生）；`enqueue()` **PENDING 瞬态去重**（重复点击拒二次派发，TTL 1h 自愈丢失任务）+ `Gr_Queue::enqueue`；`run()` 作业体——**经 Gr_Http_Client(SERVICE_DBIP) 唯一出网门**（月戳 URL `download.db-ip.com/free/dbip-country-lite-{Y-m}.csv.gz`）；失败臂：带 retry_after → 按梯级重派（PENDING 保持防双派）、give_up → 停（客户端已记审计）+ PENDING 落；**gzip 魔数头门后 gzdecode**（垃圾体零 warning 静默失败）；落盘 uploads `gr-geoip` override（mkdir 一次）→ `Gr_Geoip_Packer::pack` → tmp 清理 → **空包拒晋升=旧库原样**（packer rename 语义）→ `Gr_Geoip::reset_for_tests()`（引擎换库缝）→ 审计 `refresh` 行（before=换库前源/日期快照、after=result 词表 refreshed/failed+reason+计数——**端点永不入审计**）；`Gr_Ip_Intel_Page`（admin/，SLUG greenpng-ipintel，**Integrations 次员**（Analytics 之后））：渲染=数据版本表（源 bundled/override 词、日期、双族计数、国家数、generated_at——number_format 千分位）+ **归属披露**（DB-IP/CC BY 4.0/许可证链/NOTICE 随包说明/uploads 刷新不触插件目录/失败沿用旧库）+ **更新表单**（nonce + "点击才向 db-ip.com 发一次下载请求，无任何自动或计划更新"明示文案）+ PENDING 态按钮退位为状态行 + PRG 双通知（queued 绿/pending 黄）；AbuseIPDB **v1.2 路线注记单行**（无控件无预览，U14 设备信号标签同款纪律）；写臂=admin_init 双门（manage_options + check_admin_referer）→ enqueue + **审计 `update_requested`（词表 queued yes/already_pending，user 归档——点击即出网授权的审计痕迹）** + PRG；页面源零外呼面（出网只在作业内经 Http_Client） | 实测 2026-09-12：phpunit **576 tests 3549 assertions**（GeoipRefreshTest 6——**入队去重三段**（首队 1 cron+PENDING 起、二击拒、完清后再通）、**成功链**（gz 体真解包→override 真落盘（v4 bin 在、tmp 无）→state override/当月/3 段→引擎答新库 DE→PENDING 落→**审计一词表**（refreshed 在场/db-ip.com 反证））、**线败重派**（PENDING 预置=真点击态→客户端真梯级包裹→cron 单条 +30s 延迟戳→PENDING 保持）、**give_up 停**（预置 fails=3 账本→第 4 败→零重派+PENDING 落+客户端 http_give_up 审计一行（计数无端点））、**解包败保旧**（预播 2026-08 旧 override→垃圾体→旧库原样 override/2026-08+审计 reason=unpack）、**空包保旧**（合法 gzip 无用体→packer 拒晋升→旧库原样+reason=empty_pack））；IpIntelPageTest 9——渲染（真 bundled 态 2026-09/357,325/359,845+披露+nonce+按钮+明示文案+AbuseIPDB 注记**无控件反证**）、PENDING 态（按钮退位）、PRG 双通知、**合法点击**（1 cron+PENDING+审计 user=7+**diff 含 queued 词**+PRG gr_update=queued）、重复击（零二次派发+already_pending 词+PRG pending）、无能力（零派发零跳零审计）、坏 nonce（同+PENDING 不起）、**菜单**（Gr_Plugin::run 后 admin_menu→submenu parent=greenpng-dashboard/cap/callback 断言）、**页面源零外呼**（wp_remote_/wp_safe_remote_/curl_/fsockopen 四反证）；坑位实录：**gzdecode 对垃圾体先 warning 再 false**（PHPUnit warning→error——魔数头 `\x1f\x8b` 前置门，队列作业须静默失败）；**PENDING 是 enqueue 的产物不是 run 的**（直调 run 的测试要预置 PENDING 才是在测真态）；**wire 桩错误必被客户端梯级包裹**（想测 give_up 臂要预置 fails=3 账本而非伪造 give_up 错误对象）；**bool 词表骗过 diff**（`pending=>!$queued` 使成功点击 diff 恒空 false→false——审计词表用词不用布尔，live 一眼揭穿）；菜单臂须 `Gr_Plugin::reset_instance()+run()` 再 do_action（admin_menu 钩子是 Gr_Plugin 挂的） | :8091 实测（eval-file 五探针+HTTP）——**渲染**：12 针全真（bundled/2026-09/357,325/359,845/DB-IP/CC BY/许可证链/nonce/动作钮/无自动更新文案/AbuseIPDB）+按钮在场+页面零端点；**匿名 HTTP 302 登录**；**arm 点击链**（user1+真 nonce+redirect 滤器短接）：PRG gr_update=queued+PENDING 起+**AS 真入队 1 条**（group greenpng）+审计 update_requested user=1；**arm 重复击**：PRG pending+AS 仍 1；**arm 无能力**（user0）：零跳零审计增量；**arm 坏 nonce**：真核 wp_die "The link you followed has expired" 先于写、**末行标记永不打印**；**arm 真作业执行**（AS 自跑 action 85 complete——dev 后端在管理请求触发）：失败路径全真——**线败（沙盒把外域 DNS 映入 198.18.0.0/15，核心 SSRF 防护正确拒发该段→"A valid URL was not provided."）→梯级重派 30s/120s 真入队（action 86-89）→账本 fails=2→旧库保全（bundled 恒答 AU/US/ZZ）**；**arm 真成功链**（核心站主级 `http_request_host_is_external` 滤器过代理网——探针安装、插件永不装）：5.29s 真 4.5MB 下载→gz 解包→真落 uploads/gr-geoip（3,215,925B+11,874,885B 与随包逐字节同大）→override 生效→**PENDING 落+审计 refresh（added result refreshed+计数、removed source bundled）**；坑位实录：**沙盒 DNS 映射使一切外域 wp_safe_remote_* 结构性不可达**（198.18/15 段被核心有意拒绝——非插件非核心缺陷；直 curl 可通因走映射代理；成功链经核心留的站主 opt-in 滤器过代理验证，生产主机无此环境问题）；清理：pending AS 重派任务全清（AS_PENDING_AFTER=0）+uploads/gr-geoip 删+引擎回 bundled（357,325 恒答）+PENDING/账本瞬态删+审计 3 行删（LEFT=0）+引擎侧 2 行 WP-CLI UA 删（SEC_LEFT=0）+debug.log 159 行增量**全为 phar 自身弃用噪声零插件行**裁回 2126（备份沿用 .i5 期） | `07` §5.7、`06`、铁律 1 |
| U17 | Funnels & Goals 页（v1.0 部分） | ✅ | **`Gr_Funnels_Page`**（admin/，SLUG greenpng-funnels，**Marketing 段收尾位**（URL Builder 之后、Integrations 之前））：v1.0 = A/B 报表面——**纯只读零写臂**（定义不经此页创建：无表单/无 nonce/无 POST 处理器，Bot Signals 同款纪律）；每实验块=运行态词（running/paused）+ 变体行（control 首位）+ **分流预览**（8 个固定样本访客 id 的纯一致性哈希答案逐行 + 按变体计数行——**走引擎新纯缝 `Gr_Ab_Engine::pick_variant()`**：assign() 拆为 URL 强制门 + pick_variant() 纯哈希，预览不得被管理员自身浏览 URL 的 `?gr_variant=` 强制参数污染——强制参数属于浏览者不属于被预览的访客；paused 实验预览退位为 "Assignment is paused" 行+历史计数照读）；**显著性结果**（每变体 impressions/conversions/CVR 表 + control-vs-每变体对表 z/state/winner + **三态词**：insufficient（每臂 ≥30 提示）/inconclusive/significant+winner+95%——全部直读 `Gr_Ab_Significance::calculate()` 引擎原答案）；漏斗/步骤流失/目标 = v1.1 路线注记单行（无占位标签页无控件）；空态诚实（"No experiments defined yet" + 定义来源说明） | 实测 2026-09-12：phpunit **587 tests 3624 assertions**（FunnelsPageTest 11——空态诚实（无表反证）、running+insufficient 渲染、**significant**（手工 z=7.0711 逐位断言+winner+CVR 10.00%/30.00%）、**inconclusive**（满臂平坦）、**三态同页三实验**（closure 按查询分答 ab_counts——'实验名' 字面量派发）、**预览行逐 id 与引擎纯缝平价**（含 tally 精确串）、**预览免疫 URL 强制参数**（$_GET 强制下预览仍纯哈希）、paused（预览缺席反证+历史照读）、只读契约（form/nonce/action/nav-tab 四反证+v1.1 注记）、**菜单序**（URL Builder < Funnels < Analytics 位置断言+父/cap/callback）、页面源零外呼（四反证）；坑位实录：**内联 sprintf 单元格经空白紧凑化后带空格**（`<td> 10.00% </td>`——原始 HTML 断言与紧凑断言不可混用）；**引擎拆缝不动行为**（assign=强制门+pick_variant，C14 测试原样通过） | :8091 实测（eval-file 三探针+HTTP）——**真种子真渲染**：3 实验真定义（hero 双变体/beta 双变体/paused-demo 停用）+ **143 条真 ab 事件经真 `gr_ab_record` 落库**（hero 60+3 vs 60+18 → 真聚合）；渲染 22 针全真（三实验态词/paused 行/变体行/预览头+行/tally/计数头/insufficient 文案/Significant/z `<td>3.604</td>`（与引擎 CALC_Z=3.6037 平价）/state/winner/CVR 5.00%/30.00%/18 转化/v1.1 注记/**form 反证**）；**真菜单序**（真 WP admin_menu 后 $submenu 实序：greenpng-url=5 < greenpng-funnels=6 < greenpng-analytics=7，MENU_ORDER_OK=1）；**匿名 HTTP 302 登录**；清理：143 行 ab 事件外科 DELETE（AB_LEFT=0/ANY_AB_LEFT=0）+ 实验 option 删（absent）+ 空态复原双断言；debug.log 增量 0 | `06`、`03` §5 |

## 6. Phase 5 — 出网支柱与隐私合规

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| I1 | Http_Client | ✅ | `GreenPNG\Core\Gr_Http_Client`（core/，唯一出网门）：`get(service,url,args)`/`post(service,url,payload)`（post 自动 JSON 体+Content-Type）；**每服务独立配置表**（meta_capi/ga4_mp/dbip_update——timeout 一律钳 5、每服务单词表外键恒拒派发）；**configured 门**（meta=开关+pixel+token 双凭据全在场；ga4 同构；dbip 无凭据恒 true——唯一门是站长的显式点击在调用方）；**梯级退避**（失败 1/2/3 → 30s/2m/15m 写入 error data retry_after——调用方经 `Gr_Queue::enqueue(hook,args,delay)` 重派（注释即此契约）；**第 4 败=放弃**：清账 + 一行审计 `http_give_up`（diff 只携计数永不携凭据/端点）；**连续 3 败熔断 300s**（open 期内 ERR_BREAKER_OPEN 快失败零出网不计新败）；**抢跑门**（next_at 未到 ERR_BACKOFF_WAIT 快失败）；**429 读 Retry-After**（正值替代梯级、≤0 回落梯级、>3600 封顶防钉死）；**成功清账**（熔断/退避全消）；状态存 **transient**（非 autoload option，铁律 3）`gr_http_{service}` 单态元组 {fails,open_until,next_at}；`service_state()` 报告姿势；凭据 option 常量上移 **Gr_Secrets**（META_PIXEL/GA4_ID 等四键——Gr_Analytics_Page 改别名引用，写读双方零漂移）；审计走 Gr_Audit_Repository（Storage 层） | 实测 2026-09-12：phpunit **561 tests 3445 assertions**（HttpClientTest 13——未配置零外呼（调用日志空断言）、半对仍拒（开关开+单凭据=不配置）、成功形（code/body+**timeout=5 载荷断言**+JSON 体/头在场+**残留账清**）、GET 无体+钳时、**梯级电池**（三次真出网 30/120/900 逐级 + 第 3 败熔断 open_until>now——梯级间 next_at 归零模拟队列延迟重派）、第 4 败放弃（账清+**审计一行** action/object_type/object_id/user_id=0+diff 含 consecutive_failures + **EAAG 凭据反证**）、熔断快失败（零外呼+fails 不增）、抢跑门、429 读头（next_at=now+120 窗口断言）+无头回落 30、**线错计入**（WP_Error 传输失败=fails 1）、成功重置（2 败→200→新败回 30）、**全站无绕过机器断言**（plugin/ 全树 token 剥注释扫描：直呼族 wp_remote_get/post/request/head( 零出现 + wp_safe_remote_ 仅在客户端单文件））；坑位实录：**针文撞合法读者**（'wp_remote_' 裸串误伤 wp_remote_retrieve_* 读取器——禁单要列具体直呼函数；docblock 提及禁令的散文也要剥（token_get_all 去 T_COMMENT/T_DOC_COMMENT）；**梯级测试的背靠背调用会被自家 next_at 门扣住**（真实语义是队列延迟重派——测试模拟时序而非删门）；WP_Error 桩无 is_wp_error（REST 桩有类无谓词——补函数桩）；phpcbf 对齐 9 处 | :8091 实测（eval-file 真栈零外呼面）——**未配置门真链**（真 Gr_Settings+Gr_Secrets 空凭据路径→gr_not_configured；META_STATE configured=false 全零）；**真线臂**（wp_safe_remote_get 经核心 HTTP API 真返 WP_Error——WP 7.1 核心对非本站端口环回 URL 的 SSRF 校验拒绝（"A valid URL was not provided."）——客户端如实记账 fails=1/retry_after=30/next_at 落真 transient）；**抢跑门真验**（二呼即 ERR_BACKOFF_WAIT=30）；DBIP_STATE fails=1 真账；清理：双 transient 删净（跨进程 get_transient false）+审计 0 行+**AS gr_ 任务 0**（客户端自身从不入队）+debug.log 恒 2126 零增量；坑位实录：**核心 SSRF 校验拦本地端口**（8091/9999/8080 环回全拒——live 真拒连不可达=核心行为；线级梯级以桩线验证，live 验门与记账即足） | `07` §2、铁律 3 |
| I2 | Meta CAPI | ✅ | **`Gr_Meta_Capi`**（core/）：`GR_META_API_VERSION`（greenpng.php 主入口 v23.0 默认，**`gr_meta_api_version` filter 是站主免更新升级路**——参考项目硬编码 v19.0 随版本停支持的教训）；**生产者 `on_payment_complete`**（woocommerce_payment_complete@11——在适配器 visitor 绑定(10)之后读绑定值；**挂接前过 `Gr_Woocommerce_Adapter::is_available()` 门**：目标缺席零 woocommerce_* 钩子，保适配器契约；try/Throwable→`gr_adapter_error` 通道——结账永不因第三方排队失败而断）；**三门全在捕获时（买家请求内）评估**（队列作业在之后的请求跑，彼时 consent/会话态已不适用）：营销同意门 `Gr_Consent::allows('marketing')` → 配置门 `Gr_Http_Client::is_configured()`（新增公缝包私有 configured()——捕获侧不制造工作，门词汇单源）→ **流量质量门 `Gr_Session_Repository::visitor_bot_verdict()`**（新增：访客最近会话 is_bot；无会话=无证据不拒）——**不把机器人喂给广告算法的产品差异化落点**；`build_purchase_payload()` **纯本地可单测**（无订单对象无 I/O）：Purchase/event_time/**确定性 event_id（`gr_generate_event_id('capi','order|'.id)` 重放收敛）**/action_source website/**`user_data.em/ph` 走新 `Gr_Secrets::hash_pii_sha256()`**（提供方侧摘要=sha256(规范化值) **无本地类型前缀**——内部 `gr_hash_pii()` 的 `email|` 前缀让 Meta 无法与浏览器侧哈希配对，两语义分立各有其用）/custom_data value 2 位小数+currency 大写（空件省键不写空键）；**队列出队即哈希——明文 PII 永不入队列 args**；**消费者 `send()`**（SEND_HOOK 队列作业）：形状校验（构建器不会产出的体静默丢弃）→ 发送时重读凭据（捕获与发送之间站主可能关掉——**门在请求路径再查一次**）→ 版本经 filter → `https://graph.facebook.com/{v}/{pixel}/events` 经 `Gr_Http_Client::post()`（唯一出网门，token 在请求体）→ **重试契约**：is_wp_error 且 error-data retry_after>0 → `Gr_Queue::enqueue(delay)` 重派（线败梯级 30s/429 Retry-After/过早重试剩余秒）；retry_after=0（未配置/熔断/放弃族）→ 停（放弃审计已由客户端记）；成功零逐事件审计噪声 | 实测 2026-09-13：phpunit **604 tests 3690 assertions**（MetaCapiTest 16——**纯构建**（em==sha256('buyer@example.com') 无前缀逐位、ph、value '12.35' 舍入、USD 大写、明文反证 var_export）/空件省键（无 user_data/custom_data/event_source_url 键反证）/**未配置零工作**（cron+http_calls 双零）/**无同意零队列**/**bot 访客零队列**（http_calls 双零）/**干净订单单作业**（em 平价+129.99+USD+确定性 event_id 与 gr_generate_event_id 平价+**队列 args 明文 email 反证**）/未知订单号+非数值静默/**重放收敛同一 event_id**（两作业同 id）/订单爆炸→gr_adapter_error 记录不冒泡（结账不断）/发送成功=1 次调用+零重派+账本清/**线败梯级重派**（payload 原样回队+timestamp≥now+25）/**放弃停派**（预置 fails=3→第 4 败→零重派+http_give_up 审计恰 1 行）/**发送时未配置零触碰**/畸形体静默/**版本 filter 转向端点**（/v24.0/ 断言）/register 缺席臂（SEND_HOOK 在+零 woocommerce_ 前缀）；+WooCommerceAdapterTest 1（present 臂：Gr_Plugin::run 后 woocommerce_payment_complete@11 callback=Gr_Meta_Capi::on_payment_complete）；坑位实录：**`GreenPNG\Core\X::class` 在 Tests\Unit 命名空间内被相对解析**（成 `GreenPNG\Tests\Unit\GreenPNG\Core\X`——use 导入短名才是对的）；**适配器实例回调注册同名钩子**（callback[0] 是对象——字符串强转抛错，is_string 门）；**进程全局 eval 标记类破坏他测试的缺席前置**（标记类一次 eval 进程终身有效——present 臂测试并入拥有顺序契约的适配器测试文件，自家测试只测缺席臂） | :8091 实测（eval-file 三探针，真 Woo 站真栈）——**真注册**（has_filter woocommerce_payment_complete→11）；**真订单臂**（wc 真订单 15：email 88.50 USD+visitor 绑定 meta）；**臂 1 未配置**：AS 零作业（服务制造零工作）；**臂 2 全门通过**（真凭据 AES 落库+consent 回落开+真干净会话行）：AS 恰 1 作业、Purchase、em==sha256('livebuyer@example.com') 平价、value '88.50'、event_id 与生成器平价、**队列 args 明文 email 反证**；**臂 3 bot 访客**（真 is_bot 会话行）：作业数保持 1（拒增量）；**臂 4 真线败**（直发 → 沙盒把 graph.facebook.com 映入 198.18/15 核心拒发）：**真梯级**——账本 fails=1、next_at=+30s、重派入队（AS 2 条）；清理：逐任务按 args 反调度（**as_unschedule_all_actions 空参只匹配空参任务——带参作业要逐个 as_unschedule_action(action->get_args())**）AS_LEFT=0、账本瞬态删（fails=0）、真订单删、2 会话行删、凭据删（configured=false 回落）、开关复原、审计 0；debug.log 5 行增量全为探针/环境噪声（WP 自动更新 3+探针 prepare 无占位符 1+AS 表名误探 1）零插件行裁回 2126 | `07` §5.1 |
| I3 | GA4 MP | ✅ | **`Gr_Ga4_Mp`**（core/，镜像 Meta 转发器架构）：**`_ga` cookie 门**——`client_id_from_cookie()` 结构化解析经典 `GA1.x.<digits>.<digits>` 形（≥4 段 + digits.digits 正则存活），**无 cookie/畸形 cookie → 不回传**（自造 cid 会向属性缝垃圾会话，档案方案的错误——生产者与消费者 send() 双侧同门）；捕获侧三门同 Meta（营销同意→配置→**访客最近会话 bot 判定**）全在买家请求内评估；payment 钩子 @12（Meta 11 之后，同读适配器 @10 的 visitor 绑定；**挂接前过 is_available 门**保"目标缺席零 woocommerce_* 钩子"契约）；`build_purchase_payload()` 纯本地：**GA4 词汇零 PII**（client_id+params 而已——无 em 无 email，GA4 政策本就禁 PII）、`transaction_id`=订单号（**GA4 按 transaction_id 去重 purchase——重放收敛**）、value 2 位舍入、currency 大写（空省键）；消费者 send()：发送时重读凭据（站主可能在捕获与发送之间关闸）→ `mp/collect?measurement_id&api_secret`（查询参数是端点自身契约，门永不持久化 URL）经 `Gr_Http_Client::post()` → retry_after>0 重派否则停；**debug 连通性自检**：`debug_check()`——站长显式点击触发（U15 页新增 ACTION_DEBUG_GA4 按钮 + admin_init 双门 nonce+cap），**debug 端点只验证不摄入**（哨兵 cid '555.555' 永不污染真会话——注释写明为什么安全）、结果词表 ok（2xx+空 validationMessages）/validation（按条数记，不搬原文）/not_configured（零触碰网络）/unreachable（线败/熔断/过早门/2xx 但无验证形状体——**"没有验证形状就不是本插件愿意作的健康声明"**）；页面 arm：服务类持有出网调用（页面自身仍零网络调用）、审计行 ga4_debug_check 只带结果词（无端点无凭据）、PRG gr_debug 通知（成功绿/其余黄+"this click only, never on a schedule"文案） | 实测 2026-09-13：phpunit **629 tests 3778 assertions**（Ga4MpTest 21——纯构建（client_id/transaction_id '15'/130.0 舍入/USD/**em 与 user_data 双反证+域名反证**）/空件省键/**cookie 解析正形两案**（GA1.1/GA1.2）/**cookie 拒垃圾四案**（garbage/字母段/3 段/缺席）/**无 cookie 零队列**（垃圾会话防御，http_calls 双零）/未配置零工作/无同意零队列/**bot 零队列**/**干净+cookie 单作业**（cid/transaction_id/value/currency 逐位）/**重放收敛同一 transaction_id**（GA4 去重语义）/订单爆炸→gr_adapter_error 'ga4_mp' 不冒泡/发送成功 1 调用零重派账本清/**线败重派 +25s**/**预置 fails=3 放弃停派+审计恰 1 行**/**send 双门**（无凭据零触碰+**cid 缺席零触碰**——发送侧垃圾防御）/debug not_configured 零网络/debug ok（空 validationMessages+debug 端点 URL 断言）/debug validation（按条数 2）/**debug unreachable 双案**（线败+外星体 2xx 无验证形状）/register 缺席臂；AnalyticsPageTest +4（按钮恰一块渲染（Test connection 计数 1+披露文案）/点击链经门（1 次调用+PRG ok+审计 diff 词+**端点反证**）/未配置拒（零网络+PRG not_configured+审计词）/坏 nonce 全拒（零审计零跳转）+通知渲染（绿类+词）；坑位实录：**断言文案与真文案要逐字对**（'never on its own' vs 实际 'nothing ever on its own'）；**审计断言先读列名**（diff_json 非臆造 detail）；**unset($_COOKIE) 抹掉超全局绑定殃及后续测试**（IdentityTest 直读 $_COOKIE 报 undefined global——清空数组才是"键缺席"语义）；探针 add_filter 回调内 echo 会触发 CLI headers-already-sent 警告（无害但进日志） | :8091 实测（eval-file 双跑全栈）——**真注册**（has_filter→12）；**真订单 16/17**（两跑各一单）；**臂 1 无 cookie**（WP-CLI 天然无 _ga）：AS 零作业——**垃圾会话防御真栈生效**；**臂 2 带 cookie**：AS 恰 1 作业（client_id '123456789.1600000000' 逐位、transaction_id '16'、value 42.5、USD、**@ 域名反证零 PII**）；**臂 3 bot 访客**（真 is_bot 会话行）：作业保持 1；**臂 4 真线败**（send → 沙盒外域映射被核心拒发）：账本 fails=1+重派入队（AS 2）；**臂 5 debug 自检真词**：unreachable（**第二线上门撞过早重试门 ERR_BACKOFF_WAIT 快败不计**——fails 仍 1，真栈验证门的不计语义）；**臂 6 真点击链**（user1+真 nonce）：PRG gr_debug=ga4&gr_result=unreachable+审计行（双跑共 2 行）；**臂 7 通知渲染**（unreachable 词+Test connection 恰 1）；清理：逐 args 反调度 AS_LEFT=0、账本删（fails=0）、**双订单按 _gr_visitor_id meta 检索删（非按 id 硬编码）**、2 会话行删、凭据删+开关复原（configured=false）、审计 2 行删；debug.log 110 行增量（phar 弃用 106+CLI header 警告 4）**插件零行**裁回 2126 | `07` §5.2 |
| I4 | event_id 双端去重 | ✅ | **单一派生源 `Gr_Meta_Capi::order_event_id(订单号)`**（`gr_generate_event_id('capi','order|'.id)` 确定性派生——重放的支付回调收敛同一 ID；服务端 payload 经此构建（I2 内联调用重构为共用缝））；**感谢页桥接 `print_event_id`**（`woocommerce_thankyou`@10 与 payment 钩子同门挂接——目标缺席零 woocommerce_* 钩子；try/Throwable→gr_adapter_error——感谢页永不因桥断渲染）：`wp_print_inline_script_tag`（WP 6.0+ 核心原生发射器）打印 `window.GreenPNGPurchaseEventId='<同 ID>'`（wp_json_encode JS 上下文转义；id 是订单号确定性哈希——公开订单域字符串，暴露本身零追踪）；**门**：服务端未配置→不打印（无服务器事件可配对的 ID 帮不了任何人）+ 非真订单号静默 + 派生纯函数不读订单内部（爆炸订单店也不受影响）；**站长接线路径文档化**（docs/07 §5.1）：`fbq('track','Purchase',{eventID: window.GreenPNGPurchaseEventId})`——Meta 48h 窗口内把浏览器 Pixel 事件与服务端 CAPI 事件折叠为一次转化；**不承诺不计算 EMQ 分数**（明文）；readme External services 两行修准（Meta：SHA-256 哈希 billing email、**无原始标识符无 IP 无 UA**（原文错写发送 IP/UA）；GA4：_ga cookie 无则不发、付费订单+营销同意后） | 实测 2026-09-13：phpunit **633 tests 3792 assertions**（MetaCapiTest +4——**单一派生源**（order_event_id==gr_generate_event_id 平价+确定性+**入队 payload 恰含此 ID**——两端按构造收敛）、**配置时打印共享 ID**（全局+标签+json 值）、**未配置/无订单/畸形号三案全静默**、**爆炸订单店不炸感谢页**（派生不读订单内部——id 照印零错误）；+WooCommerceAdapterTest present 臂补 thankyou 钩子断言（print_event_id 回调）；坑位实录：**EscapeOutput 嗅探不认 wp_json_encode 也不吃多行 ignoreStart/End**（inline script 输出改走 `wp_print_inline_script_tag` 核心发射器——嗅探认核心路径且是真房规）；**新核心函数先查桩**（stub 无此函数即 fatal——tests/stubs 补形状镜像桩，断言写成格式无关（gr-meta-event-id/全局名/json 值）） | :8091 实测（eval-file 双探针真栈）——**注册**（has_filter woocommerce_thankyou→10 与 payment 同门）；**真订单 18 真捕获**：AS 恰 1 作业、服务端 event_id=`capi_ba2b03a5ce42c78d00435af0acd1c3d3`；**双端同 ID 实测**：桥接派生同一字符串（SAME_ID_BOTH_ENDS=true）+打印体携带它（全局名+标签+无 @ 域名零 PII）；**未配置臂**：凭据删后打印静默（UNCONFIGURED_SILENT=true）；清理：AS 按 args 反调度（0）+订单按 meta 检索删（1）+会话行删+凭据删+开关复原（configured=false）；debug.log 增量 0 | `07` §5.1、PEER-02 |
| I5 | GeoIP 开箱 | ✅ | **数据随包**：DB-IP Country Lite 2026-09 重打包为定宽二进制 `assets/data/gr-geoip-v4.bin`（9B/记录：start/end N + 1B 国家索引，357,325 条 3.2MB）+ `gr-geoip-v6.bin`（33B/记录，359,845 条 11.9MB）+ `gr-geoip.json`（国家表/计数/出处/日期）；归属双披露（NOTICE 第 2 节：DB-IP、CC BY 4.0、数据日期、重打包说明、uploads 刷新不触插件目录；readme External services + Attribution 段已有）；**`Gr_Geoip_Packer`**（core/，流式两遍：遍 1 国家表+形状审计（>256 国拒——1B 索引上限）、遍 2 相邻同国段流式合并（单行 lookahead）→ tmp 文件 rename 原子晋升（半写刷新永不遮好数据）；工具与 U16 更新臂共用此类——随包格式与刷新格式永不漂移；json_encode 非 wp_json_encode——工具上下文无 WP）；**`Gr_Geoip`**（core/，fseek 二分双族（v4 N 序 unsigned sprintf 归一、v6 strcmp 字节序即地址序）；uploads `gr-geoip` 子目录覆盖优先于随包（wp.org 只读插件目录）；L1 memo 上限 512 溢出即清（共享 FPM worker 不无限增长）；`state()` 报告源/日期/计数；`reset_for_tests` 缝；**unpack 'C' 非 'c'**——索引 >127 读负即错）；门面 `gr_geoip_country()`（纯本地、'' 未知）；**listener 接线：country_code 随同意门**（有同意才入 landing——无同意技术滑仍零落地属性，Dashboard Unknown 桶保全，docs/07 §1 营销轨纪律）；`tools/pack-geoip.php` 构建工具（tools/ 不入包）；更新按钮归 U16 页任务 | 实测 2026-09-12：phpunit **548 tests 3028 assertions**（GeoipTest 8——packer：合并（10.0.0.0+10.0.1.0 同国相邻段并 1 记录、v6 `::fffe`/`::ffff` 缝合并、坏行 skip 计数、**记录宽 9/33 = packer/engine 契约断言**、meta 出处 CC BY 4.0+2026-09）、拒收臂（不可读零写零文件、无可用行 tmp 清除零晋升）、**覆盖优先**（`<uploads>/gr-geoip` 胜随包——fixture 答私有段 DE/AQ 而真库不能证覆盖生效；边界电池：AQ 微段双沿含/过界空/段前空/段外空、v6 合并段含缝+过界一址、非地址 ''）、随包真数据（state source=bundled、build_date `^\d{4}-\d{2}$`、双族 >100k、1.0.0.1→AU/8.8.8.8→US/192.0.2.1→ZZ、memo 复查一致）、门面 parity、**listener 双臂**（'AU' 仅随同意入 INSERT——无同意 SQL 零 'AU'）、随包三文件+NOTICE 同船（license 断言）、**零外呼源级断言**（双文件无 wp_remote_/curl_/fsockopen））；坑位实录：**fixture 相邻性要数学验证**（首版 v6 fixture `::ffff` 尾与 `:1::` 首差一位非相邻——合并计数当场揭穿；相邻=前段 end+1==后段 start，inc16 说了算）；**fopen 不可读路径泄 PHP warning**（PHPUnit warning→error：is_readable 守卫先行，与引擎同款）；Gr_Identity 构造需 Gr_Settings（照既有 helper 形态）；覆盖测试须打进 `<uploads>/gr-geoip` 子目录（引擎解析的是子目录非 basedir 本身） | :8091 实测（eval-file 三探针）——**引擎真栈**：state=bundled/2026-09/357,325/359,845/251；查找 1.0.0.1→AU、8.8.8.8→US、192.0.2.1→ZZ、v6 2607:f8b0:…→US、127.0.0.1→ZZ、非 IP→''；门面/memo 一致；**同意链**（本机无 wp_has_consent→插件开关代理，事后恢复跨进程证 0）：无同意臂 country_code=''、同意臂 **'AU'**；`aggregate_date(今日)` → **sessions_by_country 真行（''=5 混合 + AU=1）**；Dashboard 管理员渲染 heading+`<td>AU</td>`+`<td>Unknown</td>` 全在场——**"国家分布非空"验收以真库真码达标**；坑位实录：wp-cli phar CLI 下 setcookie 恒告警 headers already sent（**CLI 探针伪影非插件缺陷**——真前台请求头未发）；探针 unset($_SERVER['REQUEST_URI']) 触发核内 cron.php warning（探针自留痕）；清理：种子 2 会话删（存量 12 留）、今日 11 聚合行删后自余会话重聚合（10 行、国家 ''=4、**探针 AU 行消失**=派生数据干净态）、引擎侧 1 行（WP-CLI UA 检出）删 SEC=0、**AS gr_ 任务 0**（探针未入队）、debug.log 裁回 2126（27 行全为探针伪影；备份 .i5） | `07` §5.7、ADR-0007 |
| V1 | 隐私双轨落地 | ✅ | 基建自 Phase 2/3 已在（C5 身份派生含 `marketing_ip_anonymize` 默认 1——回退 id 哈希输入先过 `Gr_Privacy::anonymize_ip()`；W6 安全日志仓储含 `security_log_anonymize` 默认 0——`INET6_ATON` 二进制存储前 PHP 侧截断；W14 四管理页 `gr_mask_ip` 展示掩码与存储形态解耦）；同意门 `Gr_Consent` 六检查点（attribution listener/identity cookie 签发/form adapter/Woo 绑定/Meta CAPI/GA4 MP），DNT+Sec-GPC 浏览器信号**压过一切存量同意**（访客自己的声明最大）；本任务=两轨数据流真栈实测+设置变更即时生效验证（`Gr_Settings::set()` 清静态 memo——写入即对同进程与全新实例可见，零 TTL 零缓存过期），零新代码 | 实测（既有覆盖确认）：phpunit 649 tests 3876 assertions 全绿不动（IdentityTest 4 处 anonymize/mask 断言、SecurityLogTest 2 处、consent fallback 在 IdentityTest/SettingsPageTest 双文件覆盖） | :8091 实测（eval-file + 真实 curl 三臂）——**设置即时性**：`set(mk_anon,0)` 后同实例 get=0、全新实例 get=0（memo 即清）；**营销轨身份派生**：同 REMOTE_ADDR/UA/日，匿名开→关两次派生 visitor_id **不同**（匿名化 IP 真进哈希输入）、`anonymize_ip` 原语 v4 `203.0.113.77→203.0.113.0`、v6 `2001:db8:1234:5678:abcd::1→2001:db8:1234::`（/24、/48 逐位）；**安全轨双态存储**：默认 `security_log_anonymize=0` → 存 `203.0.113.77` **完整**，同进程翻 1 → 下一写存 `203.0.113.0`（截断在存前、翻即生效）；**展示掩码**：`gr_mask_ip('203.0.113.77')='203.0.113.*`'（四页所见与存储无关恒掩码）；**真 HTTP 同意门三臂**（curl :8091 首页 `?utm_source=v1probe`）：关（默认）→ **0 会话 0 触点**（营销轨整轨静默）；站主开关开 → **1 会话+1 触点**（channel=direct、utm 落行、pv=1）**且 Set-Cookie 双签发**（`gr_attr` 30 天 HttpOnly SameSite=Lax + `gr_session` 30 分）——同意门就在 cookie 签发处；开关开+`DNT:1` 头 → **行数零增量**（浏览器信号压过存量同意）；坑位实录：**核心 WP 7.1 无 `wp_has_consent`**（Consent API 从未并入核心——`function_exists` false → 站主开关代理路径即生产主流路径）；**`gr_security_logs.ip` 是 `INET6_ATON` 二进制列**（裸 SELECT 读出乱码——读取一律 `INET6_NTOA(ip) AS ip`，写侧同列名）；探针 Gr_Database 命名空间再犯（Core 非 Storage——已入 NOTES）；清理：探针行外科删（utm_source/rule_id 域）1+1+2 全净、三设置全复原默认（consent_fb=0/mk_anon=1/sec_anon=0）、debug.log +76 全 phar/探针噪声零插件行裁回 2126 | ADR-0007、ADR-0005、`03` §3、`10` |
| V2 | WP 隐私 API | ✅ | **`Gr_Privacy_Api`**（privacy/）：四导出器+四擦除器（sessions/touchpoints/conversions/contacts）注册进 `wp_privacy_personal_data_exporters/erasers` 双过滤器；**映射链 `visitor_ids_for_email()`**（billing_email→`wc_get_orders`→订单 `_gr_visitor_id` meta→unique ids——email→订单→绑定是两侧本就持有的公共数据的 join；无 Woo=诚实空数组；Throwable→`gr_adapter_error` 隐私工具永不炸）；**导出分页语义=第 N 页导出第 N 个访客的全部行**（core 工具逐页推进天然适配）；contacts 导出按 `Gr_Secrets::hash_pii(email,'email')` 内部哈希查（**v1.0 无 CRM 写入→诚实空**，CRM 落地日同一代码即生效；hash 与加密信封永不进导出项）；**擦除**：三仓储各 `delete_for_visitor()` 行删 + **订单绑定剥除归最后注册的擦除器 contact（finalizer）**——绑定是 email→访客链的独木桥，若在 conversions 内剥除，core 工具重试/乱序时后跑的家族将找不到访客（live 对抗序实测揭穿后重构）；contact 擦除器**无 CRM 行也照剥绑定**（绑定是真个人数据，与 CRM 存在与否无关）；`policy_content()` 三段建议文（营销轨匿名化+同意可撤 / 安全轨合法利益+短保留+展示脱敏+可关 / 出网仅站主配置+导出擦除覆盖面）；**安全轨有意缺席**（合法利益、非 email 键控、短保留——文档化依据而非遗漏）；三仓储 +`rows_for_visitor()`（导出读**无窗口**——静默停在 30 天的导出会少报人的数据）；readme 虚假声明行修正（原文"every gr_ table"全表覆盖为假——改为 marketing 表+订单绑定、安全轨注明有意在外） | 实测 2026-09-13：phpunit **649 tests 3876 assertions**（WpPrivacyApiTest 16——映射链（双访客序+无人空+空 email）/双注册表（既有 exporter 保留+四族加入；eraser 四条 friendly_name）/分页（p1 真行字段、p2 次访客、p3 done；contact >1 直答 done）/诚实空（无订单三导出器空且 done）/触点转化列值（gclid/landing/source/amount/currency）/contacts 空与真行（Ann/champions+**hash 与信封不进导出键反证**）/**erase conversions 留绑定至 finalizer**/**对抗序回归**（conversions 先行→sessions 仍删得动——live 缺陷的回归锁）/无订单零删/**visitor 域 DELETE SQL 断言**/**contact 擦除=行+绑定双计**（2 项+meta 空）/无 CRM 行也剥绑定/policy 三词在场/插件级 filter 挂载（Gr_Plugin::run 后 exporter/eraser 钩子在））；坑位实录：**`Gr_Database` 在 `GreenPNG\Core` 非 Storage 命名空间**（DatabaseTest 的 use 是唯一可信来源——臆写 import 即 fatal）；**文件名排序控制 eval 标记类顺序**（WpPrivacyApiTest 需 present-target 姿态 eval 标记类=进程全局；命名使其排在 WooCommerceAdapterTest 之后，缺席臂契约保住——文件头注释写明这是有意的） | :8091 实测（eval-file 双轮真栈）——**轮 1 揭真缺陷**：对抗序（conversions 先跑）删绑定后 sessions/touchpoints removed=0（访客不可寻）→ 当场重构 finalizer 语义；**轮 2 全通**：对抗序（conversions→sessions→touchpoints）removed=1/2/1、**绑定完好**（新鲜订单实例读回 'probe-priv-a'）、post-erase 四域计数全零；**finalizer**：contact removed=2（双绑定）、双订单 meta 剥净、map=0（链断后诚实空）；**导出真数据**：sessions channel=search/utm=google、touchpoints click_id/landing/campaign、conversions woocommerce/129.99/USD、contact 诚实空；**分页真栈**（p1 false/p2 行+false/p3 done）；**映射链**（双访客逐位 found==expected、无人=0）；**真过滤器管道**（apply_filters 真核心：exporters 含四族与 core customer/order/download 并列、erasers 11 条中四条 `greenpng sessions/touchpoints/conversions/CRM contact` 具名）；**policy**：admin_init 钩子含 policy_content 回调真注册；坑位实录：**核心 `wp_add_privacy_policy_content` 数值索引存储+is_admin() 拒非 admin 调用**（CLI 反射查 'greenpng' 键=探针方法错误非插件缺陷——doing_it_wrong 是探针伪影）；**WC_Order 跨实例 meta 缓存**（擦除在 wc_get_orders 新鲜实例上 save，探针持有的旧实例 get_meta 不回读——验证须取新鲜实例）；清理：双探针订单 22/23+上轮遗留 19 删净、四域行删全零、debug.log +2 探针行（doing_it_wrong+ReflectionProperty 弃用）裁回 2126 | ADR-0005 |
| V3 | 瘦身执行 | ✅ | `Gr_Retention`（storage/）自 Phase 2 已在：**双轨**——年龄轨（`retention_days` 每表，`DELETE ... WHERE {date_col} < %s ORDER BY id ASC LIMIT %d` 最旧先行）+ 天花板轨（`retention_rows` 每表，边界读一次后批下走，保最新 N）；**批纪律**：BATCH=2000 行/语句、批间 `usleep(100000)`（100ms）、TIME_BUDGET=10s 墙（超时余量留明日——修剪收敛不事务）；日骑手挂 `Gr_Queue::DAILY_HOOK`@10（聚合器(5)之后——汇总先落、修剪永不吃报告未吸收的数据）；OPTIMIZE 手动-only（InnoDB 重建锁表，计划性锁=自伤停机）；门面 `gr_prune_table()`（gr-functions.php）；静态表（rules/funnels/contacts/tags/conversions）永不入骑手 | 实测（既有覆盖确认）：phpunit 649 tests 3876 assertions 全绿不动（DataRetentionTest 17——年龄轨精确删/0=全保/标识符畸形拒跑/短批即停/多批语句计数/每表正确日期列/天花板边界含 max+1 精确删 1/欠界零删/预算 0=墙已过零删/OPTIMIZE 与骑手分离机器断言） | :8091 实测（eval-file 真栈）——**骑手真注册**（has_action DAILY_HOOK→run_daily@10）；**年龄轨删至线**：5000 行 40 天龄 + 60 行新 → `gr_prune_table('events','created_at',30)` **恰删 5000**、超龄余 0、新鲜 60 全保；**批节拍计时证**：batch=1000 → 5 语句 4 睡眠，实测 elapsed=0.599s ≥0.4s（100ms 批间睡眠真跑）；**天花板轨**：保最新 50（113→50，边界含 63 精确删）；**run_daily 端到端**：200 行 35 天龄 → 真设置 `retention_days['events']=30` 消费之（余 0），八张真表 before/after 逐表对照 **zero moved**（无附带损伤）；清理 v3probe 域全删净 0；**如实披露**：天花板臂在含真实存量行的表上整表运行，附带删去 53 条先存事件行（dev 站瞬态数据、聚合快照在 daily_stats 完好、对功能零影响）——**探针设计错误非插件缺陷**（教训入 NOTES：天花板/整表操作要在空表或纯探针表上验）；debug.log 零插件行 | `03` §10、`05` §5 |
| V4 | readme 披露定稿 | ✅ | `== External services ==` 定稿为**仅真实现三项**：GA4 MP（含 Test connection 按钮披露——debug 端点只验证不摄入、哨兵 cid、按钮唯一触发）、Meta CAPI（SHA-256 哈希 billing email、无原始标识符无 IP 无 UA、桥接 `window.GreenPNGPurchaseEventId` 去重说明）、DB-IP 更新按钮（点击才出网、无访客数据）；**删除两项超前披露**（TikTok Events API=07 表 v1.3、Generic webhook=v1.1——代码零存在，与 I4 前的 Meta 行同病：披露先于实现=虚假声明）；新增 **v1.2 预告段**（AbuseIPDB+蜘蛛段订阅，明标 planned not present in 1.0、届时 opt-in 默认关）；FAQ 集成列表同步修真（GA4, Meta）+FCrDNS 一句；**Privacy 段新增两条**：探针双模块披露（安全模块=自动化结论only、无指纹串无 Canvas/Audio 无持久标识符、合法利益、一键关；行为模块=站长启用+访客同意双门——08 §3 探针三件套的 readme 环）+ FCrDNS 系统 DNS 披露（走服务器常规 DNS 基础设施、只记判定词与解析主机名、无第三方 API 无凭据、失败即放行——系统 DNS 无服务契约可链故归隐私段不归 External services）；**`policy_content()` 建议文补探针句**（08 §4 要求"含探针与安全 IP 双轨说明"——探针并入安全轨段：同合法利益、无指纹数据无持久标识符、设置可关）；十五表声明核对（schema DDL 实数 15 恰确） | 实测 2026-09-13：phpunit **649 tests 3878 assertions**（WpPrivacyApiTest policy 断言 +2 词：'client probe'+'no fingerprint data'）；静态核对：plugin/ 全树 TikTok/webhook 零 token 残留、schema 15 表与 readme 声明逐字符、readme 三项披露与 I2/I3/I4/U16 实测 payload 逐字段对（GA4 无 cookie 不发=I3 真栈臂、Meta 哈希域=I2 真栈臂、DB-IP 按钮唯一触发=U16） | 08 §3/§4 专项自查（readme 面）：§3 首次后台无外呼（I 系列未配置零外呼双证）✓、蜘蛛段订阅默认关（v1.2 未实现即无从默认开）✓、DB-IP 仅显式按钮 ✓、**gr-probe.js 双模块披露 readme 环就位**（三件套齐：readme 隐私段+设置页一键关+无标识符披露）✓；§4 exporters/erasers 实现（V2）✓、**policy content 含探针+安全 IP 双轨说明**（本任务补齐）✓、IP 双轨 readme 披露合法利益依据 ✓、无指纹营销画像（代码纪律 W 系列）✓、CRM 布尔+标签（v1.1 无 CRM 写入即无入画路径）✓、弃购邮件默认关（v1.1 未建即无默认开路径）✓ | `07` §1、`08` §3/§4 |
| V5 | 同意门控复核 | ✅ | **营销链路六检查点清单化**（全部 `Gr_Consent::allows('marketing')` 单原语——一个语义六处落点，无旁路）：①attribution listener 落地写（`enabled && allows` 双门）②identity cookie 签发（`issue()` 首行门——无同意永不 setcookie）③form adapter 转化绑定（`has_cookie_identity() && allows` 双门）④Woo 访客绑定（`'' === $visitor \|\| ! allows` 早退）⑤Meta CAPI 捕获（三门之首）⑥GA4 MP 捕获（同）；DNT+Sec-GPC 浏览器信号在原语内部压过一切存量同意（访客自己的声明最大）；**安全链路开关化与同意脱钩**：`probe_enabled`（探针脚本 enqueue+REST signal 端点双门）、`security_log_anonymize`（存储层截断）、拦截 per-rule 显式（规则表无 action 列——block 引擎是站主显式选择而非规则默认）；本任务=零新代码纯复核 | 实测（既有覆盖确认）：phpunit 649 tests 3878 assertions 全绿不动（FormAdaptersTest 双态/ProbeScriptTest 双态/IdentityTest consent/MetaCapiTest+Ga4MpTest 无同意零队列/WooCommerceAdapterTest 缺席臂） | :8091 实测（curl 三跑 + eval-file 双跑真栈逐点开/关）——**①落地写**：关→0 行、开→行+（V1/V5 双轮）；**②cookie 签发**：关（默认）→响应头 **0 个 gr_* Set-Cookie**、开→`gr_attr`（30 天 HttpOnly SameSite=Lax）+`gr_session`（30 分）双签发；**③Woo 绑定**（真公开入口 `capture_classic`）：关+无 cookie→订单 24 **零绑定**、开+curl 捕获的真签名 cookie→订单 26 绑定=`6ed39bd0…`（与 cookie 访客段逐位同）；**④form**：单测双态+原语在①②③⑤⑥五点真栈活证（同一 `allows()` 调用无第二实现）；**⑤⑥**：I2/I3 真栈无同意零作业臂（引用不复跑）；**DNT 压制**：V1 真栈臂；**安全开关**：`probe_enabled` 0→curl HTML **gr-probe 0 次**、复 1→3 次（即翻即生效）；`security_log_anonymize` 双态（V1）；规则表 0 行（默认仅记录、无 action 列可默认拦）；坑位实录：**WC_Order 实例缓存坑复犯**（V2 学过——验 meta 必取新鲜 `wc_get_order`，探针旧实例恒读空；首跑 binding=false 是探针伪影，重跑新鲜实例即真值）；**臆造列名触发 DB 错误行**（`rule_action` 不存在——真列是 rule_type/match_kind/is_active，先读 DDL 再写查询）；清理：v5probe 域行全删、订单 24/25/26 删、cookie 访客转化行双跑各清余 0、设置全复原默认、debug.log 裁回 2126 | ADR-0005/0007 |

## 7. Phase 6 — 测试收口与发布准备

| ID | 任务 | 状态 | 交付物 | 验收标准 | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| T1 | 单元测试集 | ✅ | 十项必测域全部在位（`11` §3）：CIDR（CidrMatcherTest）/ 归因 5 模型（AttributionModelsTest）/ 浪涌折叠（SecurityLogTest 原子 upsert 形状 + **真并发实弹**见实测列）/ 登录锁定（LoginProtectionTest）/ 提取器（SemanticExtractorTest 9 测：CF7/FluentForms 嵌套形状、深度帽、get_data 对象、首检出 email 胜出）/ 递归 diff（gr_audit_diff 递归 added/modified/removed 门面，AuditLogTest diff_json 断言）/ 参数解析（AttributionParamsTest）/ A-B 分流稳定性与 Z 检验（AbEngineTest crc32 分流 + AbSignificanceTest 三态 z）/ UA 引擎命中（ScannerUaTest 种子语料+Exclusions）/ 队列互斥（QueueTest testMutexBlocksReentryWhileHeldAndResumesAfterRelease） | 实测 2026-09-13：phpunit **649 tests 3878 assertions 全绿**；**浪涌并发真栈实弹**：10 个并行 PHP 进程同折键（ip+rule+hour）写真 MariaDB → **恰 1 行、hit_count=10、零 1062 重复键错误**（原子 upsert 在唯一键上串行化=并发保证的机制证明，不宣称压缩率百分比）；清理折键行删净、debug.log phar 噪声裁回 2126 | **如实跳过项**：`11` §7 核心域 ≥70% **行覆盖率**门槛——本机 FrankenPHP PHP 8.5 构建无 xdebug/pcov/phpdbg/pecl（扩展目录零覆盖驱动，实测核查），行覆盖数值**不可测**故不声称；以十域逐项在位+全绿+并发实弹为 T1 收口证据，覆盖率门槛移交 CI（wp-env 矩阵有驱动时测）| `11` §3 |
| T2 | 集成测试集（含常用插件矩阵） | ✅ | **采集端点七臂真栈**：无令牌 401（`gr_collect_token`）/错令牌 401/未知事件名 400（schema enum `rest_invalid_param`）/bot_score 999 越界 400（schema 0-100）/超大 body 413（9000B 过 8192 门）/好路径 200 `stored:true`+id/**`Cache-Control: no-cache, must-revalidate, max-age=0, no-store, private`**（handle 内 `nocache_headers()` 真响应头）/超频 429（预置限流瞬态至 60——**坑：curl 经 IPv6 回环，限流键是 md5('::1') 非 md5('127.0.0.1')，双键预置才命中**）；**WC 幂等双线真栈**：五回调（classic×2+store_api+payment×2）→ **恰 1 绑定+1 ATTRIBUTED 锁+1 转化行**（meta 写一次门 + `gr_conversions` UNIQUE 双线；坑：转化行在 `complete_payment→bind_order` 路径，`capture_classic` 只写 VISITOR_META——首版探针漏调支付回调误报 0 行）；**HPOS 开×关×classic/Blocks 四格**：posts 存储（order 29 classic+store_api ✓）/HPOS 自定义表（`feature_orders` 翻 yes 后 order 30/31 `wc_orders` 表在、posts=0——binding+attributed+转化行全落 ✓，四格各恰一行）；**表单三桥**：CF7（`wpcf7_mail_sent`+`wpcf7_submit` 双钩 wired）/WPForms（`wpforms_process_complete`+`wpforms_process` 双钩）/FluentForms（`fluentform/submission_inserted`+`fluentform_submission_inserted` 双钩——**坑：钩名以适配器代码为准，首猜 `entry_created` 错**）；**Elementor 共存**：激活态前台 200+gr-probe 在场+零 fatal 标记；**缓存共存**：WP Super Cache 激活态 collect 200+同款 no-cache 头；**dbDelta 幂等**：15 表 `Gr_Schema::install()` 连跑两次零差异零 DB 错 | 实测 2026-09-13：phpunit **649 tests 3878 assertions 全绿**（本任务零代码改动，四检查复核）；CF7/WPForms/Elementor/WPSC 四插件激活→验证→**逐个还原 inactive**（基线零残留）；probe 订单 28-31+转化行+事件+会话触点行全清、限流瞬态删、HPOS feature 复原 no、debug.log 398 行增量（探针 prepare 无占位符 1+Elementor 自身弃用+phar 噪声）**零插件行**裁回 2126 | **如实不做项**：卸载两模式 live 臂——dev 站插件目录是**指向仓库源树的 symlink**，`wp plugin uninstall` 会穿透删除仓库 plugin/ 源码（符号替换舞蹈有中途断链风险，且 delete 模式会抹掉 T7 依赖的 dev 状态）；两模式语义由 LifecycleTest 单测覆盖（守卫序先于任何加载、`gr_delete_data_on_uninstall` 默认 keep 显式 opt-in 才删）；live 卸载留待独立环境（CI wp-env）| `11` §3/§5、ADR-0007 |
| T3 | 性能基准 | ✅ | **`tests/benchmarks/front-request.php` 新建**（CLI-only 守卫 + try/finally 恒复原插件激活 + 探针行自清扫；tests/ 不入包）：①**HTTP P95 双相**——wp-cli runcommand 装前/装后各 5 预热+50 计时请求（每次新连接，同法双相）；②**三层 SQL 计数**——**`query` 过滤器计数器**（WP 7.1 wpdb 的 SAVEQUERIES 记录门是常量非属性——`$wpdb->save_queries=true` 静默无效，首版三臂全 0 即此因；每读写都过 `WPDB::query()` 故过滤器口径全版本稳定）；③**进程内执行中位**——真代码路径 handle()/rest_do_request 各 20 次取中位，**setcookie 告警静默 handler**（CLI 恒告警"headers already sent"且每次写 debug.log 盘 I/O——实测同工作量 13.978ms 自然 vs 1.046ms 静默，13 倍纯伪影；真 web 请求永不告警） | 实测 2026-09-13（:8091 / PHP 8.5.10 / MariaDB 12.3 / 无持久对象缓存 / 三轮）：**HTTP P95** 装前 161.0/161.6ms 装后 162.3/161.6/170.1ms——**三样本 delta -2.73/+0.62/+8.53ms = dev 服噪声带 ±9ms，插件成本在地板之下**（HTTP 口径只证"不超 ~9ms"，不做更细声称）；**进程内中位（插件自身成本的权威口径）**：稳态 **0.696ms** / 归因落地 **1.218ms**——≤5ms 预算余量 ~76%；**分层 SQL**：稳态 **1 ≤2** ✓（会话技术滑 1 + 规则 0——空规则表 L1 免查）/ 归因落地 **2 ≤4** ✓（会话 upsert+触点 INSERT）/ collect **5**（≤3 口径指**有**持久对象缓存主机；无缓存地板 5-6 条为 `09` §1.1 落地记录 + `13` §8 待议的既有张力——transient 兜底 3 条 + 事件 INSERT 1 + 会话 1，两超因均不在控制器逻辑）；运行时 DDL 0 / 同步外呼 0（T2 collect 七臂+全仓扫描既有证）；清理：探针行脚本自扫 + 会话/触点/事件域删、插件恒 active（finally 复核）、debug.log 893 行 benchmark 伪影（setcookie 告警含插件路径 + phar）裁回 2126 | 数字复现法：`php wp-cli.phar eval-file tests/benchmarks/front-request.php --path=<站>`（BENCH| 行即报告；env 行记 PHP 版本/URL/轮数） | `09` §1.1、`11` §6 |
| T4 | PHPStan | ✅ | **phpstan ^2.2 dev 依赖 + `phpstan.neon.dist`**（level 6，scanFiles 9 桩文件——新增 `constants-stubs.php`（GR_PLUGIN_DIR/URL 守卫式 define 分析侧恒定视图）与 `action-scheduler-stubs.php`（**仅分析、不入 phpunit bootstrap**——见披露列）、excludePaths assets）+ **`phpstan-baseline.neon`**（16 条防御性收窄，纪律写在 neon 尾注：只随桩变诚实而缩、永不增长）+ **`tools/phpstan-run.sh`**（主机档案 runner：PHPRC ini 注入 `phpstan.restarted=1` 消 TurboProcessRestarter 重启环；`--debug` 单进程避并行 worker argv 断裂；raw 格式 + `:[0-9]+:` 过滤 --debug 的文件清单 stdout 污染；退出码语义正确——零错误 0、有发现 1）；**85→0 全程真修 69**：stub 缺函数 8（plugin_dir_path/url/basename/load_plugin_textdomain 3 参/register_activation/deactivation_hook/dbDelta）+ 常量桩 8 + AS 桩 2；http-client docblock `|WP_Error`→`|\WP_Error` 4 条 class.notFound + 6 条 return.type 级联清零（**命名空间内 docblock 不带 `\` 的 WP_Error 解析成 GreenPNG\Core\WP_Error——方法体全是 `\WP_Error`，唯 docblock 撒谎**）；is_wp_error 桩加 `@phpstan-assert-if-true \WP_Error` → 3 条 get_error_data method.nonObject 级联清零；表单适配器 phpdoc `mixed>>}` 拼写 10 + 原生 `: ?array` 返回类型 10（translate_main/translate_fallback/translate/from_contact_form 全家族）；聚合器/维度/bands docblock 真形状 8（metrics 行实为 `[date,type,key,value]` 位置元组——docblock 原声称 assoc 是谎言；`'0'`/`'100'` 数字字符串键被 phpstan 规范化成 int 键）；`Gr_Queue::enqueue` args 契约改 `array<array-key, mixed>`（动作参数位置列表惯例——5 个调用点参数错误级联清零）；ANON_V4/V6_PREFIX 接线（substr 魔法数字 3/6 → 常量/8，自文档化零行为变化）；CHANNEL_WIDTH/OPTIONAL 错层死常量删 2（宽度归写入层 substr+DDL 管）；`array_values(array_slice(...))` 空操作删 1（array_slice 默认重建 int 键索引）；foreach 键上多余 `is_scalar` 删 2（PHP 数组键恒 int|string）；`'strlen'` 字符串回调 → 显式 typed 闭包 1 | 实测 2026-09-13：**phpstan level 6 clean（`tools/phpstan-run.sh` 退出 0，含 baseline 后零未处理错误）**；**零动态属性赋值**（level 6 全量无该类错误出现 = 静态证）；终态四检查全绿：syntax 0 / phpcs 0 / compat 0 / **phpunit 649 tests 3878 assertions 全绿**（适配器抽象签名加 `: ?array` 后测试匿名子类同步跟进）；**坑位实录**：①FrankenPHP argv 断裂三连——TurboProcessRestarter 无限重启（`-d` 旗标被当文件名、restart 标记 `-d phpstan.restarted=1` 永不注册 → argv 逐层嵌套；解法：ini 直写 `phpstan.restarted=1`，get_cfg_var 读到即跳重启）、并行 worker 子进程把 PHPRC ini 路径当控制台命令（"Command \"/private/tmp/gr-php.ini\" is not defined"；`maximumNumberOfProcesses: 1` 仍派 1 个 worker——唯 `--debug` 进程内单线程稳态）、`--debug` 往 stdout 打分析文件清单（污染 JSON 输出 → raw+grep 过滤）；②**AS 桩教训**：首版 AS 函数追加进 bootstrap 加载的 wpcli-stub.php → 测试运行时 `function_exists('as_schedule_single_action')` 恒真 → `Gr_Queue` 恒走 AS 路径 → **15 个 wp-cron 回退测试崩**（649 全红实弹教训：**分析桩与运行时桩必须分离**——AS 桩独立成 action-scheduler-stubs.php 只进 scanFiles 不进 bootstrap，测试运行时保持无 AS 才能测回退路径；Woo 主机路径由 dev 站 live 覆盖）；③phpunit 匿名子类必须跟随抽象签名演进（抽象方法加返回类型 → 匿名类不同步即 PHP fatal "declaration must be compatible"）；④`parallel` 参数是 map 不是标量（`maximumNumberOfProcesses` 子键） | **baseline 16 条不删的如实理由**：防御性 `is_array`/`isset` 是对**运行时现实比桩宽**的真防御——真 wpdb `get_results`/`get_row` 失败返 null、`dns_get_record` 失败返 false、`$_POST`/`$_GET` 畸形 SAPI 下可非数组、`wp_login_failed` 钩子参数可传 null（生态插件无纪律）、`Gr_Cli::register` 的 `!WP_CLI` 守卫被 wpcli 桩把常量冻结为 true（生产标准守卫式）、privacy-api 对 `wc_get_orders` 结果的 instanceof 防御对 Woo 生态契约——删它们等于用分析器静默换真实世界鲁棒性；baseline 纪律入 neon 尾注（缩不增） | `11` §2、ADR-0007 |
| T5 | 兼容性静态检查 | ✅ | 沿用 S9 交付的 `phpcs-compat.xml.dist` 包装器（PHPCompatibilityWP 经 installed_paths 双写 ruleset `<config>` 绕本机 dealerdirect 写 conf 失败 + assets 排除——vendored 第三方非我方源码只生噪音）；本行为正式收口记录，零新代码（T4 全部改动过同一门） | 实测 2026-09-13：`vendor/bin/phpcs --standard=phpcs-compat.xml.dist --runtime-set testVersion 7.4- plugin/` **exit 0——全量 plugin/（含 T4 新增适配器返回类型/常量接线/typed 闭包等全部改动）零 PHP 8+ 语法发现**；`str_*` polyfill 夹具复跑（tests/fixtures/str_polyfill_fixture.php）**exit 0 零误报**（str_contains/str_starts_with/str_ends_with 为 WP 6.0+ 核心 wp-includes/compat.php 自带 polyfill——`14` §1 实核、S9 确认 + 本日复验）；belt-and-braces 禁用清单 grep（match 表达式 / enum / `?->` / readonly / 命名参数 / 参数尾逗号）四命中**逐一核验全为合法**：class-gr-event.php:6 注释里解释"PHP 7.4 无 readonly"的说明文字、`Gr_Ip_Matcher::match(` 静态**方法名**非 match 表达式（×3 处）、`string ...$rule_ids` 变长参数（AGENTS §4 允许清单内） | 无跳过项（本任务纯验证，全绿） | ADR-0003/0007 |
| T6 | 生命周期冒烟 | ✅ | 无代码改动（纯验证；生命周期三件套 S5 期交付——`Gr_Activator` = Settings::install + Schema::install + Queue::ensure_daily；`Gr_Deactivator` = Queue::teardown（双排程 + 互斥瞬态 + AS 待定全清）；`Gr_Schema::maybe_upgrade` 版本门（stored < DB_VERSION 才 install，dbDelta 对齐表零操作）） | 实测 2026-09-13（:8091 四相 + 取证）：**停用相**——`wp plugin deactivate` → 排程双清（daily/send 皆 false）+ 互斥瞬态清 + **数据全留**（15 表在、设置选项 md5 逐字节不变、`gr_db_version` 不动）+ 前台 200 零 gr-probe（停用态 autoloader 未挂、插件类不可用——验证改原生 SQL 按表名模式）；**重激活相**——排程恢复、设置 md5 仍不变（install 幂等不重置站长选择）、15 表、前台 200 + gr-probe ×3 回归、队列后端 action-scheduler；**升级幂等相**——强制版本滞后（`gr_db_version`→0）→ maybe_upgrade 修复 + 版本回写 '1'；**query 过滤器 DDL 捕获：install() 0 条 DDL / 升级 0 条 / 稳态二次调用 0 条**（dbDelta 对齐表零操作的三重实证）；strip AUTO_INCREMENT 后 15 表 SHOW CREATE 零漂移；**取证插曲**——首版快照对比误报"security_logs+events 漂移"→ 铁证链：`.frm` 文件 mtime 全 2026-09-10 未动（零结构变更）+ 计数器暴力试算（相位 A hash = 今日文本配 AUTO_INCREMENT=5824/368/1740）→ **全部为计数器随真实流量移动，零 DDL**（教训：SHOW CREATE 含易变计数器，快照必须 strip 后再 hash）；**背景发现**——重激活后站点每分钟一条 `security_conclusion` 事件（空 visitor、`{"suspected_bot":true,"bot_tier":"high"}`——本机某分钟级轮询被 bot 引擎判高疑；04:33–05:33 插件停用窗零事件 = 插件行为正确，轮询属环境背景）；清理：curl 派生身份（同 IP+UA+日折叠同一 visitor id）会话/触点/事件删净 + 当日 51 条 conclusion 噪音行删（事件表归 0——T3 清扫后表内全部为今日探针/轮询噪音）、debug.log 探针噪声 127 行裁回 2126 | **如实不做项**：卸载两模式 live 臂——同 T2 理由（dev 站插件目录 symlink 指向仓库源树，`wp plugin uninstall` 穿删源码）；两模式语义由 LifecycleTest 单测覆盖（守卫序先于任何加载、`gr_delete_data_on_uninstall` 默认 keep 显式 opt-in 才删）；live 卸载留 CI 独立环境（wp-env） | `11` §1、`05` §5/§7 |
| T7 | 常用插件共存冒烟（原 SQLite 冒烟任务已废止） | ✅ | 无代码改动（纯共存验证；适配器/漂移哨兵/探测器 C11–C12 期交付） | 实测 2026-09-13（:8091）：**全载矩阵**——14 插件同活（E9 五件 cf7/wpforms-lite/elementor/wp-super-cache/wordpress-seo + 生态七件 akismet/automatorwp/cartflows/fluent-crm/wp-slimstat/stream/user-submitted-posts + 基线 woo/fluentform/limit-login）→ 前台 200（冷载 3.03s）+ gr-probe ×3 + 零 fatal 标记；**collect 端点** 200 stored:true + no-cache 头（WPSC 激活态同款）；**生态探测器**四桥全挂（woo/fluentform/cf7/wpforms active=true 各带正确 plugin 文件名）——退矩阵后 cf7/wpforms 正确转 false（目标不在=零挂载）；**漂移告警实弹可见**——仅回退钩提交 → shutdown 哨兵 → `gr_bridge_drift` 真站触发（fluentform:99002–99005），consent off 时告警照发行零写（门控正确）；**consent on + 真捕获 gr_attr cookie 注入** → 转化行 fluentform/99005/**77.50/USD**/visitor=77f2586a78（嵌套 payment.total/currency 语义提取 + cookie 身份绑定，全载环境全链无冲突）；**坑位实录**：①Elementor shutdown logger 往 stdout 打印污染 wp-cli 输出捕获——`tail -1` 抓令牌抓到 Elementor 行 → 401 假象，精确 grep 64hex 行解决；②注入 cookie 值带 `gr_attr=` 前缀 → 签名校验必败、行零写（探针 bug 伪装插件缺陷，剥前缀后即绑定成功）；③我的探针 fatal 2 笔（裸 Gr_Database 全局名第 4 次、臆测 Gr_Attribution_Service::visitor_id 方法名）；**dev 基建插曲**：12 插件停用批后 :8091 frankenphp php-server 进程死亡（前台连接拒）——WP-CLI 全程可用、按 greenpng-dev/NOTES.md 记载命令重启后 200/probe 复原（dev 单进程服务器对重负载请求脆弱，非插件缺陷；停用批经独立 WP-CLI 进程执行、死亡时序耦合未定论，如实记录）；**清理**：11 插件退回 inactive（基线 4 active 复原）、设置选项**字节级 md5 复原**（a1eaf940…/19 键——补回被我多删的 marketing_consent_fallback=0，教训：基线要存键清单不只 md5）、探针转化行/两代 curl visitor（cookie 派生 77f2586a 与匿名派生 f5718ac61d）行删净、事件表归 0、debug.log 223 行增量（Elementor 弃用 111+ 其路径 substring 命中 greenpng + automatorwp 引用 wp-plug 参考树文件的预置 Warning + 探针 fatal）裁回 2126 | 无跳过项；两笔探针 bug 与服务器死亡时序如实披露 | ADR-0007、`11` §5、`02` §2.6 |
| T8 | 版本与打包 | ✅ | 无代码改动（S10 期交付的 `tools/bump-version.sh` + `tools/build-zip.sh`（含 `--strict` 门）+ `tools/pack-geoip.php` 即本任务工具；版本三处自开发基线即 1.0.0） | 实测 2026-09-13：**版本三处同步**——plugin 头 `Version: 1.0.0` / `GR_VERSION '1.0.0'` / readme `Stable tag: 1.0.0`（zip 内实测同三处）；**bump 守卫验证**——`./tools/bump-version.sh 1.0.0` 正确拒绝（"already at 1.0.0" exit 1，防半改树机制按设计工作）；**`./tools/build-zip.sh --strict` 构建成功**——`dist/greenpng-1.0.0.zip` **110 文件 / 4.6MB**，`unzip -t` 零错误；**包内禁入项零命中**（vendor/测试/文档/composer/phpcs/phpunit/tools/.md 全不在——`assets/js/vendor/` 的 uplot 捆绑为**有意运行时资产**、带 uplot-license.txt，非开发 vendor 树）；**数据文件入包且字节一致**——DB-IP 三件（gr-geoip-v4.bin / v6.bin / json）与 CrawlerDetect 两件（gr-ua-crawlers.txt 1468 模式 / gr-ua-exclusions.txt 52 模式）cmp 逐字节同源；**NOTICE 归属就位**——CrawlerDetect 段（MIT + substantial-portions 完整许可文本 + 18/18 判定对齐实证注记 + 数据日期）与 DB-IP 段（CC BY 4.0 + 来源 + pack-geoip.php 重打包溯源 + 归属满足声明）；languages/greenpng.pot、uninstall.php、readme.txt、入口文件齐备；dist/ 为 gitignored 目录、zip 永不入库（AGENTS §7） | 无跳过项（POT 再生成与 readme 截图为 T10 后置项，不在本行口径） | `02` §5、`08` §8 |
| T9 | WP.org 自查 | ✅ | **`LICENSE` 补齐**——仓库根本无 GPLv2 全文（§1 第 2 项唯一硬缺口，本任务实测发现并修复：自本机 WP 7.1 的 license.txt 取正典正文 39–384 行、剥 WP 自家 WRITTEN OFFER 页脚，339 行就位）；其余零代码改动 | 实测 2026-09-13（`08` 清单 9 节逐项 + 自动化扫描）：**§1 许可证**——头 `GPLv2 or later`+URI ✓ / LICENSE ✓（本行交付）/ 零运行时第三方 PHP 依赖（uplot MIT 资产 + 数据文件 MIT/CC BY 4.0 皆兼容）✓ / BSL 零复制（`01`/`14` 既证）✓ / 数据 NOTICE+readme 双归属 ✓（T8）；**§2 可读性**——无混淆、minified 资产同目录同版本源码（zip 内 uplot.iife.min.js + uplot.iife.js 并存，T8 实证）✓ / External services 三项逐项披露 ✓（V4 定稿）；**§3 零外呼**——URL 全清单扫描零自有域（仅 db-ip/graph.facebook/google-analytics 三个站长配置型端点 + 注释引用）✓ / 零心跳遥测 ✓ / 集成默认关（I 系实测）✓ / DB-IP 本地预置零外呼 + 更新仅按钮 ✓ / 蜘蛛订阅默认关 ✓ / 探针双模块披露三件套 ✓（V4/V5）；**§4 数据与隐私**——exporters/erasers 真站注册 ✓（V2）/ 隐私政策文本含双轨说明 ✓（V4）/ IP 双轨 ✓（V1）/ 无指纹标识符（探针仅类别信号）✓ / CRM 仅布尔+标签 ✓ / 弃购邮件默认关 ✓ / uninstall 默认 keep ✓（T6）；**§5 安全基线**——prepare 全量 ✓（phpcs + 裸 query 扫描零命中）/ 转义 ✓ / cap+nonce ✓ / REST 零 `__return_true` ✓ / 零 eval/create_function/test_runner/auto_prepend/mu-plugins（唯一命中为 payload-inspector **注释**描述其检测目标）✓ / REMOTE_ADDR 默认 ✓（V1）/ 密钥加密+诊断脱敏 ✓ / 插件目录外零写入（运行时 fopen 仅 `'rb'` 读数据；packer 为开发工具）✓ / 无独立 collect.php（REST 控制器唯一路径）✓；**§6 UI**——text domain 字面量 ✓（PrefixDisciplineTest）/ 零硬编码文案 ✓（phpcs）/ 零 CDN ✓（扫描）/ 零竞品名/Pro/Premium/License key ✓（扫描）/ 1 顶级菜单+子菜单、资源仅本插件页 ✓（C 系）；**§7 readme**——Contributors/Tags 4≤5/Requires 6.0/Tested 7.1/PHP 7.4/Stable 1.0.0 ✓ / 名称无商标前缀 ✓ / FAQ 数据存放说明 ✓（V4）/ DB-IP 归属+NOTICE 指针 ✓；**§8 打包**——T8 全证 ✓（SVN-Git tag 对应归 T10 人工）；**§9 高风险表**——仅记录默认（Googlebot 误封零风险）、探针三件套、数据归属全就位 ✓ | **如实未竟项→已补齐（2026-09-13 同日）**：①readme 截图——`wporg-assets/screenshot-1..5.png`（agent-browser 真登录 :8091 后台实拍五页：Dashboard/Settings/Traffic & Security/Campaigns/Funnels & Goals，1280px 宽，标题逐页核实；**SVN /assets/ 侧资产，不入 zip**——重建后包内零 screenshot 命中、110 文件不变），readme 增 `== Screenshots ==` 五条图注；②POT 再生成——S8 版仅 6 msgid（早于全部后台页落地），`wp i18n make-pot` 重生成 **339 msgid**（X-Domain/Report-Msgid-Bugs-To 头三要素核对，--slug 避符号链接 realpath 陷阱），**zip 重建携新 POT**；至此 08 清单 42 项全过、无未竟项 | `08` 全文 |
| T10 | 提交上线 | ⬜ | WordPress.org svn 提交（人工执行） | — | `08` |
| T11 | 公共仓库 CI 矩阵（ADR-0008） | ✅ | 公共仓库 `greenpng/wp-greenpng` 五工作流：Unit 纯 PHP 矩阵（7.4–8.5 七格，8.4/8.5 实验位）、Static·Security（phpcs+PHPCompatibilityWP+phpstan level 6+JS 组件+gitleaks）、WP integration（wp-env 4 格：PHP 8.1/8.3 × WP 6.0/7.1，15 表建齐/dbDelta 零漂移/collect 全臂/限流/生命周期/卸载双模式）、WP floor（`wordpress:6.0-php7.4`+`mysql:5.7` compose 栈，PHP 7.4 与 MySQL 5.7 运行时地板）、E2E Playwright（WP 7.1+PHP 8.1+Woo+CF7，11 顾客模拟规格）；峰值并发 17<20；`tests/integration/run.sh` 双栈共用断言臂 | 实测 2026-09-13（第 1–17 轮迭代，ADR-0008 六段勘误全录）：**五线全绿**——Unit 657 tests 3,902 assertions（7 格）、Static 三 job、Integration 4 格、Floor 7 臂（含删数据卸载真跑）、E2E 11/11；**揪出五处真实插件缺陷并修复（单测覆盖）**：①停用/卸载遗留 gr_ 待执行 wp-cron 事件（dev 站带 AS 不可复现，唯 CI 无 AS 栈暴露）；②两个 WP_List_Table 页空渲染（未声明 `_column_headers`，核心从注册屏解析列头而本插件页面未注册屏）；③`Gr_Queue::enqueue` 对 AS 拒绝零防御——AS 3.x 表未建时 DBStore 抛 RuntimeException 击穿前台请求、派发两头落空，现 try/catch+wp-cron 回退；④Woo 带外支付完成丢归因——同意是请求作用域，网关 webhook/CLI 永不带同意，现结账时快照 `_gr_marketing_consent` 随 visitor 落单；⑤（连带）`catch ( Throwable )` 命名空间陷阱——解析为不存在的 `GreenPNG\Core\Throwable` 什么都捕不住，须 `\Throwable`；**环境定案**：wp-env 无 MySQL 旋钮（services 键拒绝）→ 5.7 地板独立 compose 工作流；wp-env PHP 7.4 格上游不可修（EOL bullseye apt 404）移除，7.4 证明保留地板栈；E2E 栈共享 docker 桥地址——折叠键（地址+规则+小时窗）不含 UA，健康检查 curl 永占窗，爬虫命中的按代理可见性是抽签 → 规格以计数增量+裁决列断言（诚实化三轮收敛）；**测试器自身教训全录 ADR-0008**：Playwright 文本引擎不读 script 体、wp-env 参数层吃方括号/星号值（eval-file+PHP 字面量免疫）、CF7 模板在 `_form` meta、Woo payment_complete 仅从未支付状态触发（Store API COD 单生即 processing，完成步须 pending→payment_complete 模拟卡网关 webhook）、本机 PHPUnit @runInSeparateProcess 因 PHP_BINARY=argv[0] 自繁进程链（改显式 CLI 子进程）、README/ADR 随轮更新 | ADR-0008、`11`、`08` |

## 8. 决议记录（原开放问题已全部收口）

| 编号 | 决议（2026-09-09） | 落点 |
| :--- | :--- | :--- |
| OQ-1 | 独立 Settings 页设立，页面上限解除（19→20 页，去重纪律保留） | ADR-0007 / `06` / U13 |
| OQ-2 | MySQL-only，不做 SQLite 兼容承诺；浪涌折叠用 MySQL 原生 upsert | ADR-0007 / `05` / W6 |
| OQ-3 | WP 6.0 下界维持；兼容成本被证实过高时经复审上调（触发点已记录于 ADR-0007） | ADR-0007 |
| OQ-4 | 测试矩阵覆盖站长常用插件（E9 补装 + T2/T7 矩阵） | ADR-0007 / `11` |
| Q-安全IP | 完整 IP 存储 + 展示脱敏 + 匿名化开关（默认完整） | ADR-0007 / `05` / W14 / V1 |
| Q-访客身份 | cookie visitor_id（30 天，同意门控）为主 + 无 cookie 每日盐回退 | ADR-0007 / `05` / C5 |
| Q-任务队列 | `Gr_Queue` 自适应（AS 嗅探 → WP-Cron + 互斥锁回落）；不打包 AS | ADR-0007 / `02` / S7 |
| Q-v1.0 范围 | **四支柱首发**：探针 / A-B / CAPI / GeoIP 全进 v1.0 | ADR-0007 / `12` / 本表 |

仍开放（非 v1.0 阻塞）：DB-IP 数据月度更新与构建流程（T8 前定稿）；Turnstile 渐进阈值（v1.2）；设备信号标签独立隐私评审（v1.3）；**C6 实测暴露**——`09` §1.1 REST collect ≤3 条口径与 `09` §3"约八成用户无对象缓存 + transient 兜底"的内部张力（无缓存主机实测 6 条、其中 §3 指定机制固有 3 条，下限 5 条；可选解：`09` §1.1 行为无缓存主机单列口径，或事件仓储改 prepare 直写省 1 条后仍 5 条，T 阶段统一测量时定夺，见 `09` §1.1 落地记录）。

## 9. 提交与验收纪律

- 一任务一提交，提交信息 `feat|fix|docs: <摘要>`；不提交 `vendor/`（运行时）与构建产物（AGENTS §7）。
- 每提交前跑 AGENTS §3.3 四项检查；验收标准一栏的数字必须来自实测并写入提交说明。
- 本清单的状态列是唯一进度真源；与其他记录冲突时以本表为准。

## 10. Phase 7 — v1.0.1 热修复（ISS-09 评审落地）

> 依据链：`iss/ISS-09` 同事深度审计（C1~C5）→ `15` 逐条事实核验（C1/C2/C3 实锤、C4 收窄为生即 paid 订单、C5 线索留存收 v1.1 而"默示同意"驳回）→ `adr/0009` 决策记录 D1~D5 → 本表。G1（ISS-08 遗留 P1 缺口）随 H1 一并闭环；退款冲销与表单线索留存归 v1.1（`12`/`15` §4）。

| ID | 任务 | 状态 | 交付物 | 验收标准（实测） | 依据 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| H1 | G1 五控件 + D5 架构不变式 | ✅ | `Gr_Settings_Page` Security 标签新增 5 控件（`bot_verdict_threshold` / `login_fail_threshold` / `login_lockout_base` / `honeypot_enabled` / `blackhole_enabled`）；`save_security()` 钳制写入（阈值 1..100、失败阈值 2..100、锁定基数 60..86400；**缺省字段落默认值，不落 0**）；snapshot 扩至 16 键随审计 diff；`Gr_Settings::defaults()` 增 `bot_verdict_threshold=70`（D1 双信号佐证）。**不变式测试**：`Gr_Settings::defaults()` 标量键 ⊆ Settings 页可写键集（Retention/Analytics 两页所属键为记录在案的豁免）——G1 的产生机制被测试堵死 | 实测 2026-09-13：phpunit SettingsPageTest 11→15 例全绿（渲染含 5 控件与默认值 70/5/300；`1`/`999999`/`250` 越界钳至 2/86400/100 且审计 diff 含 `bot_verdict_threshold`；缺省 dial 回落 5/300/70；不变式零缺失键）；:8091 Security 标签实存五控件 | `15` §2、`adr/0009` D1/D5 |
| H2 | 会话仓两挂载 | ✅ | `Gr_Session_Repository::apply_probe_score()`（独立 UPDATE：`bot_score = GREATEST(bot_score, %d)` 只升不降 + `is_bot = IF(判定=1, 1, is_bot)` 粘滞；输入钳 0..100）与 `mark_session_bot()`（只写 `is_bot` 不动探针分值）；`touch()` SQL 字节不动（前台预算形状保持，`09` §1.1；ISS-09 补丁 1 的 touch 改法据此驳回） | 实测 2026-09-13：phpunit SessionRepositoryTest 4→8 例全绿（GREATEST/IF 形状与 WHERE 双键、阈值下永不清除、250→100 钳制、mark 不含 bot_score）；:8091 实弹——`signal(bot_score=87)` 后会话行 `is_bot=1, bot_score=87` | `adr/0009` D2 |
| H3 | 采集端判定接线 | ✅ | `Gr_Collect_Controller::handle()` signal 分支接 `apply_probe_score()`；`verdict_threshold()` 读 `bot_verdict_threshold`（钳 1..100）——服务端定罪、客户端只报分值（ISS-09 补丁 2 的硬编码 80 据此改设置键默认 70） | 实测 2026-09-13：phpunit CollectControllerTest 18→22 例全绿（87≥70 写双列 / 40<70 只写分值 / pageview 两列零触碰 / 阈值改 90 后 80 不定罪） | `adr/0009` D1/D2 |
| H4 | 结论通道 shutdown 标记 | ✅ | `Gr_Security_Conclusions`：high 档结论布防一次性 PHP shutdown 标记（`arm_marker()` 幂等——多规则一请求仍一次；`mark_current_session()` Throwable 隔离；`register_shutdown_function('shutdown_action_hook')` 实核，wp_die 后亦执行，故陷阱路径的结论也有机会落行）；medium/人类结论不布防；登录/注册/陷阱等无会话行路径如实零行（ISS-09 补丁 3 的 init 挂载点据此后移） | 实测 2026-09-13：phpunit SecurityConclusionsTest 10→13 例全绿（high 布防 shutdown 回调 / medium+空+record 均不布防 / 双 high 规则恰一次布防 + mark 只写 is_bot） | `adr/0009` D2 |
| H5 | 检查器前门执法 | ✅ | `Gr_Access_Rules::is_static_banned()`（仅手写封禁臂、不含临时锁——两臂分立谓词，允许列表优先）；`Gr_Request_Inspector::run()` 挂 `front_door()`：静态封禁无条件 403（`action_taken='blocked'`，W12 同款形状）、临时锁仅 block 档、URL 允许只免检测、Throwable fail-open 报 `gr_inspector_error`；拒绝直写 `Gr_Security_Log_Repository` **不入结论通道** | 实测 2026-09-13：phpunit RequestInspectorTest 8→12 例（403 先于检测器 + `ip_ban`/`blocked` 折叠行 + 结论通道零喂入；log 档过 / block 档拒；URL 豁免免检测不免封禁；前门故障 fail-open）+ AccessRulesTest 8→10 例（静态臂忽略临时锁、允许列表胜静态臂）全绿；:8091 实弹——封禁 127.0.0.1 后前台与 REST 双 403 + `ip_ban` 折叠行，删规则后 200 复原 | `adr/0009` D3 |
| H6 | Woo 状态钩子 | ✅ | `woocommerce_order_status_processing` / `woocommerce_order_status_completed` 复用 `complete_payment()`（ADR-0008 round 14 实证：Store API 线下网关订单生即 processing，`payment_complete` 结构性不可达——C4 真实洞=生而 paid 订单）；幂等双防线（meta 锁 + `gr_conversions` UNIQUE）折叠双发 | 实测 2026-09-13：phpunit WooCommerceAdapterTest 11→12 例全绿（挂载 3→5 钩断言；born-paid 新例——processing 触发即绑定、`payment_complete`+`completed` 双重放后绑定查询数不变）；:8091 实弹——订单 43（带 visitor/consent meta）pending→processing 即绑（`woocommerce/43/42.50/USD`），completed 重放 `attributed=1` 仍恰 1 行 | `adr/0009` D4、ADR-0008 round 14 |
| H7 | e2e 09 自然 COD 路径 + 05 表盘 | ✅ | 09 规格：Store API COD 结账**即**断言绑定（生即 paid 路径不再依赖 stand-in 完成步）+ `conversion-count` 重放坍缩断言；ci-seed 增 `conversion-count` 任务；05 规格：Security 表盘控件保存往返断言（90 落盘、PRG 回显、复位 70） | 实测 2026-09-13（公共仓库 v1.0.1 推送，五工作流 main + tag 双跑 10/10 绿）：E2E **12/12 通过**（11→12）——`the engine dials save from the security tab` ✓ 6.1s（G1 表盘真 UI 往返）、`a customer buys a product and the paid order is attributed` ✓ 18.6s（生即 processing 结账即绑定 + 卡网关重放计数不变）；Unit 679 tests 3,978 assertions（7 格）、Static/WP integration/WP floor 同批全绿 | `15` §4 |
