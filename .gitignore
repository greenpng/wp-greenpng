# Dev tooling: installed, never committed (AGENTS §7).
/vendor/
/.phpunit.cache/
/.phpunit.result.cache
composer.phar

# Build outputs (tools/build-zip.sh, S10).
/dist/
*.zip

# OS noise.
.DS_Store

# PHPStan result cache (tools/phpstan-run.sh).
/.phpstan-tmp/

# Node dev tooling (ADR-0008): installed by npm ci, never committed.
/node_modules/

# wp-env runtime configs are generated per CI job (ADR-0008).
/.wp-env.json
/.wp-env.override.json
/.wp-env-plugins/

# Playwright output (ADR-0008).
/test-results/
/playwright-report/
